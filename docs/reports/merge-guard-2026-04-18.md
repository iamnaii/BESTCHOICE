# Pre-Merge Guard Report — 2026-04-18

Generated by: Pre-Merge Guard agent  
Date: 2026-04-18  
Branches reviewed: 3 of 73 unmerged (selected by recency + impact)

---

## Branch 1: `fix/fraud-audit-p1-quickwins`

**Author:** Akenarin Kongdach (iamnaii@)  
**Commits:** 1 (`fix(fraud-audit): ปิดช่องโหว่ Phase 1 quickwins (8 รายการ)`)  
**Size:** 14 files changed, 495 insertions(+), 116 deletions(-)

### Changes Summary

| File | Nature |
|------|--------|
| `commission.service.ts` | Added SoD: salesperson cannot self-approve own commission |
| `commission.service.spec.ts` | New tests for SoD on `approve()` + `approvePayout()` |
| `kyc.service.ts` | **Removed dev-mode OTP bypass** (security fix) |
| `kyc.service.spec.ts` | Updated tests — now assert OTP always validates |
| `loyalty.service.ts` | Replaced non-atomic idempotency check with `updateMany` CAS + `$transaction(Serializable)` |
| `loyalty.service.spec.ts` | New tests for race-safe referral award |
| `dashboard.controller.ts` | Removed SALES from `@Roles` — financial KPIs need-to-know |
| `dashboard.controller.spec.ts` | New test asserting SALES never appears in effective roles |
| `health.controller.ts` | Split `/health` (public, no details) vs `/health/detailed` (OWNER/FINANCE_MANAGER auth'd) |
| `credit-check.service.ts` | Fixed wrong model ID `claude-sonnet-4-20250514` → `claude-sonnet-4-5-20250514` |
| `DashboardPage/index.tsx` | Added `enabled: canReadDashboard` guard on all 7 dashboard queries |

### Issues Found

**Critical:** None  
**Warning:** None  
**Info:** None

All changes are security hardening or test coverage improvements. The KYC dev-bypass removal (`if (process.env.NODE_ENV !== 'production') { return verified: true }`) is a P0 fix — that bypass would auto-accept any OTP in staging/dev if NODE_ENV was accidentally set wrong in prod, or in any non-production environment. The health endpoint split correctly separates probe availability from ops information disclosure.

### Recommendation: ✅ APPROVE

---

## Branch 2: `refactor/ui-design-tokens-2026-04-17`

**Author:** Akenarin Kongdach (iamnaii@)  
**Commits:** Multiple (Phases 7–8 + chat fixes)  
**Size:** 159 files changed, 1952 insertions(+), 1292 deletions(-)

### Changes Summary

This branch has two distinct layers:

**Layer A — Design token refactor (151 web files):** Replaces hardcoded Tailwind color classes (`text-gray-*`, `bg-gray-*`, hardcoded hex values) with semantic CSS variable tokens (`text-muted-foreground`, `bg-background`, `border-border`, etc.) across 32 component/hook/lib/constant files. No logic changes.

**Layer B — Chat engine fixes (7 API files):**

| File | Nature |
|------|--------|
| `chat-adapters.module.ts` | Registers all 5 adapters via `OnModuleInit.registerAdapter()` (previously injected via DI token that was unreachable due to module scope) |
| `line-finance.adapter.ts` | Fixed `isConfigured` call: was `adapter.isConfigured` (property), now `await adapter.isConfigured()` (async method) |
| `message-router.service.ts` | Added public `registerAdapter()` / `registerDomainHandler()` (idempotent); `sendStaffMessage` now returns `{ success, error? }` instead of `void` |
| `staff-chat.gateway.ts` | Emits `chat:message:send-failed` to the sending staff when LINE/FB delivery fails |
| `facebook-domain.module.ts` | Registers `FacebookDomainHandler` via `OnModuleInit` (same pattern as adapters) |
| `migration.sql` | Adds `response_type` + `media_url` to `canned_responses` table |
| `chat-events.ts` | Adds `MESSAGE_SEND_FAILED` constant |

### Issues Found

**Critical:** None  
**Warning:**
- `Number(p.amountPaid) > 0` in one LIFF component — used for a CSS conditional only (not arithmetic/storage). Not a financial calculation bug but technically deviates from the "no `Number()` on money fields" rule. Risk is minimal (display-only).

**Info:**
- Branch is large (159 files). The two layers are conceptually separate and could have been two PRs for easier review. No blocker, just a note for future hygiene.
- Chat engine fixes are bundled with the UI refactor; they appear unrelated. Git log shows they were committed to the same branch during the same session.

### Recommendation: ⚠️ REVIEW

Recommend reviewer confirm the chat engine `OnModuleInit` wiring doesn't create duplicate adapter registrations on hot-reload in dev (the `registerAdapter` method is idempotent by adapter identity so this should be safe, but worth a smoke test). The `Number(p.amountPaid)` in the LIFF display component is low risk but should be noted.

---

## Branch 3: `fix/users-page-polish`

**Author:** Akenarin Kongdach (iamnaii@)  
**Commits:** 3  
**Size:** 18 files changed, 1093 insertions(+), 348 deletions(-)

### Changes Summary

| File | Nature |
|------|--------|
| `trade-in.service.ts` | **New feature:** auto-create `Product` (PHONE_USED, PHOTO_PENDING) on TradeIn `ACCEPTED` — includes IMEI uniqueness guard, branch validation |
| `voucher.service.ts` | PDF perf: inline base64 logo, table layout fix |
| `users.service.ts` | Tracks `lastLoginAt` on user record |
| `auth.service.ts` | Sets `lastLoginAt` on successful login |
| `customers.service.ts` | Minor query fix |
| `migration.sql` | Adds `last_login_at` column to `users` table |
| `UsersPage/index.tsx` + `UserTable.tsx` | UI polish: last login column, proper `invalidateQueries` on all mutations |
| `CustomersPage.tsx` | UI polish |
| `TradeInPage/` components | Table polish to match design system |
| `CreditChecksPage/` components | Table polish + filters |

### Issues Found

**Critical:** None

**Warning:**
- `apps/api/src/modules/trade-in/trade-in.service.ts` — new code stores money value in JSON blob using `Number(costPrice)`:
  ```ts
  // costPrice is Prisma.Decimal
  checklistResults: {
    agreedPrice: Number(costPrice),   // ← Warning: Decimal → Number in JSON
  } as unknown as Prisma.InputJsonValue,
  ```
  This is a JSONB field (not a `@db.Decimal` column), so it's not a DB precision issue. However `Number()` on `Prisma.Decimal` can lose precision for amounts ≥ 2^53 (≈ 9 quadrillion THB — not a realistic risk). The project rule "ห้ามใช้ `Number()` สำหรับจำนวนเงิน" technically applies. Recommend `costPrice.toFixed(2)` to store as string instead for consistency.
  
  _Note: pre-existing `Number(tradeIn.agreedPrice)` in `voucher.service.ts` lines 53 + 99 is unchanged from `main` — not introduced by this branch._

**Info:**
- `trade-in.service.ts` new block (~56 lines) in `accept()` method is not covered by new tests in this branch. The auto-create-Product logic is complex (IMEI check, branch guard, stock entry) and warrants unit tests.
- `schema.prisma` adds `lastLoginAt DateTime?` — migration present, backwards-compatible (nullable field). ✅

### Recommendation: ⚠️ REVIEW

Fix `Number(costPrice)` → `costPrice.toFixed(2)` or `costPrice.toString()` in `checklistResults`, and add unit tests for the new auto-create-Product logic before merging.

---

## Summary Table

| Branch | Files | Critical | Warning | Info | Verdict |
|--------|-------|----------|---------|------|---------|
| `fix/fraud-audit-p1-quickwins` | 14 | 0 | 0 | 0 | ✅ APPROVE |
| `refactor/ui-design-tokens-2026-04-17` | 159 | 0 | 1 | 1 | ⚠️ REVIEW |
| `fix/users-page-polish` | 18 | 0 | 1 | 1 | ⚠️ REVIEW |

### Branches Skipped (no valid merge base with main)
- `feat/accounting-audit-fixes` — orphan branch (no common ancestor with `origin/main`)
- `feat/chatbot-production-ready` — same issue

### Branches Not Reviewed (de-prioritized)
- `chore/deps-tier3-*` (10 branches) — dependency updates, low business logic risk
- `claude/*` (11 branches) — auto-generated planning/doc branches
- `guard/review-*` (22 branches) — previous guard reports
