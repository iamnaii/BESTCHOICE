# Pre-Merge Guard Report — 2026-04-28

Generated by: Pre-Merge Guard Agent  
Date: 2026-04-28  
Branches reviewed: 3 (most recently active, excluding watchdog/guard/deps branches)

---

## Branch 1: `feat/collections-partial-payment-escalate`

**Author:** Akenarin Kongdach + Claude  
**Last commit:** 2026-04-28 12:02 +0700  
**Base:** `origin/main`  
**Commits ahead:** 3

### File Changes Summary
30 files changed, 3,817 insertions(+), 1,936 deletions(−)  
*(Note: ~3,810 lines of the diff are package-lock.json churn from weekly dep updates)*

Key code changes:
- `apps/api/src/modules/overdue/overdue.controller.ts` — 2 new endpoints
- `apps/api/src/modules/overdue/dto/escalate.dto.ts` — new DTO (14 lines)
- `apps/api/src/modules/overdue/dto/partial-payment-reschedule.dto.ts` — new DTO (59 lines)
- `apps/api/src/modules/overdue/overdue.service.ts` — 369 lines added (partialPaymentReschedule + escalate methods)
- `apps/api/src/modules/overdue/queue.service.ts` — 48 lines added
- `apps/web/src/pages/CollectionsPage/components/PartialPaymentRescheduleDialog.tsx` — new, 320 lines
- `apps/web/src/pages/CollectionsPage/hooks/usePartialPaymentReschedule.ts` — new, 46 lines
- `apps/web/src/pages/CollectionsPage/hooks/useEscalate.ts` — new, 39 lines
- `apps/api/prisma/schema.prisma` — 4 lines added (minor field addition)

### Security & Quality Checks

#### Critical
*None found.*

| Check | Result |
|-------|--------|
| `@UseGuards(JwtAuthGuard, RolesGuard)` on OverdueController | ✅ Present at class level |
| `@Roles()` on new `partialPaymentReschedule` endpoint | ✅ `OWNER, BRANCH_MANAGER, SALES, FINANCE_MANAGER` |
| `@Roles()` on new `escalate` endpoint | ✅ `OWNER, BRANCH_MANAGER, FINANCE_MANAGER, SALES` |
| `Number()` on DB-stored money fields | ✅ None — service uses `new Prisma.Decimal(dto.amountPaid)` |
| `deletedAt: null` in new queries | ✅ Both new `findFirst` calls include it |
| Hardcoded secrets/API keys | ✅ None |
| Raw `$queryRaw` (SQL injection risk) | ✅ None |
| Raw `fetch()` in React components | ✅ None — uses `api.post()` from `@/lib/api` |

#### Warning
*None found.*

| Check | Result |
|-------|--------|
| DTO validation decorators with Thai messages | ✅ Both DTOs have full validation + Thai messages |
| `queryClient.invalidateQueries()` after mutations | ✅ Both `usePartialPaymentReschedule` and `useEscalate` hooks call it |
| Error handling on new service methods | ✅ Both methods throw typed NestJS exceptions |

#### Info

- `outstandingAfter = outstandingBefore.sub(paid).toNumber()` — Decimal arithmetic for response payload only (not stored to DB). Acceptable pattern.
- `Number(settlementAmount)` and `Number(amountPaid)` in `PartialPaymentRescheduleDialog.tsx` — frontend display formatting only. Acceptable.
- SALES can call `/escalate` but LEGAL action is internally gated to `OWNER/FINANCE_MANAGER` inside service (`ForbiddenException` on `action === 'LEGAL'` for other roles). Segregation of duties is maintained — but this dual-layer gate means SALES submitting LEGAL will get a 403 *after* the request reaches the server. Minor UX gap; the FE already disables the LEGAL option for non-OWNER/FM roles via `useEscalate.ESCALATION_BROKEN_PROMISE_THRESHOLD`. 

### Recommendation: ✅ APPROVE

---

## Branch 2: `feat/collections-promise-to-pay-lifecycle`

**Author:** Akenarin Kongdach + Claude  
**Last commit:** 2026-04-28 10:59 +0700  
**Base:** `origin/main`  
**Commits ahead:** 33

### File Changes Summary
44 files changed, 7,373 insertions(+), 525 deletions(−)

Key code changes:
- **Prisma schema** — `PromiseSlot` model + lifecycle fields on `CallLog` + `Contract.keptPromiseCount`
- **2 new migrations** — promise-slots table + lifecycle column additions
- **New services** — `promise.service.ts` (225 lines), `mdm-lock.service.ts` (81 lines)
- **New crons** — `promise-resolution.cron.ts` (231 lines), `no-promise-lock.cron.ts` (136 lines); replaces `broken-promise.cron.ts`
- **`payments.service.ts`** — 143 lines added (`checkPromiseAfterPayment` non-blocking hook)
- **`overdue.service.ts`** — 189 lines added (`getCycleDeadline`, `getOverdueInstallments`)
- **`overdue.controller.ts`** — 2 new GET endpoints
- **Frontend** — `ContactLogDialog.tsx` refactored (631 lines total), new `InstallmentPickerPopover.tsx`, `SupersedePromiseConfirmDialog.tsx`, `usePromiseSlots.ts`
- **E2E** — 2 new Playwright specs (`promise-lifecycle-happy.spec.ts`, `promise-supersede.spec.ts`)
- **Backfill script** — `apps/api/scripts/backfill-promise-slots.ts`

### Security & Quality Checks

#### Critical
*None found.*

| Check | Result |
|-------|--------|
| `@UseGuards` on new controller endpoints | ✅ New endpoints inherit class-level `JwtAuthGuard, RolesGuard, BranchGuard` |
| `@Roles()` on `getCycleDeadline` and `getOverdueInstallments` | ✅ All roles including ACCOUNTANT |
| `Number()` on DB-stored money fields | ✅ None — all DB writes use `Prisma.Decimal`; `.toNumber()` only on response payloads |
| `deletedAt: null` in new queries | ✅ Present in `promise.service.ts`, `promise-resolution.cron.ts` |
| Hardcoded secrets/API keys | ✅ None |
| Raw `$queryRaw` | ✅ None |
| Raw `fetch()` in React components | ✅ `api.get()` used for both new REST calls in `ContactLogDialog.tsx` |

#### Warning

1. **`(p: any)` parameter in `promise-resolution.cron.ts:86`** (`resolvePromise` method)  
   The Prisma include type for a deeply-nested `callLog.findMany` result isn't inferred — escaped with `eslint-disable`. Should be typed via `Prisma.CallLogGetPayload<{include: {slots: true}}>`. Not blocking, but weakens refactor safety.

2. **Multiple `as any` casts in `overdue.service.ts` (lines 1295–1309)** — `getCycleDeadline` reads `active.cycleDeadline`, `active.slots`, `active.settlementAmount` etc. via `(active as any).field`. These are Prisma type inference limitations on a complex `findFirst` with include, but proper typed selects or `Prisma.CallLogGetPayload` would eliminate all 6 casts.

3. **`user.findFirst` in `promise-resolution.cron.ts` without `deletedAt: null`** — querying for `isSystemUser: true`. System users are not expected to be soft-deleted, but to be consistent with project conventions (`deletedAt: null` on every `findFirst`/`findMany`) this should be added.

4. **`ContactLogDialog.tsx` is 610 lines** — above the 500-line info threshold. The file is complex by design (N-slot promise manager + supersede + installment picker), but could be split further by extracting the slot-list section into a `PromiseSlotManager` sub-component.

#### Info

- `Number(s.settlementAmount)` in `queue.service.ts` — mapping Prisma.Decimal → number for the `activePromise.slots` response shape. DB writes still use Decimal. Acceptable.
- `(sum._sum.amountPaid as Prisma.Decimal | null)?.toNumber()` — correct aggregate-sum handling. Acceptable.
- The `checkPromiseAfterPayment` hook in `payments.service.ts` is non-blocking (`.catch(Sentry.captureException)`) — correct pattern per v2 hardening.
- `createPromise` uses `Serializable` isolation to prevent race where two collectors simultaneously create an active promise. This aligns with v3 PaySolutions atomicity pattern.

### Recommendation: ⚠️ REVIEW

Fix the 3 Warning items (2× `any` types, 1× missing `deletedAt: null`) before merge. Not blocking-critical, but degradation in type coverage in a complex service is a risk for future refactors.

---

## Branch 3: `chore/audit-quick-wins`

**Author:** Akenarin Kongdach  
**Last commit:** 2026-04-26 20:17 +0700  
**Base:** `origin/main`  
**Commits ahead:** ~5

### File Changes Summary
13 files changed, 209 insertions(+), 44 deletions(−)

Key code changes:
- **File upload hardening** — `ParseFilePipe` + `MaxFileSizeValidator` + `FileTypeValidator` added to `broadcast.controller.ts` and `line-oa.controller.ts` (image upload endpoints)
- **Query limit capping** — `customers.controller.ts` caps `limit` params at 100 via `Math.min()` (prevents DoS via huge result sets)
- **Dashboard aggregation** — `dashboard.service.ts` refactored to use `aggregate._sum` instead of per-row reduce (N+1 fix)
- **Throttle on public endpoints** — `web-widget.controller.ts` adds `@Throttle({ short: { limit: 30, ttl: 60_000 } })` on `POST /init` and 60/min on `GET /messages/:roomId`
- **Shop public endpoints** — `ShopBotDefenseGuard` added to `shop-auth-social.controller.ts`, `shop-installment-apply.controller.ts`, `shop-tracking.controller.ts`
- **Input DTO on `POST /init`** — replaces raw `{ visitorId?: string }` body with `InitWidgetDto` with `@IsString()` + `@MaxLength(64)`
- **Prisma migration** — 15 lines added (minor schema additions)

### Security & Quality Checks

#### Critical
*None found.*

| Check | Result |
|-------|--------|
| New controller endpoints with missing `@UseGuards` | ✅ None added; existing guards maintained |
| `Number()` on DB-stored money fields | ✅ None — only percentage ratio `Number(((overdueCount / totalContracts) * 100).toFixed(1))` which is correct |
| `deletedAt: null` in modified queries | ✅ All modified queries retain it |
| Hardcoded secrets | ✅ None |
| Raw SQL | ✅ None |

#### Warning

- **`@Body() body: any` on `createWithImage` in `line-oa.controller.ts:576`** — this is a pre-existing issue (not introduced by this branch) but was visible in the diff context. Should be typed with a proper DTO. Recommend creating a ticket rather than blocking this PR for a pre-existing issue.

#### Info

- File upload limits are sensible: 10MB for broadcast images, 1MB for rich-menu images.
- `Math.min(parseInt(limit) || 10, 100)` — correctly handles NaN via `|| 10` fallback and caps at 100. 
- `ShopBotDefenseGuard` on shop endpoints is appropriate for public-facing routes (not JWT-gated but rate-limited + bot-defense).
- This branch is purely hardening — no new business logic, low risk.

### Recommendation: ✅ APPROVE

---

## Summary

| Branch | Files Changed | Recommendation |
|--------|--------------|----------------|
| `feat/collections-partial-payment-escalate` | 30 (20 meaningful) | ✅ APPROVE |
| `feat/collections-promise-to-pay-lifecycle` | 44 | ⚠️ REVIEW (3 warnings) |
| `chore/audit-quick-wins` | 13 | ✅ APPROVE |

### Items to Fix Before Merging `feat/collections-promise-to-pay-lifecycle`

1. `promise-resolution.cron.ts:86` — Type `resolvePromise(p: any, ...)` → use `Prisma.CallLogGetPayload<{include: {slots: true}}>` or an extracted interface
2. `overdue.service.ts:1295–1309` — Replace `(active as any).field` casts with a typed `Prisma.CallLogGetPayload` or a local interface matching the findFirst include shape
3. `promise-resolution.cron.ts` (user lookup) — Add `deletedAt: null` to `user.findFirst({ where: { isSystemUser: true } })`
