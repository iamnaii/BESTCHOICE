# Pre-Merge Guard Report — 2026-06-01

> Generated by Pre-Merge Guard agent. Reviewed the 3 most recently active unmerged branches against `origin/main`.

---

## Summary

| Branch | Author | Commits | Files | Recommendation |
|--------|--------|---------|-------|----------------|
| `feat/contact-rich-fields` | Akenarin Kongdach | 5 | 7 | ✅ APPROVE |
| `fix/fb-webhook-integration-config` | Akenarin Kongdach | 1 | 3 | ✅ APPROVE |
| `fix/letters-e2e-sales-assertion` | Akenarin Kongdach | 1 | 1 | ✅ APPROVE |

No blocking issues found across all 3 branches.

---

## Branch 1: `feat/contact-rich-fields`

**Commit range:** 5 commits ahead of `main`  
**Changed files:** `contacts.service.ts`, `contacts.service.spec.ts`, `contacts.ts` (web api lib), `ContactDetailPage.tsx`, `ContactDetailPage.test.tsx`, + 2 docs  
**Net change:** +592 insertions / −100 deletions

### What this branch does
Upgrades the Contact detail page from a simple linked-name list to rich read-through cards. Each linked entity (Supplier, Customer, External Finance, Trade-In seller) now shows its key fields inline with a deep-link into its source module page. Backend expands the `findOne` SELECT to include these fields.

### Critical — None found

### Warning — None found

- Frontend API calls go through `contactsApi.detail()` → `api.get()` (correct pattern) ✅
- No raw `fetch()` or `axios` calls ✅
- No mutations on this page → no `invalidateQueries` needed ✅
- No `Number()` on financial fields ✅

### Info

1. **PII protection is correct** — `customers.select` deliberately excludes `addressCurrent` and `addressIdCard`. The spec comment and the new unit test (`ContactDetailPage.test.tsx` + `contacts.service.spec.ts`) both verify this invariant. Good defensive practice.

2. **Controller guards intact** — `contacts.controller.ts` retains `@UseGuards(JwtAuthGuard, RolesGuard)` at class level with `@Roles(...)` on all methods. No changes to controller in this branch.

3. **ContactDetailPage.tsx is 258 lines** — reasonable for a multi-section detail page. The decomposition into `SupplierCard`, `CustomerCard`, `FinanceCard`, `TradeInCard` sub-components is clean.

4. **`peakContactCode` field** — used in the page but already exists on the base `Contact` interface in `main` (line 8). No type drift.

### Recommendation: ✅ APPROVE

---

## Branch 2: `fix/fb-webhook-integration-config`

**Commit range:** 1 commit ahead of `main`  
**Changed files:** `chat-adapters.module.ts`, `facebook-webhook.controller.ts`, `facebook-webhook.controller.spec.ts`  
**Net change:** +96 insertions / −11 deletions

### What this branch does
Moves the Facebook webhook's `FB_VERIFY_TOKEN` and `FB_APP_SECRET` resolution from `ConfigService` (environment variables only) to `IntegrationConfigService` (database config with environment variable fallback). This lets the owner update the webhook secrets via the Settings → Integrations UI without redeploying.

### Critical — None found

- Controller remains in the documented intentionally-public exception list (security.md: `chatbot-finance-liff, sms-webhook, paysolutions, address`) — `facebook-webhook` falls in this category with its own `@SkipCsrf()` decorator ✅
- No guards were removed ✅
- No `Number()` on financial fields ✅
- No `deletedAt` query changes ✅

### Warning — None found

- "Fail-closed" behavior is **correct and improved**: empty/missing `verifyToken` now returns 400 (previously an empty env var would silently accept any token). This is a security improvement.
- `verifySignature` made `async` — caller site updated with `await` ✅
- `IntegrationsModule` properly imported in `ChatAdaptersModule` ✅

### Info

1. **New tests are thorough** — 3 new describe blocks cover: (a) challenge accepted on matching token, (b) 400 on wrong token, (c) 400 when token is empty string (fail-closed). Good coverage.

2. **`getAppSecret()` private helper** — normalizes `cfg.appSecret || undefined` cleanly so callers uniformly get `undefined` rather than empty string.

3. **`handleDataDeletion` and `handleDeauthorize`** both updated to use the new `getAppSecret()` helper consistently ✅

### Recommendation: ✅ APPROVE

---

## Branch 3: `fix/letters-e2e-sales-assertion`

**Commit range:** 1 commit ahead of `main`  
**Changed files:** `apps/web/e2e/letters-page.spec.ts`  
**Net change:** +8 insertions / −4 deletions

### What this branch does
Fixes a brittle E2E assertion: the old test used `getByRole('button', { name: 'ยกเลิก' })` to confirm that SALES users cannot see a Cancel button. This was flaky because the "CANCELLED" status tab also contains the text "ยกเลิก", making Playwright match the tab instead of (or in addition to) the row button.

The fix changes the assertion to verify that SALES users can successfully **access** the `/letters` page (correct concern — was the role guard allowing access?) and documents that the cancel-button RBAC is validated at the API level + component unit test level.

### Critical — None found

### Warning — None found

### Info

1. The new assertion `expect(page.getByRole('heading', { name: 'จัดการจดหมาย' })).toBeVisible()` is the right level of confidence for a role-access smoke test.

2. The comment explaining _why_ the old assertion was removed (`CANCELLED` tab text collision) is accurate and prevents someone from re-introducing the same brittle assertion.

### Recommendation: ✅ APPROVE

---

## Checklist (all branches)

| Check | B1 contact-rich-fields | B2 fb-webhook | B3 letters-e2e |
|-------|----------------------|---------------|----------------|
| `@UseGuards(JwtAuthGuard)` on new controllers | n/a (no new controller) | n/a (intentionally public, documented) | n/a (no controller) |
| `@Roles()` on all controller methods | n/a | n/a | n/a |
| No `Number()` on money fields | ✅ | ✅ | ✅ |
| `deletedAt: null` in new queries | ✅ | ✅ | ✅ |
| No hardcoded secrets | ✅ | ✅ | ✅ |
| No unparameterized `$queryRaw` | ✅ | ✅ | ✅ |
| DTO validation decorators present | ✅ (no new DTOs) | ✅ (no new DTOs) | ✅ |
| Frontend uses `api.get/post()` | ✅ | ✅ | ✅ |
| `invalidateQueries` after mutations | ✅ (read-only) | ✅ (no mutations) | ✅ |
| Thai validation messages on new DTOs | ✅ (no new DTOs) | ✅ (no new DTOs) | ✅ |
