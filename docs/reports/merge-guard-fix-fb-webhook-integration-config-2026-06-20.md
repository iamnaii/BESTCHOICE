# Merge Guard Report — fix/fb-webhook-integration-config

**Date**: 2026-06-20  
**Branch**: `fix/fb-webhook-integration-config`  
**Author**: Akenarin Kongdach (iamnaii@MacBook-Pro-khxng-Akenarin.local)  
**Latest commit**: `72da1b25` — 2026-05-28  
**Commits ahead of main**: 18 (includes letters PDF + fix stack from parent `fix/letters-e2e-sales-assertion`)

---

## Summary

This branch fixes Facebook webhook inbound verification to read `verifyToken` and `appSecret` from `IntegrationConfig` (DB with env fallback) instead of env-only. This makes inbound webhook handling consistent with outbound paths, which already read from `IntegrationConfig`. Before this fix, production webhook verification would fail silently unless env vars were duplicated separately from the Settings UI. The branch also carries 11 letters-PDF commits from its parent branch.

### Key commit unique to this branch
- `72da1b25` — `fix(facebook-webhook): resolve verify token + app secret from IntegrationConfig`
  - 3 files changed: `chat-adapters.module.ts`, `facebook-webhook.controller.ts`, `facebook-webhook.controller.spec.ts`
  - 96 insertions, 11 deletions

---

## File Changes

| File | Change |
|------|--------|
| `chat-adapters/chat-adapters.module.ts` | +`IntegrationsModule` import |
| `chat-adapters/facebook-webhook.controller.ts` | Read verifyToken + appSecret from `IntegrationConfigService` (DB → env fallback) |
| `chat-adapters/facebook-webhook.controller.spec.ts` | 68 lines — new test suite for verify token from IntegrationConfig |

---

## Issues Found

### Critical
_None_

### Warning
_None_

### Info

- **[I-1] Controller intentionally public — confirmed correct**: `FacebookWebhookController` has no `JwtAuthGuard` per design. Listed in `security.md` as an intentionally public endpoint (`paysolutions`, `sms-webhook`, etc.). The controller uses FB's own verification: HMAC-SHA256 on POST + challenge token on GET. This is the correct pattern for webhook receivers.

- **[I-2] `$queryRaw` — safe**: The only `$queryRaw` in changed files is `SELECT current_database()` in the backfill CLI (not in this branch). No `$queryRaw` appears in the Facebook webhook changes. ✓

- **[I-3] Fail-closed on missing config**: Both `verifyWebhook` (GET) and `verifySignature` (POST) now return 400 when `verifyToken` / `appSecret` is null/empty. This is the correct fail-closed behavior. ✓

- **[I-4] Test coverage**: 3 new test cases added — challenge success, token mismatch, missing-config (fail closed). ✓

---

## Recommendation

**✅ APPROVE**

The fix is narrowly scoped, well-tested, and closes a real production gap (settings-UI-configured FB tokens were silently ignored). No security regressions. The change pattern mirrors how outbound FB API calls already work.

---
_Generated by Pre-Merge Guard — 2026-06-20_
