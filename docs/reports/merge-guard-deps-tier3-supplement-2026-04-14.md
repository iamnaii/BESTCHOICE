# Pre-Merge Guard Report — deps-tier3 Supplement — 2026-04-14

Generated by: Pre-Merge Guard Agent  
Date: 2026-04-14  
Context: Supplemental review of 7 `chore/deps-tier3-chunk*` branches skipped in the earlier main report.

---

## Branches Scanned

| Branch | Files Changed | Insertions | Deletions | Summary |
|--------|-------------|-----------|-----------|---------|
| `chore/deps-tier3-chunk3-class-validator` | 2 | +37 | -109 | class-validator 0.14 → 0.15 |
| `chore/deps-tier3-chunk4-eslint9` | 8 | +871 | -108 | ESLint 8 → 9 flat config |
| `chore/deps-tier3-chunk5-react19` | 3 | +65 | -10 | React 18 → 19 |
| `chore/deps-tier3-chunk7-vite8` | 4 | +1,219 | -1,553 | Vite 6 → 8 (Rolldown) |
| `chore/deps-tier3-chunk8-zod4` | 6 | +22 | -14 | Zod 3 → 4 |
| `chore/deps-tier3-chunk10-zustand` | 2 | +44 | -115 | Zustand 4 → 5 |
| `chore/deps-tier3-chunk11-lucide-react` | 2 | +37 | -109 | lucide-react 0.400 → 1.8 |

---

## Branch 1: `chore/deps-tier3-chunk3-class-validator`

**Commit:** `bfa87edf` — "chore(api): bump class-validator ^0.14.1 → ^0.15.1 — Tier 3 chunk 3"

### Summary
Version bump of `class-validator` in `apps/api/package.json`. No code changes — only `package.json` and `package-lock.json` modified. class-validator 0.15 is a minor release with no breaking changes for the decorators used in this project.

### Issues
None.

### Recommendation: **APPROVE**
Package-only bump. Run `./tools/check-types.sh api` + `./tools/run-tests.sh --skip-e2e` before merge to confirm no DTO validation regressions.

---

## Branch 2: `chore/deps-tier3-chunk4-eslint9`

**Commit:** `4d129859` — "chore: migrate ESLint 8 → 9 + flat config — Tier 3 chunk 4"

### Summary of Changes
- Deleted `apps/api/.eslintrc.json` and `apps/web/.eslintrc.json` (legacy format)
- Created `apps/api/eslint.config.mjs` and `apps/web/eslint.config.mjs` (flat config)
- Updated deps: `eslint ~8.57` → `^9.39.4`, replaced `@typescript-eslint/eslint-plugin` + `@typescript-eslint/parser` with `typescript-eslint ^8.58.1`, added `globals ^17.4.0`, added `@eslint/js ^9.39.4`
- **Code fix in `apps/web/src/pages/StockTransfersPage.tsx`:** Corrected broken `alt` attribute — changed `|| 'รูปสินค้า'` (short-circuit on truthy brand, not on empty string) to proper ternary: `t.product.brand ? \`...\` : 'รูปสินค้า'`. This was required to satisfy `jsx-a11y/alt-text`.
- Web config now excludes `playwright-report/` (previously not excluded)

### Issues

#### 🟡 WARNING

**[W-1] CI lint scripts may need updating**  
The previous `.eslintrc.json` files are deleted. Any CI script calling `eslint --ext .ts,.tsx` (ESLint 8 flag) will fail with ESLint 9 flat config. ESLint 9 infers extensions automatically from the config.  
**Verify:** Check `.github/workflows/deploy.yml` for any `npx eslint --ext` flags.

#### ℹ️ INFO

**[I-1] `no-empty` rule retained in web config** — correctly set to `'off'` matching legacy behavior.  
**[I-2] `jsx-a11y/alt-text` rule preserved as `'error'`** — good.  
**[I-3] No test infra change** — jest and vitest environments are declared via `globals.jest`/`globals.browser` (correct for flat config).

### Recommendation: **APPROVE** (after CI check)
Migration follows ESLint 9 flat config best practices. The StockTransfersPage fix is a genuine a11y improvement. Verify CI pipeline doesn't pass `--ext` flags.

---

## Branch 3: `chore/deps-tier3-chunk5-react19`

**Commit:** `8a7e4797` — "chore(web): bump react ^18.3.0 → ^19.2.5 — Tier 3 chunk 5"

### Summary
Bumps `react` + `react-dom` from 18.3 → 19.2.5, and `@types/react` + `@types/react-dom` to their React 19 versions. Changes are limited to `package.json` and `package-lock.json` only — no source code modified.

### Issues

#### 🟡 WARNING

**[W-1] React 19 is a major version with breaking changes**  
React 19 removes legacy APIs (`createRoot` replaces `ReactDOM.render`, `act()` import moved, etc.). This project uses `@testing-library/react ^16.3.2` which supports React 19. However, any use of deprecated `ReactDOM.render`, string refs, or legacy `contextTypes` APIs will silently break.  
**Verify:** Run `./tools/check-types.sh web` and the full web test suite before merge.

**[W-2] Missing peer dependency audit**  
`react-day-picker`, `react-qr-code`, `react-resizable-panels` may have peer dependency constraints. Check `npm ls react --depth=0` after merge to confirm no peer conflicts.

### Recommendation: **REVIEW**
Run `./tools/check-types.sh web` + `./tools/run-tests.sh` + smoke-test key pages (POS, Customers, Contracts, LIFF) before merge.

---

## Branch 4: `chore/deps-tier3-chunk7-vite8`

**Commit:** `a4d3a628` — "chore(web): bump vite 6→8, @vitejs/plugin-react 4→6, vitest 2→4 — Tier 3 chunk 7"

### Summary of Changes
- `vite`: `^6.x` → `^8.x`, `@vitejs/plugin-react`: `^4.3` → `^6.x`, `vitest`: `^2.x` → `^4.x`
- `apps/web/vite.config.ts`: `rollupOptions` → `rolldownOptions` (Vite 8 uses Rolldown, the Rust-based bundler)
- `manualChunks`: changed from static object to **function form** (required by Rolldown)
- **Removed `target: 'es2020'`** from build config
- `apps/web/src/contexts/AuthContext.test.tsx`: Updated one test — the description changed from "stays unauthenticated and skips /auth/me" to "stays unauthenticated when /auth/me fails (no valid session)" and the mock now expects `apiGetMock` to be called with `/auth/me` (with a 401 response), reversing the previous assertion that it was NOT called.

### Issues

#### 🟡 WARNING

**[W-1] Removed `target: 'es2020'` from build config**  
The previous config explicitly set `target: 'es2020'`. Vite 8 defaults to a modern target (`es2022` or higher). This could affect users on older browsers (Chrome <94, Firefox <93, Safari <15). Given this is a Thai B2B internal app accessed via modern LINE browsers and Chrome, this is low risk — but should be confirmed.  
**Fix if needed:** Re-add `build: { target: 'es2020' }` if support for older browsers is required.

**[W-2] `rolldownOptions` is Vite 8 experimental**  
Rolldown support in Vite 8 is stable for production but `rolldownOptions` differs from `rollupOptions` in some edge cases (plugin compatibility, output format). Manual chunk splitting via function is correct syntax for Rolldown.  
**Verify:** Run `npm run build` in `apps/web` and check that all chunk files are generated (vendor, query, liff, excel, pdf, charts).

**[W-3] AuthContext test behavior change**  
The test update implies `AuthContext` now **always** calls `/auth/me` on init (even when no token is in memory), relying on the refresh cookie. This is a behavioral change from the previous "skip /auth/me when no token" logic. Ensure the auth flow has been regression-tested — this change could affect initial load performance.

#### ℹ️ INFO

**[I-1] `manualChunks` function now checks `node_modules/react-router/`** instead of `react-router-dom/` — consistent with chunk9 (react-router v7 migration).  
**[I-2] vitest 2→4 bump** — verify vitest config syntax is compatible.

### Recommendation: **REVIEW**
Run `npm run build` (check chunk output), `./tools/run-tests.sh`, and verify auth flow on staging before merge.

---

## Branch 5: `chore/deps-tier3-chunk8-zod4`

**Commit:** `9e117394` — "chore(web): bump zod ^3.25.76 → ^4.3.6 — Tier 3 chunk 8"

### Summary of Changes
- `apps/web/src/lib/schemas.ts`: Changed import to `from 'zod/v4'` (compat layer for Zod 4)
- **`isForeigner: z.boolean().default(false)` → `z.boolean()`** — `.default()` removed
- `z.enum({ required_error: ... })` → `z.enum({ error: ... })` — Zod 4 API change
- `apps/web/src/pages/CustomersPage.tsx`, `POSPage.tsx`, `ContractCreatePage/components/PlanDetailsStep.tsx`: `zodResolver` → `standardSchemaResolver` from `@hookform/resolvers/standard-schema`
- Removed `// eslint-disable-next-line @typescript-eslint/no-explicit-any` cast on CustomersPage form resolver

### Issues

#### 🟡 WARNING

**[W-1] `isForeigner` field lost `.default(false)` — potential validation failure**  
`apps/web/src/lib/schemas.ts`, line 47 (approx):
```ts
// Before
isForeigner: z.boolean().default(false),
// After
isForeigner: z.boolean(),
```
Zod 4's `z.boolean()` without `.default()` requires the field to be explicitly `true` or `false`. If any form renders with `defaultValues` that doesn't include `isForeigner: false` (e.g., `defaultValues: {}` or `defaultValues: emptyForm` where `emptyForm` omits this field), the validation will fail with a type error on submit.  
**Verify:** Check `emptyForm` in `CustomersPage.tsx` to confirm `isForeigner: false` is in `defaultValues`. If not, either restore `.default(false)` or add it to `defaultValues`.

**[W-2] `standardSchemaResolver` is a new integration path**  
Switching from `@hookform/resolvers/zod` to `@hookform/resolvers/standard-schema` is the correct approach for Zod 4, but this is a less-tested code path in some react-hook-form versions. Verify the form validation behavior matches previous behavior on: Customer create/edit, POS sale, Contract plan step.

#### ℹ️ INFO

**[I-1] `required_error` → `error`** — correct Zod 4 API, no behavioral difference.  
**[I-2] Removed `as any` cast** — improvement, correctly typed by standardSchemaResolver.

### Recommendation: **REVIEW**
Verify `emptyForm` includes `isForeigner: false` [W-1]. Run `./tools/check-types.sh web` + manual smoke-test of Customer form and POS form before merge.

---

## Branch 6: `chore/deps-tier3-chunk10-zustand`

**Commit:** `28617418` — "chore(web): bump zustand ^4.5.7 → ^5.0.12 — Tier 3 chunk 10"

### Summary
Version bump of `zustand` in `apps/web/package.json`. No source code changes. Zustand 5 has minor breaking changes (removed `devtools` middleware option changes), but this project's Zustand usage should be unaffected.

### Issues
None observed in source diff (package + lock only).

### Recommendation: **APPROVE** (after type check)
Run `./tools/check-types.sh web` to confirm no Zustand API usage breakage.

---

## Branch 7: `chore/deps-tier3-chunk11-lucide-react`

**Commit:** `c4737e2c` — "chore(web): bump lucide-react ^0.400.0 → ^1.8.0 — Tier 3 chunk 11"

### Summary
Version bump of `lucide-react` in `apps/web/package.json`. No source code changes. lucide-react 1.x renamed several icons from the 0.x series. Any renamed icon will cause a TypeScript error at compile time (safe fail).

### Issues

#### 🟡 WARNING

**[W-1] lucide-react 1.x may have renamed icons**  
The jump from 0.400 to 1.8 crosses a major version boundary where some icon names changed.  
**Verify:** Run `./tools/check-types.sh web` — TypeScript will catch any renamed icons at compile time.

### Recommendation: **APPROVE** (after type check)
TypeScript will catch renamed icons. Run `./tools/check-types.sh web` before merge.

---

## Summary Table

| Branch | Critical | Warning | Info | Recommendation |
|--------|----------|---------|------|----------------|
| `chore/deps-tier3-chunk3-class-validator` | 0 | 0 | 0 | **APPROVE** |
| `chore/deps-tier3-chunk4-eslint9` | 0 | 1 | 3 | **APPROVE** (verify CI scripts) |
| `chore/deps-tier3-chunk5-react19` | 0 | 2 | 0 | **REVIEW** (smoke-test required) |
| `chore/deps-tier3-chunk7-vite8` | 0 | 3 | 2 | **REVIEW** (build + auth test) |
| `chore/deps-tier3-chunk8-zod4` | 0 | 2 | 2 | **REVIEW** (isForeigner default + form smoke) |
| `chore/deps-tier3-chunk10-zustand` | 0 | 0 | 0 | **APPROVE** (after type check) |
| `chore/deps-tier3-chunk11-lucide-react` | 0 | 1 | 0 | **APPROVE** (after type check) |

---

## Action Items

1. **`chore/deps-tier3-chunk4-eslint9`** — Check `.github/workflows/deploy.yml` for `--ext` flags. Then merge.

2. **`chore/deps-tier3-chunk5-react19`** — Run `./tools/check-types.sh web` + `./tools/run-tests.sh` + smoke-test POS, Customers, Contracts, LIFF pages. Check `npm ls react --depth=0` for peer conflicts.

3. **`chore/deps-tier3-chunk7-vite8`** — Run `npm run build` in `apps/web`, confirm all 6 chunks present (vendor, query, liff, excel, pdf, charts). Verify auth flow on staging.

4. **`chore/deps-tier3-chunk8-zod4`** — Confirm `emptyForm` in `CustomersPage.tsx` includes `isForeigner: false`. Run `./tools/check-types.sh web` + manual smoke-test Customer and POS forms.

5. **`chore/deps-tier3-chunk3-class-validator`, `chunk10-zustand`, `chunk11-lucide-react`** — Run `./tools/check-types.sh all` + merge in any order.

### Suggested merge order (to minimize conflicts)
```
chunk3-class-validator → chunk10-zustand → chunk11-lucide-react
→ chunk4-eslint9 → chunk8-zod4 → chunk5-react19 → chunk7-vite8
```
(ESLint before Zod/React since flat config affects linting of all subsequent changes)
