# Pre-Merge Guard Report — 2026-05-27

> Generated by Pre-Merge Guard agent  
> Base branch: `origin/main` (1f642c9b — fix(letters): drop evidence-upload)  
> Reviewed: 3 most-recently-modified non-guard branches

---

## Branch 1: `feat/canned-response-admin-redesign`

**Author:** Akenarin Kongdach  
**Last commit:** 2026-05-25  
**Size:** 59 files, +8 203 / -661 lines

### File Changes Summary
- `apps/api/prisma/schema.prisma` — new models: `CannedResponseBubble`, `CannedResponseQuickReply`, new `BubbleType`/`QuickReplyType` enums (+77 lines)
- 3 new DB migrations
- New services: `CannedResponseBubbleService`, `CannedResponseQuickReplyService`, `CannedResponseSenderService`, `BubbleTranslatorService`
- `StaffChatController` — +10 new endpoints (bubble CRUD, QR CRUD, send, reorder)
- New DTOs: `CreateBubbleDto`, `UpdateBubbleDto`, `CreateQuickReplyDto`, `UpdateQuickReplyDto`, `UpdateCannedResponseDto`
- Frontend: `CannedResponseAdminPage.tsx` refactored (381 lines), new sub-components `BubbleList`, `ChannelTabs`, `TemplateEditorPane`, `QuickReplyEditor`
- 5 new spec files (bubble translator, sender, bubble service, QR service, controller)

---

### 🔴 Critical Issues

**None found.**

- `StaffChatController` has `@UseGuards(JwtAuthGuard, RolesGuard)` at class level ✅
- All new endpoints have `@Roles(...)` decorators ✅
- No `Number()` on financial/Decimal fields — the `Number(payment.amountDue)` was *removed* (replaced with `this.formatDecimalThai()`) ✅
- All new DB queries include `deletedAt: null` filters ✅
- No hardcoded secrets or API keys ✅
- No `$queryRaw` usage ✅

---

### 🟡 Warning Issues

**W1 — `reorderBubbles` / `reorderQuickReplies` use inline body types instead of DTOs**

```ts
// controller
async reorderBubbles(@Body() body: { items: Array<{ id: string; sortOrder: number }> })
async reorderQuickReplies(@Body() body: { items: Array<{ id: string; sortOrder: number }> })
```

Neither uses a DTO class, so `ValidationPipe({ whitelist: true })` cannot strip extra fields, and there is no per-item validation that `id` is a valid UUID or that `sortOrder` is actually an integer. The *service* does validate max count (5 / 13), but per-item shape is unchecked at the HTTP layer.

**Recommendation:** Extract a `ReorderItemDto` with `@IsUUID()` + `@IsInt() @Min(0)` and wrap it in a proper DTO class.

**W2 — Thai validation messages missing in `UpdateCannedResponseDto`**

All `@MaxLength(...)` decorators in `UpdateCannedResponseDto` lack Thai-language `message` options. Other DTOs in the codebase (e.g. `CreateQuickReplyDto`) consistently provide Thai messages. Convention: `{ message: 'label ยาวเกิน 20 ตัวอักษร' }`.

**Recommendation:** Add Thai `message` strings to all decorators in `UpdateCannedResponseDto`.

**W3 — `json?: any` field type in `CreateBubbleDto` / `UpdateBubbleDto`**

The `json` field is typed `any` in both DTOs and the Prisma schema. While `@IsObject()` is present, deeply nested malicious payloads are not size-bounded. LINE's flex message JSON can be arbitrarily large.

**Recommendation:** Add `@MaxLength` on serialized JSON string or add a size guard in the service (`JSON.stringify(json).length > 50_000 → BadRequestException`).

**W4 — `StaffChatController` now 808 lines**

Exceeds the 500-line soft limit for easy reviewability. The file has grown from ~700 lines. Not a blocker but worth scheduling a split (e.g., move bubble/QR CRUD methods to a separate `CannedResponseAdminController`).

---

### 🔵 Info Issues

**I1 — `any` typed variables in spec/mock files**

Test files use `let prisma: { cannedResponseBubble: any }` as Prisma mock types. This is acceptable in test isolation but could be improved with `jest.Mocked<PrismaService>`.

**I2 — `cardToFbGenericTemplate` takes `any` argument**

`BubbleTranslatorService.cardToFbGenericTemplate(card: any)` — internal private method, low risk, but reduces type safety around Facebook template rendering.

---

### Recommendation: ✅ REVIEW

No critical blockers. The two warnings (W1, W2) should be addressed before merge but do not block security or data integrity. W3 is a best-practice hardening item.

---

## Branch 2: `feat/canned-response-postback-routing`

**Author:** Akenarin Kongdach  
**Last commit:** 2026-05-25  
**Size:** 15 files, +701 / -22 lines

### File Changes Summary
- New service: `QuickReplyPostbackRouterService` (156 lines) — parses `TEMPLATE:<id>` postback payloads
- `FacebookWebhookController` — wires router into FB postback handler, adds `deletedAt: null` query for room lookup
- `LineOaChatbotController` — adds `QuickReplyPostbackRouterService` injection
- 3 new spec files with solid test coverage (165 lines for postback router)
- Minor frontend: `CannedResponseAdminPage.tsx` +1 line, `QuickReplyEditor.tsx` +74 lines
- `CannedResponseSenderService` — +40 lines extending Phase 4a

---

### 🔴 Critical Issues

**None found.**

- `FacebookWebhookController` is an **intentionally public endpoint** (webhook verification via HMAC `x-hub-signature-256`) — listed in `security.md` as a known-public endpoint ✅
- `LineOaChatbotController` uses `LineWebhookGuard` for webhook signature verification ✅
- New `chatRoom.findFirst({ where: { ..., deletedAt: null } })` query correctly filters soft-deleted rooms ✅
- No `Number()` on financial fields ✅

---

### 🟡 Warning Issues

**W5 — In-memory rate limiting for postback loop guard (W7)**

`QuickReplyPostbackRouterService` uses an in-memory `Map<roomId, number[]>` for the loop guard (MAX 5 per 10s window per room). The code comment documents this is intentionally process-local and resets on restart. This is fine for a *defensive* guard but note:

- In a multi-replica deployment (Cloud Run ≥2 instances), each pod has an independent counter — the effective limit becomes `N × 5` per window across replicas
- The map grows unbounded until app restart (one entry per room that ever sent a postback)

This is documented in the code and is acceptable for now, but should be moved to Redis when multi-replica scaling is needed.

---

### 🔵 Info Issues

**I3 — `route()` returns silently on null room**

When `this.prisma.chatRoom.findFirst(...)` returns `null` (room not found), the router just returns `{ handled: false }` without logging. The outer webhook handler logs a no-op but doesn't capture the room-not-found case as a warning. Low impact but useful for debugging zombie postback replays.

---

### Recommendation: ✅ APPROVE

Clean implementation with good defensive coding (loop guard, graceful fallthrough to existing pipeline). W5 is informational. Ready to merge.

---

## Branch 3: `fix/letters-e2e-sales-assertion`

**Author:** Akenarin Kongdach  
**Last commit:** 2026-05-26  
**Size:** 1 file, +8 / -4 lines

### File Changes Summary
- `apps/web/e2e/letters-page.spec.ts` — updates the SALES-role test to assert page accessibility rather than absence of a cancel button

### 🔴 Critical Issues

None found.

### 🟡 Warning Issues

None found.

### 🔵 Info Issues

**I4 — Test scope narrowed**

The original test verified that SALES users cannot see the "ยกเลิก" bulk-cancel button. The replacement test only confirms the page loads (`/letters` is accessible and heading is visible). The code comment explains the original assertion was brittle because a "CANCELLED" status tab also contains "ยกเลิก" text. The comment points out that backend role enforcement (`403` for `POST .../cancel`) + unit tests cover the cancel-button suppression.

This is an acceptable trade-off, but worth a follow-up to add a more targeted assertion (e.g., checking the absence of the kebab-menu cancel option on a data row) if possible without brittleness.

---

### Recommendation: ✅ APPROVE

Trivial E2E fix with valid reasoning. No security or functional concerns.

---

## Summary Table

| Branch | Lines Changed | Critical | Warning | Info | Recommendation |
|--------|-------------|----------|---------|------|----------------|
| `feat/canned-response-admin-redesign` | +8,203 / -661 | 0 | 4 | 2 | ✅ REVIEW |
| `feat/canned-response-postback-routing` | +701 / -22 | 0 | 1 | 1 | ✅ APPROVE |
| `fix/letters-e2e-sales-assertion` | +8 / -4 | 0 | 0 | 1 | ✅ APPROVE |

### Action Items Before Merging `feat/canned-response-admin-redesign`

1. **[W1]** Add `ReorderItemDto` with `@IsUUID()` + `@IsInt() @Min(0)` for `reorderBubbles` and `reorderQuickReplies` endpoints
2. **[W2]** Add Thai `message` strings to `UpdateCannedResponseDto` decorators
3. **[W3]** *(Optional hardening)* Add JSON blob size guard for `json` field in bubble create/update
4. **[W4]** *(Tech debt)* Schedule `StaffChatController` split — already at 808 lines
