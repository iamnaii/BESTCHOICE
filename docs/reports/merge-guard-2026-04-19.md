# Pre-Merge Guard Report — 2026-04-19

Generated by: Pre-Merge Guard agent  
Date: 2026-04-19  
Branches reviewed: 3 (of 3 candidates selected)

---

## Branch 1: `fix/fraud-audit-p1-quickwins`

**Author:** Akenarin Kongdach  
**Commits:** 1 (`fix(fraud-audit): ปิดช่องโหว่ Phase 1 quickwins (8 รายการ)`)  
**Files changed:** 14 (2 migrations, schema, 4 services, 3 specs, 1 controller, 1 page)

### Changes Summary
- **commission.service.ts** — SoD: ForbiddenException when salesperson attempts to self-approve own commission or payout
- **dashboard.controller.ts** — Removed `SALES` from class-level `@Roles` (financial intelligence need-to-know boundary)
- **health.controller.ts** — Split `/health` (public minimal) from `/health/detailed` (OWNER/FINANCE_MANAGER guarded)
- **kyc.service.ts** — Removed dev-mode OTP bypass (`NODE_ENV !== 'production'`) that could silently fail in mis-configured prod
- **credit-check.service.ts** — Fixed wrong Claude model ID (`claude-sonnet-4-20250514` → `claude-sonnet-4-5-20250514`)
- **loyalty.service.ts** — Replaced non-atomic referral-award logic with `$transaction(Serializable)` + compare-and-swap `updateMany`
- **schema.prisma** — Added `referralAwardedAt DateTime?` idempotency marker on `Customer`
- **DashboardPage** — Added `enabled: canReadDashboard` guard on all 6 dashboard queries to suppress 403 floods for SALES users

### Issues Found

| Severity | Location | Issue |
|----------|----------|-------|
| — | — | No critical issues found |

### Verdict: ✅ APPROVE

All changes are security hardening. Patterns are correct:
- New guards properly use `JwtAuthGuard + RolesGuard`
- Decimal arithmetic untouched
- All soft-delete queries retain `deletedAt: null`
- New SoD checks throw appropriate `ForbiddenException`

---

## Branch 2: `refactor/ui-design-tokens-2026-04-17`

**Author:** Akenarin Kongdach  
**Commits:** 3 (chat adapter fix, E2E auth improvement, migration fix)  
**Files changed:** 159 (frontend UI, chat backend, E2E tests, schema)

### Changes Summary
- **Frontend (100+ TSX files)** — Replace hardcoded Tailwind colors (`text-green-600`, `text-red-600`, `text-red-500`, `text-emerald-600`, `bg-gray-*`) with semantic design tokens (`text-success`, `text-destructive`, `bg-muted`, etc.)
- **chat-engine/message-router.service.ts** — Exposed `registerAdapter()`/`registerDomainHandler()` as public methods for `OnModuleInit` wiring; `sendStaffMessage` now returns `{ success, error? }` instead of `void`
- **chat-adapters.module.ts** — Adapters self-register via `OnModuleInit` to avoid circular-import issue
- **e2e/global-setup.ts** — Pre-login all 5 roles sequentially (stays under 10 req/min throttle); share tokens across workers via `.playwright-roles-auth.json`
- **staff-chat.gateway.ts** — Surfaces `sendStaffMessage` failures to WS client as `send_error` event

### Issues Found

| Severity | Location | Issue |
|----------|----------|-------|
| Info | `PaymentHistorySheet.tsx`, `QuickBuyModal.tsx`, `LiffPayment.tsx` | `Number()` used on Decimal API values for display (`.toLocaleString()`). These are **pre-existing** (7 removed = 7 added; only color classes changed). Not introduced by this branch. |
| Info | `e2e/global-setup.ts` | Hardcoded test credentials (`admin1234`). These match documented dev accounts in CLAUDE.md — expected for E2E test setup. |
| Info | — | Very large PR (159 files) — mechanical token replacement. Risk is low but review surface is wide. |

### Verdict: ✅ APPROVE

The branch is mechanical design-token cleanup (no new `Number()` calls added, no Decimal arithmetic changed). Chat backend changes are safe and well-structured. E2E improvements are a net positive.

---

## Branch 3: `feat/defect-exchange-7day`

**Author:** Akenarin Kongdach  
**Commits:** 1 (`feat(defect-exchange): 7-day defect exchange for used phones`)  
**Files changed:** 18 (~905 LOC added)

### Changes Summary
- **New module** `defect-exchange` — Controller, Service, DTO, Module
- **DefectExchangeController** — Proper guards on all endpoints
  - `GET /defect-exchange/eligibility/:contractId` → `@Roles('OWNER', 'BRANCH_MANAGER', 'SALES')`
  - `POST /defect-exchange/execute` → `@Roles('OWNER', 'BRANCH_MANAGER')`
  - `GET /defect-exchange` → `@Roles('OWNER', 'BRANCH_MANAGER', 'FINANCE_MANAGER')`
- **DefectExchangeService** — Serializable `$transaction`, proper `Decimal` arithmetic in reduce, eligibility checks with 7-day window
- **contracts.service.ts** — Block duplicate active contracts; OWNER/BRANCH_MANAGER can override via `overrideActiveContractCheck`
- **customers.service.ts** — Search now returns `activeContractCount` field
- **Schema** — Added `DEFECT_EXCHANGED` enum, `DEFECT_RETURN` product status, `deviceReceivedAt` field on Contract
- **Frontend** — New `DefectExchangePage.tsx`, React Query patterns correct, `invalidateQueries` called after mutation

### Issues Found

| Severity | Location | Issue |
|----------|----------|-------|
| **Warning** | `defect-exchange.service.ts` line ~175 | `Number(p.amountPaid) > 0` used in `.filter()` predicate. For PHONE prices (max ~฿60,000) JS float precision is fine in practice, but prefer `new Decimal(p.amountPaid).greaterThan(0)` to stay consistent with project Decimal rules. The `.reduce()` that follows uses `new Decimal()` correctly. |
| Info | `defect-exchange.controller.ts` | `BranchGuard` not applied (same as sibling `exchange.controller.ts`). Service filters by `branchId` query param. Consistent with existing pattern — not a regression. |
| Info | `defect-exchange.service.ts` | `transferredCredit: paidInstallments.toNumber()` in audit metadata JSON and response object — correct (serialization to JSON). DB update uses `paidInstallments` (Decimal) directly. |

### Verdict: 🔍 REVIEW (1 Warning)

Module is structurally sound: guards present, Decimal arithmetic correct in the critical path, transactions at Serializable isolation, Thai validation messages in DTO. One warning on `Number()` in filter predicate — low risk for Thai phone prices but should be fixed for consistency.

**Suggested fix:**
```typescript
// Before
.filter((p) => p.status === 'PAID' || Number(p.amountPaid) > 0)

// After
.filter((p) => p.status === 'PAID' || new Decimal(p.amountPaid).greaterThan(0))
```

---

## Summary

| Branch | Files | Verdict | Blocker |
|--------|-------|---------|---------|
| `fix/fraud-audit-p1-quickwins` | 14 | ✅ APPROVE | None |
| `refactor/ui-design-tokens-2026-04-17` | 159 | ✅ APPROVE | None |
| `feat/defect-exchange-7day` | 18 | 🔍 REVIEW | 1 Warning (fixable) |

### Skipped Branches
The following branches were identified but skipped (no diff vs main, or no merge base):
- `fix/hardening-non-accounting` — fully merged into main (0 diff lines)
- `feat/accounting-audit-fixes` — no merge base with main (diverged history, cannot diff)
- All `guard/review-*` branches — guard-generated, not feature branches
- All `chore/deps-tier3-*` branches — dependency update chunks, not reviewed in this pass
