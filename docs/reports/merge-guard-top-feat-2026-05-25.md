# Pre-Merge Guard Report — 2026-05-25

Generated by Pre-Merge Guard agent. Reviewing 3 most-recently-active unmerged feature branches.

---

## Branch 1: `feat/canned-response-channel-tabs`

**Author:** Akenarin Kongdach  
**Last commit:** 2026-05-25 13:08 BKK  
**Commits on branch:** 2

### File Changes Summary
| File | Type | Lines |
|------|------|-------|
| `BubbleList.tsx` | Modified | +86 / -20 |
| `ChannelTabs.tsx` | New | +63 |
| `TemplateEditorPane.tsx` | Modified | +17 / -3 |
| `bubble-reorder-logic.ts` | New | +31 |
| `bubble-reorder-logic.test.ts` | New | +100 |

**Total:** 5 files, 277 insertions, 20 deletions — frontend-only.

### Issues Found

#### Critical
_None._

#### Warning
_None._

#### Info
- `BubbleList.tsx` uses `(r: any) => r.data` on the `useQuery` `queryFn`. Not a new pattern (existed before this branch), but the type could be tightened with the `CannedResponseBubble[]` type that's already imported.

### Notable Positives
- `bubble-reorder-logic.ts` correctly operates on `allBubbles` (not just the visible filtered set), preserving cross-channel sort order integrity.
- 100-line unit test file covers edge cases: hidden-bubble interleaving, universal bubbles, identity/no-op cases.
- `invalidateQueries` is called properly after all mutations.
- Uses design tokens only (`bg-primary`, `bg-muted`, `text-muted-foreground`) — no hardcoded hex.
- `leading-snug` present on all Thai text.

### Recommendation: **APPROVE** ✅

---

## Branch 2: `feat/canned-response-postback-routing`

**Author:** Akenarin Kongdach  
**Last commit:** 2026-05-25 13:02 BKK  
**Commits on branch:** 3

### File Changes Summary
| File | Type | Lines |
|------|------|-------|
| `chat-adapters.module.ts` | Modified | +12 / -3 |
| `facebook-webhook.controller.spec.ts` | Modified | +22 |
| `facebook-webhook.controller.ts` | Modified | +43 / -6 |
| `chatbot-finance.service.spec.ts` | Modified | +12 |
| `chatbot-finance.service.ts` | Modified | +40 / -4 |
| `line-oa-chatbot.controller.ts` | Modified | +38 / -6 |
| `line-oa.module.ts` | Modified | +6 / -1 |
| `canned-response-sender.service.spec.ts` | New | +103 |
| `canned-response-sender.service.ts` | Modified | +40 / -5 |
| `quick-reply-postback-router.service.spec.ts` | New | +165 |
| `quick-reply-postback-router.service.ts` | New | +156 |
| `staff-chat.module.ts` | Modified | +5 / -1 |
| `CannedResponseAdminPage.tsx` | Modified | +1 |
| `QuickReplyEditor.tsx` | Modified | +74 / -13 |
| `TemplateEditorPane.tsx` | Modified | +6 / -2 |

**Total:** 15 files, 701 insertions, 22 deletions — backend-heavy, some frontend.

### Issues Found

#### Critical
_None._

#### Warning
- **`facebook-webhook.controller.ts` is not in the documented public-endpoint allow-list** (`security.md`). The controller does have HMAC-SHA256 signature verification via `createHmac` + `timingSafeEqual` and has a JSDoc comment stating it is intentionally public — this is architecturally correct (mirrors the `paysolutions` pattern). However, the `security.md` allow-list should be updated to include `webhooks/facebook` to prevent future guard-check false positives.  
  _File:_ `apps/api/src/modules/chat-adapters/facebook-webhook.controller.ts`

- **In-memory sliding-window rate limiter in `QuickReplyPostbackRouterService`** (`recentSends: Map<string, number[]>`) is reset on app restart. On Cloud Run, horizontal scaling means each instance has its own map — a loop attack that hits multiple instances would bypass the cap. Low risk for current traffic, but worth documenting.

#### Info
- `QuickReplyEditor.tsx` uses `(r: any) => r.data` in `queryFn` callbacks — consistent with existing codebase pattern, but typed `CannedResponseQuickReply[]` is already available.
- `sendDirectMut` mutation in `QuickReplyEditor.tsx` does not call `invalidateQueries` after a "send directly" action — but this mutation sends a message to a chat room (fire-and-forget side-effect), not a data-model mutation, so no cache invalidation is needed. No issue.

### Notable Positives
- `deletedAt: null` present in all new Prisma `findFirst`/`findUnique` queries.
- No money fields touched — no `Number()` risk.
- No `$queryRaw` usage.
- 165-line spec for `QuickReplyPostbackRouterService` covers loop prevention, rate-limit, format mismatch fallthrough.
- `forwardRef()` used correctly to resolve the circular dependency between `ChatAdaptersModule` and `StaffChatModule`.

### Recommendation: **REVIEW** ⚠️

Action required before merge:
1. Add `webhooks/facebook` to the intentionally-public-endpoints allow-list in `security.md`.
2. Add a code comment to `QuickReplyPostbackRouterService.recentSends` noting the multi-instance caveat (or move to Redis if blast-radius becomes a concern).

---

## Branch 3: `feat/canned-response-admin-redesign`

**Author:** Akenarin Kongdach  
**Last commit:** 2026-05-25 10:00 BKK  
**Commits on branch:** 18+

### File Changes Summary (top files)
| File | Lines Changed | Notes |
|------|---------------|-------|
| `staff-chat.controller.ts` | +808 total / large diff | 13 new endpoints |
| `ChatPanel.tsx` | 715 lines total | Large component |
| `CannedResponseAdminPage.tsx` | 381 lines total | Within limit |
| `quick-reply-postback-router.service.spec.ts` | +165 | New service spec |
| `reorder-logic.ts` | +126 | Pure logic |
| 2× spec docs in `docs/superpowers/` | +1542 / +983 | Spec/design docs |
| Prisma schema | +77 lines | 2 new models, 2 new enums |
| 3× migration files | +76 lines total | 3 new migrations |

**Total:** 59 files, 8203 insertions, 661 deletions.

### Issues Found

#### Critical
_None._

#### Warning
- **`ChatPanel.tsx` is 715 lines** — exceeds the Info threshold of 500 lines. Component handles chat UI, media preview, canned-response picker, and send logic. Consider extracting the canned-response picker section into a dedicated component in a follow-up.  
  _File:_ `apps/web/src/pages/UnifiedInboxPage/components/ChatPanel.tsx`

- **`staff-chat.controller.ts` is 808 lines** — large but structured. All 13 new endpoints verified to have `@Roles` decorators; class-level `@UseGuards(JwtAuthGuard, RolesGuard)` present. Size is a maintainability concern, not a correctness issue.

- **TypeScript `any` in several frontend files** — `(r: any) => r.data`, `(e: any) => toast.error(...)`, `onSuccess: (created: any)`. Not new to this codebase but the volume is higher in this branch (18 occurrences in new TSX code). Typed API response wrappers would eliminate these.

- **Prisma `onDelete: Cascade`** on `CannedResponseBubble → CannedResponse` and `CannedResponseQuickReply → CannedResponse`. Per `database.md`, `Cascade` is appropriate "when child has no meaning without parent" — bubbles and quick replies are meaningless without their template, so this is correct. Flagging for human confirmation only.

#### Info
- `CannedResponseBubble` model uses `Float?` for `latitude`/`longitude` — acceptable for geographic coordinates (not money fields).
- Spec docs (`2026-05-25-canned-response-admin-redesign.md` at 1542 lines, design doc at 229 lines) added to `docs/superpowers/` — large but not shipped to production.
- `seed.ts` modified (+54 lines) — seeding new canned-response test data. Safe for dev; production seed should be reviewed to ensure it's idempotent.

### Notable Positives
- All 13 new controller endpoints have `@Roles` decorators; class-level guards intact.
- `deletedAt: null` present in all new Prisma queries across service files.
- No money fields or `Number()` usage.
- No `$queryRaw` usage.
- DTOs have 74 `@IsString`/`@IsOptional`/`@IsNotEmpty`/`@IsNumber` decorators with Thai validation messages.
- New models correctly follow database rules: UUID IDs, `createdAt`/`updatedAt`/`deletedAt`, `@@index` on FK + sortOrder.
- `useReorderMutation.ts` extracted as a separate hook — clean separation.
- `invalidateQueries` present after all data-modifying mutations in frontend code.

### Recommendation: **REVIEW** ⚠️

Action required before merge:
1. (Warning) Plan a follow-up ticket to split `ChatPanel.tsx` (715 lines) — not a blocker.
2. (Warning) Review `seed.ts` changes to confirm idempotency for production seeding.
3. (Info) Consider typing `queryFn` return values with explicit types to eliminate `any` casts.

---

## Summary Table

| Branch | Files | Insertions | Critical | Warning | Info | Verdict |
|--------|-------|-----------|----------|---------|------|---------|
| `feat/canned-response-channel-tabs` | 5 | 277 | 0 | 0 | 1 | **APPROVE** ✅ |
| `feat/canned-response-postback-routing` | 15 | 701 | 0 | 2 | 2 | **REVIEW** ⚠️ |
| `feat/canned-response-admin-redesign` | 59 | 8203 | 0 | 3 | 3 | **REVIEW** ⚠️ |

**No branch is BLOCKED.** All three branches are free of critical security vulnerabilities (no unguarded controllers, no `Number()` on money fields, no missing `deletedAt` filters, no raw SQL injection risk, no hardcoded secrets).
