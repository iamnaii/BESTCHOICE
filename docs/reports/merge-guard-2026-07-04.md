# Pre-Merge Guard Report — 2026-07-04

Generated by the Pre-Merge Guard agent. Three most-recently-updated feature/fix branches reviewed.

---

## Branch 1: `fix/reschedule-qa-test-slip-contract`

**Author**: akenarin.ak@gmail.com  
**Last commit**: 2026-07-04 19:11 +0700  
**Commits**: 3 — `feat(payments): แท็บ "ชำระครบ"`, `fix(receipts): ยกเลิกใบเสร็จ SoD`, `test(web): reschedule QA test`  
**Diff**: 19 files, 900 insertions, 56 deletions

### File Changes Summary
- `payments.pending-live-fee.spec.ts` — 44 new test cases (PAID installment late fee logic)
- `payment-query.service.ts` — PAID status gate (skip live fee recompute)
- `receipts.service.spec.ts` / `receipt-void.service.ts` — SoD approver field on void
- `users.controller.ts` / `users.service.ts` / `users.service.spec.ts` — new `GET /users/approvers`
- `ReceiptVoidDialog.tsx` — 4-eyes approver picker + useMutation + invalidateQueries
- `PaymentsPage/index.tsx` — new "ชำระครบ" tab with PAID payments table
- `PaymentTable.tsx` / `RecordPaymentWizard.tsx` — paid-mode columns

### Critical Issues

**None found.**

Guards check:
- `GET /users/approvers` — `users.controller.ts` has class-level `@UseGuards(JwtAuthGuard, RolesGuard)` ✓
- New endpoint has `@Roles('OWNER', 'BRANCH_MANAGER', 'FINANCE_MANAGER', 'ACCOUNTANT', 'SALES')` ✓

Money fields:
- No `Number()` on `Decimal` financial fields in new service code ✓
- `receipt-void.service.ts` new approver lookup does not touch money fields ✓

Soft delete:
- New `findApprovers()` query includes `deletedAt: null` ✓

### Warning Issues

**W1 — `parseFloat` in CSV export (display-only)**  
File: `apps/web/src/pages/PaymentsPage/index.tsx` (new PAID export block)
```ts
amountDue: parseFloat(p.amountDue).toLocaleString(),   // for CSV display
lateFee:   parseFloat(p.lateFee).toLocaleString(),
amountPaid: parseFloat(p.amountPaid).toLocaleString(),
```
Context: These three calls are building a CSV export row — the result is immediately converted to a locale string for human display. No arithmetic is performed on the float values. Low financial risk, but inconsistent with the codebase convention of using `Prisma.Decimal.toFixed(2)` for display formatting.  
**Recommendation**: Replace `parseFloat(x).toLocaleString()` with `new Prisma.Decimal(x).toFixed(2)` or the existing `formatCurrency` helper if one exists.

### Info

- `ReceiptVoidDialog.tsx` correctly uses `useQuery` + `useMutation` + `invalidateQueries` for approver fetch and void mutation ✓
- New test file `PaymentTable.paid-mode.test.tsx` (120 lines) adds good coverage
- Thai validation messages present on new DTO fields ✓

### Recommendation: **APPROVE** (fix W1 before merge recommended but not blocking)

---

## Branch 2: `fix/late-fee-split-reschedule-collect-first`

**Author**: akenarin.ak@gmail.com  
**Last commit**: 2026-07-02 21:11 +0700  
**Commits**: 1 — `fix(payments): fee-first late-fee split + reschedule collect-first (ปรับดิว)`  
**Diff**: 33 files, 2244 insertions, 273 deletions

### File Changes Summary
- **New**: `reschedule-collect.service.ts` (424 lines) — atomic ปรับดิว + collect execution
- **New**: `reschedule-quote.util.ts` — SSoT for fee/lateFee/collectAmount computation
- **New**: `paysolutions-intent.service.ts` — create RESCHEDULE QR link
- **New**: `paysolutions-confirmation.service.ts` — webhook exec for reschedule QR
- **New**: `reschedule-qr.flex.ts` — LINE flex message for QR send
- **New**: `split-receipt.ts` / `reconstruct-prior.ts` — journal split helpers
- `payments.controller.ts` — 2 new endpoints (`GET reschedule-quote`, `POST :id/reschedule-qr`)
- `payment-receipt.template.ts` — refactored (88 lines removed, delegate to helpers)
- `schema.prisma` — `purpose` + `metadata` fields on `PartialPaymentLink`
- Migration `migration.sql` added

### Critical Issues

**None found.**

Guards:
- `GET /payments/reschedule-quote` has `@Roles(...)` ✓
- `POST /payments/:id/reschedule-qr` has `@Roles(...)` ✓
- `PaymentsController` has class-level `@UseGuards(JwtAuthGuard, RolesGuard, BranchGuard)` ✓

Money fields — core computation:
- `reschedule-quote.util.ts` uses `new Prisma.Decimal(...)`, `.div()`, `.times()`, `.toDecimalPlaces()` ✓
- `reschedule-collect.service.ts` uses `Prisma.Decimal` throughout arithmetic ✓

Soft delete:
- `paysolutions-intent.service.ts` new `payment` lookup includes `payment.deletedAt` guard ✓

SQL injection:
- `$executeRaw` uses tagged template literals (parameterized) — no injection risk ✓

### Warning Issues

**W1 — `Number(link.amount)` converting Decimal for service call**  
File: `apps/api/src/modules/paysolutions/services/paysolutions-confirmation.service.ts`
```ts
await this.paymentsService.rescheduleWithCollect({
  ...
  amount: Number(link.amount),   // link.amount is Prisma.Decimal
});
```
`RescheduleWithCollectInput.amount` is typed as `number`. The service immediately wraps it back to Decimal via `d(input.amount)`. The round-trip `Decimal → Number → Decimal` is safe for 2dp values but breaks the codebase rule of never using `Number()` on financial Decimals. Additionally, the same value is stored in `link.metadata` as a JS number at QR creation time.  
**Recommendation**: Change `RescheduleWithCollectInput.amount` to `string | Prisma.Decimal` and pass `link.amount.toFixed(2)` instead of `Number(link.amount)`.

**W2 — Large new service (424 lines) without integration test**  
File: `apps/api/src/modules/payments/services/reschedule-collect.service.ts`  
The file has a companion `reschedule-collect.service.spec.ts` (304 lines) covering the happy paths, but the end-to-end QR→webhook→reschedule execution path is only tested in `paysolutions.callbacks.spec.ts` (+67 lines). Consider adding a test for the partial-payment guard (`diff.gt(0.01)` threshold) and the advance balance preload path.

**W3 — `@IsNumber()` on `daysToShift` in inline DTO**  
File: `apps/api/src/modules/payments/payments.controller.ts` — `CreateRescheduleQrDto`  
The `daysToShift` field uses `@IsNumber()` without `{ message: '...' }` in Thai. Consistent with existing `CreatePartialQrDto` style (same file), but other DTOs in the project use Thai messages.

### Info

- Migration adds `purpose` and `metadata Json?` to `PartialPaymentLink` — migration is backwards compatible (both optional/default) ✓
- `paysolutions-confirmation.service.ts` returns HTTP 200 on reschedule-exec failure with Sentry alert (correct pattern — avoids PaySolutions infinite retry) ✓
- Reschedule QR expires in 24h via existing `partial-qr-expiry.cron` (no new cron needed) ✓

### Recommendation: **REVIEW** (fix W1 before merge — Decimal precision policy)

---

## Branch 3: `feat/ai-hardening-followups`

**Author**: akenarin.ak@gmail.com  
**Last commit**: 2026-07-02 15:24 +0700  
**Commits**: 5 — AI backfill poison-row skip, sales-bot error tracking, systemic-outage circuit breaker, longest-prefix rate match, symlink cleanup  
**Diff**: 30 files, 672 insertions, 111 deletions

### File Changes Summary
- `embedding-backfill.cron.ts` — per-row fallback (`EMBED_FAILED` poison marker), systemic outage breaker (3 consecutive errors → stop run)
- `ai-auto-reply.service.ts` — consolidation for errorKind + rolling-window label fix
- `ai-assistant.service.ts` — circuit breaker for provider outage
- `sales-bot.service.ts` — record error usage rows on provider throw (accumulates tokens)
- `ai-pricing.ts` — longest-prefix match, `gemini-2.5-flash-lite` rate
- `anthropic-ocr.client.ts` — per-model token caps, graceful timeout
- `credit-check.ai-analysis.service.ts` — consistent errorKind propagation
- `knowledge-extractor.service.ts` — language guard + fallback
- `AiSettingsPage.tsx` — rolling-window wording fix

### Critical Issues

**None found.**

Guards: No new controllers or endpoints added — all AI modules route through existing guarded controllers ✓  
Money fields: No financial Decimal fields modified ✓  
Soft delete: No new Prisma queries missing `deletedAt: null` ✓  
SQL injection: `$executeRaw` tagged template literals only ✓

### Warning Issues

**None significant.** Minor observations:

**W1 — `.gitignore` entry for symlinked node_modules (Info level)**  
File: `.gitignore` — adds entry to exclude accidentally committed `node_modules` symlinks. No code risk, just hygiene.

### Info

- `EMBED_FAILED_MARKER` constant prevents poison rows from being retried endlessly ✓
- Per-row fallback in `embedBatchWithFallback` continues the batch even if one row fails ✓
- `$executeRaw` tagged templates are parameterized — no SQL injection risk ✓
- 5 commits include `sales-bot.service.spec.ts` (+65 lines) and `embedding-backfill.cron.spec.ts` (+123 lines) — good coverage increase ✓

### Recommendation: **APPROVE**

---

## Summary Table

| Branch | Files | Insertions | Critical | Warning | Recommendation |
|--------|-------|-----------|----------|---------|----------------|
| `fix/reschedule-qa-test-slip-contract` | 19 | +900 | 0 | 1 | **APPROVE** (minor fix W1) |
| `fix/late-fee-split-reschedule-collect-first` | 33 | +2244 | 0 | 3 | **REVIEW** (fix W1 Decimal) |
| `feat/ai-hardening-followups` | 30 | +672 | 0 | 0 | **APPROVE** |

**No BLOCK-level issues found in any branch.**  
The most important pre-merge fix is `fix/late-fee-split-reschedule-collect-first` W1: change `Number(link.amount)` → `link.amount.toFixed(2)` and update `RescheduleWithCollectInput.amount` type to `string`.
