# Pre-Merge Guard Report — 2026-05-13

Generated by: Pre-Merge Guard Agent  
Date: 2026-05-13  
Branches reviewed: 3

---

## Branch 1: `fix/other-income-v2-1-review-followup`

**Author:** Akenarin Kongdach  
**Latest commit:** 2026-05-12 15:27  
**Unique commits vs main:** 3

### File changes summary
- `apps/api/src/modules/other-income/__tests__/maker-checker.spec.ts` — afterEach cleanup + concurrent CAS regression test
- `apps/api/src/modules/other-income/other-income.controller.ts` — role tightening (remove SALES from template endpoints)
- `apps/api/src/modules/other-income/other-income.service.ts` — Thai error messages + CAS TOCTOU fix
- `apps/web/src/pages/other-income/OtherIncomeTemplatesPage.tsx` — rename `useMutation_` → `applyTemplateMutation`
- `apps/web/src/pages/other-income/components/TemplatePickerCombobox.tsx` — same rename

**Note:** The 3 unique commits are CI fixup commits that follow PR #825 (already merged to main). The branch has not been rebased after #826 (`chore(payments): remove per-row "ล่วงหน้า" button`), so a direct diff vs. main also shows PaymentsPage diffs — those reflect content in main that isn't in this branch yet.

### Issues

| Severity | File | Line | Issue |
|----------|------|------|-------|
| Info | `other-income.controller.ts` | `@Roles` on template endpoints | SALES removed from template management — correct tightening |
| Info | `maker-checker.spec.ts` | afterEach | Safety-net afterEach added to prevent test state leak — good defensive pattern |

**No Critical or Warning issues found.**

### Recommendation: **APPROVE**

The 3 unique commits are minor CI fixups (Thai message assertions, afterEach restore, CI-failing approve() message). The role tightening (removing SALES from template write endpoints) is a correct security improvement. Needs rebase onto main before merge to pick up the #826 PaymentsPage removal.

---

## Branch 2: `feat/other-income-v2-1-combined`

**Author:** Akenarin Kongdach  
**Latest commit:** 2026-05-12 14:14  
**Unique commits vs main:** 38 (large feature — Maker-Checker + Templates + bug fixes)

### File changes summary (key files)
- **New schema:** `OtherIncomeTemplate`, `OtherIncomeMakerChecker` Prisma models (2 migrations)
- **New service:** `apps/api/src/modules/other-income/services/template.service.ts` (143 lines)
- **New service:** `apps/api/src/modules/other-income/services/template-vars.util.ts`
- **Extended service:** `other-income.service.ts` +213 lines (requestApproval, approve, reject)
- **Extended controller:** `other-income.controller.ts` +112 lines (7 new endpoints)
- **New pages:** `OtherIncomeTemplatesPage`, `OtherIncomePendingApprovalPage`
- **Extended pages:** `OtherIncomeViewPage`, `OtherIncomeEntryPage`, `OtherIncomeListPage`
- **New components:** `RejectModal`, `RenameTemplateModal`, `SaveAsTemplateModal`, `TemplatePickerCombobox`
- **New web API client methods** in `otherIncome.ts`
- **Tests:** 4 new spec files (maker-checker, template.service, template-vars, validation)

**Note:** The majority of this feature was squash-merged into main as PR #823. The unique commits are the granular branch history. The CONTENT is already in main via the squash. This review covers the code quality of what landed.

### Issues

| Severity | File | Location | Issue |
|----------|------|----------|-------|
| Warning | `services/template.service.ts` | `createFromDoc()` L68-72 | `Number()` on Decimal money fields (`unitAmount`, `discountAmount`) when serialising to `itemsJson` JSONB. Loses precision for values > 15 significant digits. Should use `.toString()` and store as string, or accept the precision loss as a documented trade-off for JSON storage. |
| Warning | `services/template.service.ts` | `list()` | `const where: any` — drops type safety on the Prisma query filter. Use `Prisma.otherIncomeTemplateWhereInput` instead. |
| Info | `other-income.controller.ts` | `@Post('templates')` | Template write endpoints include `SALES` role. The follow-up branch (`fix/other-income-v2-1-review-followup`) removes SALES — ensure that fix is merged before or alongside. |
| Info | `other-income.service.ts` | `approve()` | CAS-claim `updateMany` guards against concurrent approval (TOCTOU) — good pattern. Verified via new concurrent test. |

### Critical issues: None

Guards verified:
- Controller class has `@UseGuards(JwtAuthGuard, RolesGuard)` ✅
- All 7 new endpoints have `@Roles()` decorators ✅
- `GET /other-income/maker-checker-enabled` has broad role list (all 5 roles) — intentional, it's a flag read ✅
- No raw `$queryRaw` ✅
- No hardcoded secrets ✅
- All new `findMany`/`findFirst` include `deletedAt: null` ✅

### Recommendation: **REVIEW**

Feature quality is good. One Warning (Decimal→Number conversion) should be addressed before a future respin. The `where: any` is a minor type-safety gap. Both are low-risk in practice (JSONB template data, dashboard display) but deviate from project conventions. Since content is already in main via #823, these are tracked items for a follow-up patch.

---

## Branch 3: `chore/audit-quick-wins`

**Author:** Akenarin Kongdach  
**Latest commit:** 2026-04-26 20:17 (stale — 17 days behind HEAD of main)  
**Unique commits vs main:** 5 (but branch tip is 2229 commits behind main's history — very diverged)

### File changes summary (5 unique commits)
1. `b00d5ac5` — Security: throttle public shop endpoints + file upload validators + ShopBotDefenseGuard
2. `b42b7bb9` — Perf: dashboard staff-metrics `groupBy` + 3 compound indexes
3. `9922aca3` — Feat: collections guided session workflow + UI redesign
4. `84f6e7e7` — Feat: remove hover-snapshot popup from collections queue
5. `e1cb4266` — Fix: Yeastar `@Inject(EventsGateway)` + `@Optional()` for DI token

**These 5 commits are NOT in main** (confirmed via `git log origin/main --grep`).

### Issues

| Severity | File | Location | Issue |
|----------|------|----------|-------|
| Warning | `apps/api/src/modules/collections/collections.service.ts` | `Number(p._sum.amountPaid ?? 0)` | Converts Decimal aggregate `_sum.amountPaid` to JS Number for a collections KPI dashboard map. Money aggregate stored as float in memory. Use `new Prisma.Decimal(p._sum.amountPaid ?? 0)` with `.toNumber()` only at the final response serialisation point, consistent with v4 hardening. |
| Warning | `apps/api/src/modules/accounting/dashboard.service.ts` (or similar) | `.toNumber()` on `_sum.sellingPrice` inside accumulation loop | Same pattern — intermediate accumulation uses `.toNumber()` then re-wraps with `Prisma.Decimal.add()`. While functionally correct for typical THB amounts, it deviates from the 0-`Number(_sum` rule established in v4. |
| Info | `b00d5ac5` — `shop-auth-social.controller.ts` | `@UseGuards(ShopBotDefenseGuard)` | ShopBotDefenseGuard added on public social-auth endpoints. Good defensive addition. Confirm `ShopBotDefenseGuard` is registered as a global provider and handles the `ENABLE_SHOP_BOT_DEFENSE` env flag gracefully when unset. |
| Info | `b42b7bb9` | Schema | 3 new compound indexes added — positive. Verify migration was generated for production deploy path. |
| Info | Branch staleness | All files | Branch is 17 days old and diverged ~2229 commits behind main tip. Many files in the direct diff conflict with main's Phase A.4 accounting rewrite, asset module removal, and expense module additions. **A full rebase will be needed before merge.** |

### Critical issues: None

Guards in security commit:
- `ShopBotDefenseGuard` added on 4 public controllers — intentionally public, not requiring JWT ✅
- `JwtAuthGuard` not added to public shop endpoints (correct — these are customer-facing) ✅
- `@Throttle()` uses `'short'` bucket matching existing ThrottlerModule.forRoot config ✅
- File upload validators (`MaxFileSizeValidator`, `FileTypeValidator`) added correctly ✅

### Recommendation: **REVIEW**

Content quality is good (security improvements, performance gains). The 2 Warning-level `Number()` on money aggregates should be fixed. The primary blocker is **branch staleness** — a rebase onto main is required before this can be merged, and conflicts with the Phase A.4 accounting module restructuring will need manual resolution.

---

## Overall Summary

| Branch | Unique Commits | Critical | Warning | Info | Recommendation |
|--------|---------------|----------|---------|------|----------------|
| `fix/other-income-v2-1-review-followup` | 3 | 0 | 0 | 2 | ✅ APPROVE (needs rebase) |
| `feat/other-income-v2-1-combined` | 38 | 0 | 2 | 3 | 🔶 REVIEW (content in main via #823) |
| `chore/audit-quick-wins` | 5 | 0 | 2 | 3 | 🔶 REVIEW (needs rebase — 17d stale) |

### Action items

1. **`fix/other-income-v2-1-review-followup`**: Rebase on main, then merge. Small clean-up commits.
2. **`feat/other-income-v2-1-combined`**: Already in main (#823). Track the two Warnings as follow-up patch work:
   - `template.service.ts`: Replace `Number(it.unitAmount)` etc. with `.toString()` in `itemsJson`
   - `template.service.ts`: Replace `where: any` with `Prisma.otherIncomeTemplateWhereInput`
3. **`chore/audit-quick-wins`**: Rebase urgently (17 days stale, 2229-commit gap). Fix the 2 `Number()` on aggregates, then re-submit. Security additions and performance changes are valuable.
