node_modules/
node_modules
dist/
.turbo/
.env
.env.local
*.log
.DS_Store
coverage/
.prisma/
prisma/*.db
uploads/

# Playwright
playwright-report/
test-results/
blob-report/
**/e2e/screenshots/
*.temp.config.ts
.playwright-mcp/

# Temp files
.tmp/
*.tmp

# Playwright auth token (generated by global-setup, contains JWT)
.playwright-auth.json
apps/web/.playwright-auth.json
apps/web/.playwright-roles-auth.json
backups/
.superpowers/

# Anthropic skills - binary assets (fonts, PDFs, images)
.claude/skills/**/*.ttf
.claude/skills/**/*.otf
.claude/skills/**/*.woff
.claude/skills/**/*.woff2
.claude/skills/**/*.pdf
.claude/skills/**/*.png
.claude/skills/**/*.jpg

# Claude Code runtime
.claude/scheduled_tasks.lock
.claude/worktrees/
.worktrees/

# UI audit tool outputs (generated artifacts)
scripts/ui-audit/
__pycache__/

# ผลลัพธ์ที่ดัมพ์จาก production (preflight / enumerate SQL)
# มี PII ของพนักงาน (ชื่อ + อีเมลส่วนตัว) และยอดเงินจริง — เก็บไว้ในเครื่องเท่านั้น
# ห้าม commit ตาม .claude/rules/security.md ("ห้าม log ... PII")
shop-preflight-*.txt
shop-opening-balance-*.txt
interco-preflight-*.txt
*-preflight-[0-9]*.txt

# ไฟล์ล็อกชั่วคราวของ Microsoft Office (สร้างขณะเปิดเอกสารค้างไว้)
# เช่น docs/guides/~$aff-test-script-muay-pop-2026-08.docx
~$*

# TypeScript incremental build artifacts (หลุดเข้า repo ตอน merge #1503)
*.tsbuildinfo
