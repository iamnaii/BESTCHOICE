# Pre-Merge Guard Report — 2026-04-14

Generated by: Pre-Merge Guard Agent  
Date: 2026-04-14  
Branches reviewed: 3 of 30 unmerged branches (selected by code-change volume)

---

## Branches Scanned

| Branch | Files Changed | Insertions | Deletions | Author | Age |
|--------|-------------|-----------|-----------|--------|-----|
| `feat/chatbot-production-ready` | 16 | +734 | -313 | iamnaii | 2 days |
| `chore/deps-tier3-chunk6-tailwind4` | 126 | ~1,200+ | ~600+ | iamnaii | 2 days |
| `chore/deps-tier3-chunk9-react-router7` | 56 | ~60 | ~54 | iamnaii | 2 days |

Skipped (no code changes vs main): `feat/accounting-audit-fixes`, `feature/chatbot-finance`, `E2E-TEST`, `chore/quickbuy-step1-reorder`, `chore/trade-in-orchestrator-rebrand`, all `claude/*` branches, `guard/*` and `reports/*` housekeeping branches, and 7 other dep-upgrade branches with <8 files changed.

---

## Branch 1: `feat/chatbot-production-ready`

**Author:** iamnaii  
**Commit:** `72a6fcce` — "feat(chatbot): production-ready — feedback Quick Reply, admin prompt editor, KB seed, remove CHATCONE"

### Summary of Changes
- Added LINE postback handler for 👍/👎 feedback Quick Reply after AI tool-backed responses
- Added admin system prompt editor (DB-backed via `SystemConfig`, 5-min in-memory cache in `FinanceAiService`)
- Added `/chatbot/finance/admin/knowledge/seed` endpoint to seed default KB entries via API
- Added `UpdatePromptDto` with `@MinLength(100)` / `@MaxLength(10000)` validation
- Added `seedKnowledgeBase()` to `KnowledgeService` (mirrors existing Prisma seed)
- Removed `ChatconeModule` entirely (controller, service, module — was scaffold-only)
- Frontend: extended `ChatbotFinanceKnowledgePage` with system prompt editor tab + seed button

---

### Issues

#### 🔴 CRITICAL

**[C-1] Hard delete on `SystemConfig` row**  
File: `apps/api/src/modules/chatbot-finance/services/finance-config.service.ts`  
```ts
// resetSystemPrompt()
await this.prisma.systemConfig.deleteMany({
  where: { key: SYSTEM_CONFIG_KEYS.systemPrompt },
});
```
`SystemConfig` model has `deletedAt DateTime?`. Using `deleteMany` bypasses soft-delete and permanently destroys the record. Per `database.md`: **"ห้าม hard delete เด็ดขาด"**.  
**Fix:** Replace with:
```ts
await this.prisma.systemConfig.updateMany({
  where: { key: SYSTEM_CONFIG_KEYS.systemPrompt, deletedAt: null },
  data: { deletedAt: new Date() },
});
```

---

#### 🟡 WARNING

**[W-1] `findUnique` does not filter `deletedAt: null`**  
File: `apps/api/src/modules/chatbot-finance/services/finance-config.service.ts`  
```ts
// getSystemPrompt()
const config = await this.prisma.systemConfig.findUnique({
  where: { key: SYSTEM_CONFIG_KEYS.systemPrompt },
});
```
`findUnique` on a `@unique` field cannot include `deletedAt: null` in the same `where` clause — it requires using `findFirst` instead. If the prompt is soft-deleted (after calling `resetSystemPrompt` once fixed), this query would still return the deleted record.  
**Fix:** Change to `findFirst`:
```ts
const config = await this.prisma.systemConfig.findFirst({
  where: { key: SYSTEM_CONFIG_KEYS.systemPrompt, deletedAt: null },
});
```

**[W-2] Duplicate seed logic**  
Files: `apps/api/prisma/seeds/knowledge-base.ts` and `apps/api/src/modules/chatbot-finance/services/knowledge.service.ts`  
Both implement identical KB seeding loops (~40 lines duplicated). The service method `seedDefaults()` was added to allow runtime seeding via the admin API — a valid use case — but the logic is copy-pasted from the Prisma seed file rather than reusing a shared constant and shared function.  
**Impact:** Low — logic divergence risk over time. Not a blocker.  
**Suggestion:** The seed file could call `knowledge.seedDefaults()` after DI setup, or extract the loop into a shared util. Not required for merge.

---

#### ℹ️ INFO

**[I-1] Placeholder `__MSG_ID__` replaced post-save**  
File: `apps/api/src/modules/chatbot-finance/services/chatbot-finance.service.ts`  
The feedback Quick Reply data contains `messageId=__MSG_ID__` which is replaced after `saveMessage()` returns. This is intentional (message must be saved to get its UUID before sending the LINE reply). The pattern is clear and correct, but worth noting for future maintainers.

**[I-2] `UpdatePromptDto` not exported from `dto/admin.dto.ts` barrel**  
File: `apps/api/src/modules/chatbot-finance/dto/admin.dto.ts`  
`UpdatePromptDto` is defined but its usage is already correctly imported in the controller. No barrel issue since direct imports are used. No action required.

**[I-3] CHATCONE module removal**  
Entire `apps/api/src/modules/chatcone/` directory deleted and removed from `app.module.ts`. This was a scaffold-only module with placeholder TODO comments. Confirmed no other module imports `ChatconeService`. Deletion is safe.

---

### Recommendation: **REVIEW** (fix C-1 before merge)

The hard delete on `SystemConfig` is the only blocker. Fix is a one-liner. All security guards, roles, and DTO validation are properly in place on the new endpoints. Frontend uses `api.get/put/post` correctly and `queryClient.invalidateQueries` is called after all mutations.

---

## Branch 2: `chore/deps-tier3-chunk6-tailwind4`

**Author:** iamnaii  
**Commit:** `0140289d` — "chore(web): migrate Tailwind CSS v3.4 → v4.2 — Tier 3 chunk 6"

### Summary of Changes
- `tailwindcss`: `^3.4.0` → `^4.2.2`
- Added `@tailwindcss/vite: ^4.2.2` (new Vite plugin replaces PostCSS integration)
- Removed `autoprefixer`, `postcss` devDependencies
- Deleted `postcss.config.js` (no longer needed — Vite plugin handles processing)
- Deleted `tailwind.config.js` (177 lines — theme migrated to `@theme {}` block in `index.css`)
- Rewrote `index.css`: added `@import 'tailwindcss'`, `@plugin 'tailwindcss-animate'`, `@custom-variant dark`, and full `@theme {}` block with all color tokens, font scales, and animations
- `vite.config.ts`: added `import tailwindcss from '@tailwindcss/vite'` + `tailwindcss()` plugin
- 126 component files updated for v4 class syntax changes (e.g. `bg-gradient-to-br` → `bg-linear-to-br`, `origin-[...]` → `origin-(...)`, CSS var shorthand `max-h-(--radix-...)`)

---

### Issues

#### 🔴 CRITICAL

None.

---

#### 🟡 WARNING

**[W-1] Old `tailwind.config.js` theme completeness**  
The old `tailwind.config.js` contained 177 lines of custom theme config (Metronic font scale, custom colors, animations, keyframes). The new `@theme {}` block in `index.css` contains ~250 lines including color tokens and animations. A line-by-line comparison was not performed here — manual verification that **all** custom keyframes (fadeIn, slideUp, float, accordion-down/up, collapsible-down/up, pulse-subtle) and **all** color tokens were migrated is recommended before merge.

**[W-2] No TypeScript/build verification in branch**  
Tailwind v4 is a major version with breaking utility name changes. The branch changes 126 files. TypeScript checks won't catch CSS class regressions. A visual smoke test of key pages (POS, Customers, Contracts, Dashboard) in a preview environment is strongly recommended before merging to confirm no Tailwind utility regressions.

---

#### ℹ️ INFO

**[I-1] Correct v4 migration pattern**  
The migration follows official Tailwind v4 migration guidance: Vite plugin replaces PostCSS, `@theme` replaces `tailwind.config.js`, `@import 'tailwindcss'` replaces `@tailwind` directives. No backwards-compat shims.

**[I-2] `ring-0` and `shadow-sm` usage**  
Some new classes added reference `ring-0` and `shadow-sm` — both valid in Tailwind v4 with no breaking change.

**[I-3] `autoprefixer` removed**  
`@tailwindcss/vite` v4 handles autoprefixing internally. Removing `autoprefixer` as explicit dep is correct.

---

### Recommendation: **REVIEW**

No security or data integrity issues. The migration pattern is architecturally correct. The risk is visual regression across 126 components. Merging without browser smoke-test is inadvisable. Suggest deploying to staging and doing a page-by-page visual check before merging to main.

---

## Branch 3: `chore/deps-tier3-chunk9-react-router7`

**Author:** iamnaii  
**Commit:** `94f514d3` — "chore(web): migrate react-router-dom v6.28 → react-router v7.14 — Tier 3 chunk 9"

### Summary of Changes
- `react-router-dom: ^6.28.0` removed → `react-router: ^7.14.0` added in `apps/web/package.json`
- All 54 `from 'react-router-dom'` imports replaced with `from 'react-router'` across 56 files
- No API usage changes — only import source changed
- `BrowserRouter`, `Routes`, `Route`, `Navigate`, `Link`, `useNavigate`, `useParams`, `useSearchParams`, `useLocation`, `Outlet` all continue to work identically in `react-router` v7

---

### Issues

#### 🔴 CRITICAL

None.

#### 🟡 WARNING

None.

#### ℹ️ INFO

**[I-1] Migration is import-only**  
React Router v7's `react-router` package is the unified package that replaces `react-router-dom` for browser apps. All v6 APIs used here (`BrowserRouter`-based routing, not the new `createBrowserRouter` data API) remain fully supported. Zero behavior change.

**[I-2] 0 remaining `react-router-dom` imports**  
Verified: 54 old imports removed, 0 new `react-router-dom` imports added. Migration is complete.

---

### Recommendation: **APPROVE**

Clean, mechanical migration with no logic changes. Low risk. Safe to merge.

---

## Summary Table

| Branch | Critical | Warning | Info | Recommendation |
|--------|----------|---------|------|----------------|
| `feat/chatbot-production-ready` | 1 | 2 | 3 | **REVIEW** — fix C-1 (hard delete) before merge |
| `chore/deps-tier3-chunk6-tailwind4` | 0 | 2 | 3 | **REVIEW** — browser smoke-test required |
| `chore/deps-tier3-chunk9-react-router7` | 0 | 0 | 2 | **APPROVE** |

---

## Action Items

1. **`feat/chatbot-production-ready`** — Fix `finance-config.service.ts:resetSystemPrompt()`: replace `deleteMany` with `updateMany({ data: { deletedAt: new Date() } })` + apply W-1 `findFirst` fix. Then re-review.

2. **`chore/deps-tier3-chunk6-tailwind4`** — Run `./tools/check-types.sh web` + deploy to staging + visual smoke-test of Dashboard, POS, Contracts, Customers, Settings pages before merge.

3. **`chore/deps-tier3-chunk9-react-router7`** — Ready to merge after `./tools/check-types.sh web` passes.
