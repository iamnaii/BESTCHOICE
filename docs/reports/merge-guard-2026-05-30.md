# Pre-Merge Guard Report — 2026-05-30

Generated by: Pre-Merge Guard Agent  
Date: 2026-05-30  
Branches reviewed: 3 (top recent unmerged, sorted by last commit date)

---

## Branch 1: `fix/fb-webhook-integration-config`

**Last commit:** 2026-05-28  
**Commits ahead of main:** 1 (`06efe119`)  
**Files changed:** 3 (+96 / −11)

### File Changes Summary
| File | +Lines | −Lines | Notes |
|------|--------|--------|-------|
| `apps/api/src/modules/chat-adapters/chat-adapters.module.ts` | +3 | 0 | Adds `IntegrationsModule` import |
| `apps/api/src/modules/chat-adapters/facebook-webhook.controller.spec.ts` | +68 | 0 | Adds `IntegrationConfigService` mock + 3 new test cases |
| `apps/api/src/modules/chat-adapters/facebook-webhook.controller.ts` | +25 | −11 | Migrates verify token + app secret from env vars to DB-backed `IntegrationConfig` |

### Issues

**Critical (must fix):** None

**Warning (should fix):**
- `verifySignature` now makes a DB round-trip on every POST `/webhook/facebook`. Performance impact is negligible in practice (fast indexed lookup), but worth noting. No code change required — acceptable trade-off for secret rotation via UI.

**Info:**
- None

### Security Review
- Controller remains intentionally public (documented in `security.md` under "Intentionally Public Endpoints") ✅
- Verify token check now fails-closed when token is empty: `verifyToken && token === verifyToken` ✅
- `getAppSecret()` returns `undefined` when unset — all callers fail-closed ✅
- All three secret-resolution paths updated: `verifyWebhook`, `verifySignature`, `handleDataDeletion` ✅
- New tests cover: token match → 200, token mismatch → 400, empty token → 400 ✅

### Recommendation: **APPROVE**

---

## Branch 2: `fix/letters-e2e-sales-assertion`

**Last commit:** 2026-05-26  
**Commits ahead of main:** 1 (`824d7e1b`)  
**Files changed:** 1 (+8 / −4)

### File Changes Summary
| File | +Lines | −Lines | Notes |
|------|--------|--------|-------|
| `apps/web/e2e/letters-page.spec.ts` | +8 | −4 | Rewrites brittle SALES-role assertion |

### Issues

**Critical (must fix):** None

**Warning (should fix):** None

**Info:**
- The old test checked absence of `ยกเลิก` button for SALES, but the CANCELLED status tab also renders a button with that text — causing a false failure. The fix replaces it with a page-loads-successfully check (`heading` visible + URL contains `/letters`). The original intent (SALES cannot cancel) is verified at the API layer via `@Roles` + unit tests per the commit message — this is the correct split of concerns.

### Recommendation: **APPROVE**

---

## Branch 3: `feat/canned-response-channel-tabs`

**Last commit:** 2026-05-25  
**Commits ahead of main:** 2 (`bc4603d3`, `d7b6c4bf`)  
**Files changed:** 5 (+277 / −20)

### File Changes Summary
| File | +Lines | −Lines | Notes |
|------|--------|--------|-------|
| `apps/web/src/pages/canned-response-admin/BubbleList.tsx` | +52 | −18 | Channel filter + badge count reporting + reorder delegation |
| `apps/web/src/pages/canned-response-admin/ChannelTabs.tsx` | +63 | 0 | New component — per-channel tab switcher with badge counts |
| `apps/web/src/pages/canned-response-admin/TemplateEditorPane.tsx` | +17 | −2 | Integrates `ChannelTabs`, resets tab on template switch |
| `apps/web/src/pages/canned-response-admin/bubble-reorder-logic.ts` | +31 | 0 | Extracted pure reorder helper |
| `apps/web/src/pages/canned-response-admin/bubble-reorder-logic.test.ts` | +100 | 0 | 7 unit tests covering edge cases |

### Issues

**Critical (must fix):** None

**Warning (should fix):**
- `useEffect(() => { ... }, [allBubbles, onCountsChange])` in `BubbleList.tsx` depends on `onCountsChange`. If a parent ever passes an inline function (arrow function in JSX), this would cause re-renders on every render cycle. Current usage (`onCountsChange={setBubbleCounts}`) is safe because `setBubbleCounts` from `useState` is stable. If `BubbleList` is reused elsewhere with an inline callback, this becomes a bug. Consider wrapping the effect body in a null-check (already done) and adding a note, or wrapping callers in `useCallback`.

**Info:**
- `ChannelTabs.tsx` (63 lines) is below the 500-line split threshold ✅
- `bubble-reorder-logic.ts` (31 lines) is a clean pure function — correctly extracted for testability ✅
- 7 test cases cover: basic reorder, filtered-reorder preserving hidden bubbles, FB bubbles interleaved with LINE, identity, missing-id guard, universal bubbles ✅
- All API calls use `api.get()`/`api.post()` from `@/lib/api` ✅
- `queryClient.invalidateQueries()` called after mutations ✅
- Design tokens used throughout (`bg-primary`, `text-muted-foreground`, `bg-muted`, `border-border`, `bg-background`) — no hardcoded hex/gray ✅
- `leading-snug` used on Thai text ✅
- `toast.error()` from `sonner` ✅
- `useQuery`/`useMutation` from `@tanstack/react-query` ✅

### Recommendation: **APPROVE** *(with advisory on `onCountsChange` stability)*

---

## Summary

| Branch | Commits | Critical | Warning | Info | Recommendation |
|--------|---------|----------|---------|------|----------------|
| `fix/fb-webhook-integration-config` | 1 | 0 | 1 (perf note) | 0 | **APPROVE** |
| `fix/letters-e2e-sales-assertion` | 1 | 0 | 0 | 1 | **APPROVE** |
| `feat/canned-response-channel-tabs` | 2 | 0 | 1 (useEffect dep) | 4 | **APPROVE** |

No branches are blocked. All three are safe to merge.
