# Pre-Merge Guard Report — 2026-05-14

Generated by: Pre-Merge Guard agent  
Date: 2026-05-14  
Branches reviewed: 3 (most recently active, excluding watchdog/guard/chore/claude/docs/reports branches)

---

## Branch 1: `feat/other-income-v2-2-pr1-override-jv-pagination`

**Author**: Akenarin Kongdach  
**Last commit**: 2026-05-13 17:47 +07:00  
**Scope**: 56 files changed, +5924 / -478 lines

### Summary of changes
Other-income v2.2 mega-PR covering:
- Override JV flow (`JournalOverrideService`, `EditableJournalTable`, `OverrideConfirmDialog`)
- Pagination + sort on `OtherIncomeListPage` (`PaginationBar`, `usePaginationParams`)
- Maker-Checker toggle (`MakerCheckerToggle`, `ToggleMakerCheckerDto`)
- Reopen Period redesign (CAS guard, `ReopenPeriodModal`, `ReopenedPeriodBanner`)
- Settings 5-tab consolidation (`SettingsPage/index.tsx` → tabs: Company, VAT, Periods, Attachment, Users)
- `PERIOD_REOPENED` + `JV_OVERRIDDEN` + `CONFIG_CHANGED` audit log actions
- 3 new migration files, Sentry capture on audit write failures

### Issues

#### Warning

**W1 — `boardResolutionId` 90-day rule not enforced at service layer**  
File: `apps/api/src/modules/accounting/dto/reopen-period.dto.ts` (comment: "Required only when reopening a CLOSED period older than 90 days")  
File: `apps/api/src/modules/accounting/monthly-close.service.ts` (`reopenPeriod` method)

The DTO comment documents a requirement: `boardResolutionId` is mandatory for periods older than 90 days. But the service never computes age from `closedAt` and never throws when the field is absent on an old period. Any OWNER can reopen a period closed >90 days ago (e.g., a filed ภ.พ.30 period) without providing a board resolution reference. The audit log records `boardResolutionId: null` silently.

**Recommendation**: Add an age check in `reopenPeriod()`:
```ts
const daysSinceClosed = (Date.now() - existing.closedAt!.getTime()) / 86_400_000;
if (daysSinceClosed > 90 && !dto.boardResolutionId) {
  throw new BadRequestException('งวดที่ปิดเกิน 90 วัน ต้องระบุ boardResolutionId');
}
```

**W2 — `as any` casts on AuditLog display fields**  
File: `apps/web/src/pages/AuditLogsPage.tsx`

Three patterns: `(log.newValue as any)?.jvLines`, `(log.oldValue as any)?.jvLines`, `(log.newValue as any)?.diffSummary`. These are runtime-typed JSON blobs, so casting is understandable, but adding a narrow discriminated type or `zod.safeParse` guard would prevent display of `undefined` in production if the shape ever changes.

**W3 — Approver list silently truncated at 200 users**  
File: `apps/web/src/components/expense-form-v4/ApproverSection.tsx`

`GET /users?limit=200` + client-side role filter. If the org ever exceeds 200 users, approvers beyond the 200 cut-off will not appear in the dropdown without any error or warning. For the current business size this is a non-issue, but the backend `/users` endpoint should eventually support a `roles` filter to remove the magic constant.

#### Info

**I1 — Large PR scope**  
56 files, 5924 insertions is the largest single PR in recent history. Recommend splitting Override JV, Maker-Checker, and Reopen Period into separate atomic PRs in future to reduce regression surface.

**I2 — `password: 'x'` in perf test setup**  
File: `apps/api/src/modules/other-income/__tests__/pagination-perf.spec.ts`  
In-memory test DB seed — not a production secret. Acceptable.

**I3 — `Number(searchParams.get('page'))` in frontend hook**  
File: `apps/web/src/hooks/usePaginationParams.ts`  
Parsing URL query params into integers, not financial amounts. Acceptable.

**I4 — CAS guard added to `reopenPeriod`**  
Positive: the new `updateMany` with `status: 'CLOSED'` filter prevents TOCTOU race where two concurrent OWNER requests could both reopen. Good hardening.

**I5 — Sentry fallback on audit write**  
`JV_OVERRIDDEN` and `PERIOD_REOPENED` audit writes are outside the main `$transaction` with `Sentry.captureException` fallback. Pattern is consistent with v2 hardening. Acceptable.

### Recommendation: **REVIEW**

No Critical issues. Warning W1 (boardResolutionId 90-day enforcement gap) is a governance regression that should be addressed before merging — a CLOSED period older than 90 days almost certainly has a filed ภ.พ.30, and reopening it without a board resolution reference weakens the audit trail. Fix is a ≤5 line addition to the service.

---

## Branch 2: `hotfix/expense-form-v4-modal-scroll`

**Author**: Akenarin Kongdach  
**Last commit**: 2026-05-11 09:13 +07:00  
**Scope**: 2 files changed, +15 / -7 lines

### Summary of changes
- `ExpenseFormV4.tsx`: Modal restructured from `overflow-y-auto` on full-viewport overlay to `flex flex-col` with `max-h-[95vh]` + `flex-1 overflow-y-auto` on content area. Footer and header changed from `sticky` to `flex-none`. Fixes footer getting clipped on short viewports.
- `ApproverSection.tsx`: API response shape fix — `GET /users` returns `{ data, total, page, limit }` not a bare array. Changed to `res.data?.data ?? (Array.isArray(res.data) ? res.data : [])` with OWNER-only role filter.

### Issues

No Critical, Warning, or significant Info issues found.

- Uses semantic design tokens throughout (`bg-background`, `border-b`, `bg-muted`, `text-muted-foreground`) — compliant with frontend rules.
- No raw `fetch()` calls.
- No financial (`Number()`) conversions.
- No new controller/guard changes.

### Recommendation: **APPROVE**

---

## Branch 3: `hotfix/expense-form-v4-approvers-shape`

**Author**: Akenarin Kongdach  
**Last commit**: 2026-05-11 08:58 +07:00  
**Scope**: 1 file changed, +9 / -1 lines

### Summary of changes
`ApproverSection.tsx` only — subset of the changes in branch 2. This branch contains the API response shape fix but not the modal scroll fix.

### Issues

**W1 — Hardcoded limit=200 for approver fetch** (same as branch 2 W3)

### Recommendation: **APPROVE**

Note: This branch is a strict subset of `hotfix/expense-form-v4-modal-scroll`. If branch 2 is merged first, branch 3 will fast-forward cleanly or become redundant.

---

## Summary Table

| Branch | Files | +Lines | Critical | Warning | Info | Recommendation |
|--------|-------|--------|----------|---------|------|----------------|
| `feat/other-income-v2-2-pr1-override-jv-pagination` | 56 | +5924 | 0 | 3 | 5 | **REVIEW** |
| `hotfix/expense-form-v4-modal-scroll` | 2 | +15 | 0 | 0 | 0 | **APPROVE** |
| `hotfix/expense-form-v4-approvers-shape` | 1 | +9 | 0 | 1 | 0 | **APPROVE** |

**No branches are blocked.** The two hotfixes are safe to merge immediately. The Other Income v2.2 branch needs one service-layer fix (W1) before merge.
