# Pre-Merge Guard Report — 2026-05-24

Generated by Pre-Merge Guard agent. Reviewed the 3 most recently updated unmerged feature/fix branches.

---

## Branch 1: `feat/defect-exchange-wizard-flow`

**Author:** Akenarin Kongdach  
**Last commit:** 2026-05-23 17:40 BKK  
**Commits on branch:** 3

### File Changes Summary

| File | +Lines | -Lines | Notes |
|------|--------|--------|-------|
| `apps/web/src/pages/DefectExchangePage.tsx` | +286 | -149 | Wizard refactor |
| `apps/api/src/cli/seed-sp1-used-exchange.sql` | +73 | 0 | Dev-only seed data |
| `apps/web/package.json` | +1 | -1 | Minor dep bump |

### Issues Found

#### Critical
_None_

#### Warning
_None_

#### Info

1. **`DefectExchangePage.tsx` approaching 500-line limit** — 459 lines after refactor. Not over threshold yet, but if wizard steps grow (e.g. repair-ticket integration callback), consider splitting step panels into sub-components before that point.

2. **`bypassWindow` server-side enforcement not visible in diff** — The frontend correctly role-gates `bypassWindowCheck: true` (line 89: `bypassWindow = bypassWindowRaw && canExecute`), but the corresponding API endpoint (`POST /defect-exchange/:id/execute`) must also guard `bypassWindowCheck` against non-OWNER/BM roles. The API controller is not modified in this diff, so the existing server guard should already be in place — confirm before shipping.

3. **Dev seed SQL uses hardcoded branch/supplier IDs** — `seed-sp1-used-exchange.sql` references `branch-002`, `sup-001`, `user-004`. These are dev-environment IDs and the file is not executed automatically (CLI-only). Acceptable for a dev fixture; just ensure it is never run in production.

### What Changed (Summary)
Refactored the single-form `DefectExchangePage` into a 3-step wizard matching the `CreateInsuranceWizardPage` pattern:
- Step 1: contract selection + eligibility check
- Step 2: replacement device picker
- Step 3: defect description + confirm

Key quality checks passed:
- Uses `api.get()` / `api.post()` — no raw `fetch()`
- `queryClient.invalidateQueries()` called on all 3 relevant query keys after mutation
- No hardcoded hex colors; uses semantic tokens (`bg-muted`, `text-foreground`, etc.)
- No `Number()` on money fields
- Product limit corrected 300 → 200 (matches `PaginationDto.@Max(200)`)
- `bypassWindow` frontend role-gate is correctly applied

### Recommendation: ✅ APPROVE

---

## Branch 2: `fix/menu-dedup-and-restructure`

**Author:** Akenarin Kongdach  
**Last commit:** 2026-05-23 13:17 BKK  
**Commits on branch:** 3

### File Changes Summary

| File | +Lines | -Lines | Notes |
|------|--------|--------|-------|
| `apps/web/src/config/menu.ts` | +26 | -84 | Menu restructure |
| `apps/web/src/config/menu.test.ts` | +1 | -1 | Test update |
| `apps/web/package.json` | +1 | -1 | Minor dep bump |

### Issues Found

#### Critical
_None_

#### Warning

1. **`/settings/document-config` appears as parent path for two sibling menu sections** — Within `OWNER_CONFIG`, both the 'เอกสารรายรับ' and 'เอกสารรายจ่าย' collapsible sections share `path: '/settings/document-config'`. If the sidebar highlights the active section by matching the parent path, both sections will appear active simultaneously when the user is on `/settings/document-config`. Consider using distinct anchor paths (e.g. `?tab=receivable` vs `?tab=payable`) as the parent paths so active-state detection is unambiguous.

#### Info

2. **Test for removed `owner-fin-collection` replaced with comment, not assertion** — `menu.test.ts` line 91 replaces `expect(keys).toContain('owner-fin-collection')` with a comment noting the section was merged. However, it does not add assertions to confirm that the moved items (`/overdue`, `/repossessions`, `/mdm`) are now reachable under `owner-fin-revenue`. Recommend adding:
   ```ts
   const ownerRevItems = getSidebarForRole('OWNER', 'fin')
     .find((s) => s.key === 'owner-fin-revenue')?.items ?? [];
   const ownerRevPaths = ownerRevItems.map((i) => i.path);
   expect(ownerRevPaths).toContain('/overdue');
   expect(ownerRevPaths).toContain('/repossessions');
   ```

3. **ACCOUNTANT_CONFIG cleanup removes placeholder routes** — Links to `profit-loss`, `cash-flow`, `equity-statement`, `general-ledger`, `vat`, `wht`, `e-tax`, `vat-auto-journal` removed from ACC sidebar. This matches the "only show built pages" principle and reduces dead navigation. No broken functionality — the underlying pages still exist and are reachable via direct URL.

### What Changed (Summary)
- Merged `owner-fin-collection` section (overdue, repossessions, MDM) into `owner-fin-revenue`
- Extracted asset menu into shared `assetMenuSection` constant (used by OWNER, ACC, FM)
- Removed `bank-accounts` placeholder from ACCOUNTANT expense section
- Removed MDM duplicate from OWNER integrations section
- Cleaned ACCOUNTANT `acc-reports` from unbuilt placeholder routes
- Refactored OWNER `owner-period-close` as a separate top-level section

No new controllers, no security-relevant changes, no money field changes.

### Recommendation: ⚠️ REVIEW (Warning item #1 should be addressed or accepted)

---

## Branch 3: `fix/menu-revenue-dedupe`

**Author:** Akenarin Kongdach  
**Last commit:** 2026-05-23 12:25 BKK  
**Commits on branch:** 1

### File Changes Summary

| File | +Lines | -Lines | Notes |
|------|--------|--------|-------|
| `apps/web/src/config/menu.ts` | 0 | -3 | Remove 3 lines from OWNER revenue section |

### Issues Found

#### Critical / Warning / Info
_None_

### What Changed (Summary)
Removes `/overdue`, `/mdm` (ล็อคเครื่อง), and `/repossessions` from the `owner-fin-revenue` items list. These 3 links were erroneously duplicated after a prior restructure.

**Note — superseded by `fix/menu-dedup-and-restructure`:** This branch's single commit (`02e49fae`) is included verbatim in `fix/menu-dedup-and-restructure`. Merging this branch standalone would create a conflict or no-op once `fix/menu-dedup-and-restructure` is merged. Recommend either:
- (a) Close this branch and merge `fix/menu-dedup-and-restructure` which is the superset, or
- (b) Merge this branch first (it is a strict subset), then merge `fix/menu-dedup-and-restructure`

### Recommendation: ✅ APPROVE (merge order matters — see note above)

---

## Summary Table

| Branch | Author | Files | Critical | Warning | Info | Recommendation |
|--------|--------|-------|----------|---------|------|----------------|
| `feat/defect-exchange-wizard-flow` | Akenarin | 3 | 0 | 0 | 3 | ✅ APPROVE |
| `fix/menu-dedup-and-restructure` | Akenarin | 3 | 0 | 1 | 2 | ⚠️ REVIEW |
| `fix/menu-revenue-dedupe` | Akenarin | 1 | 0 | 0 | 0 | ✅ APPROVE (see merge note) |

---

## Security Checklist

| Check | Result |
|-------|--------|
| New controllers with missing `@UseGuards(JwtAuthGuard)` | None found — no new controllers in any branch |
| `Number()` on money/financial fields | None found |
| Missing `deletedAt: null` in new queries | N/A — no new Prisma queries |
| Hardcoded secrets or API keys | None found |
| Missing `@Roles()` on controller methods | N/A — no controller changes |
| SQL injection via `$queryRaw` | None — SQL file is static seed data |
| Raw `fetch()` in React components | None — `api.get()` used correctly |
| Missing `invalidateQueries` after mutations | All mutations properly invalidate |

---

_Report generated: 2026-05-24 by Pre-Merge Guard agent_  
_Branches reviewed: 3 of 281 unmerged feature/fix branches (top 3 by recency)_
