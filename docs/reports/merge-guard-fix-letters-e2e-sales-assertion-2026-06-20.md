# Merge Guard Report — fix/letters-e2e-sales-assertion

**Date**: 2026-06-20  
**Branch**: `fix/letters-e2e-sales-assertion`  
**Author**: Akenarin Kongdach (iamnaii@MacBook-Pro-khxng-Akenarin.local)  
**Latest commit**: `8f3439b6` — 2026-05-26  
**Commits ahead of main**: 7 (letters page + canned-response + E2E fix)

---

## Summary

This branch introduces the `/letters` management page (splitting out from `/collections`), Puppeteer-based letter PDF generation backend, and a fix for a brittle E2E assertion where `"ยกเลิก"` matched both Cancel buttons and the "CANCELLED" status tab. Also carries earlier canned-response work.

### Key commits
- `69f56192` — `feat(letters): /letters management page`
- `8a8c7dc3` — `feat(letters): generate letter PDFs via Puppeteer + HTML template`
- `8f3439b6` — `fix(letters): E2E SALES assertion was matching CANCELLED tab button`

---

## File Changes

| File | Change |
|------|--------|
| `overdue/letter-pdf.service.ts` | 519 new lines — Puppeteer PDF generation |
| `overdue/overdue.controller.ts` | +`GET /overdue/letters/:id/pdf` endpoint |
| `overdue/overdue.module.ts` | +`LetterPdfService` registration |
| `LettersPage/components/BulkPrintDialog.tsx` | Fetch from backend PDF endpoint instead of client jsPDF |
| `apps/web/e2e/letters-page.spec.ts` | Fixed brittle assertion |

---

## Issues Found

### Critical
_None_

### Warning
_None_

### Info

- **[I-1] New PDF endpoint has correct guards**: `GET /overdue/letters/:id/pdf` is under the `OverdueController`, which has class-level `@UseGuards(JwtAuthGuard, RolesGuard, BranchGuard)`. The new endpoint decorates with `@Roles('OWNER', 'FINANCE_MANAGER', 'BRANCH_MANAGER', 'ACCOUNTANT', 'SALES')`, which matches the `/letters` frontend route's `ProtectedRoute` roles. ✓

- **[I-2] HTML injection in PDF template — escaped correctly**: `letter-pdf.service.ts` defines a local `esc()` helper and uses it consistently for all customer-data interpolations into the HTML template (name, address, contract number, company info). `escapeHtmlAttr()` is used for attribute values. No raw unescaped customer data in template strings. ✓

- **[I-3] Frontend follows correct patterns**: `LettersPage.tsx` uses `useQuery`, `useMutation`, `useQueryClient` from `@tanstack/react-query`. No raw `fetch()` calls detected. `queryClient.invalidateQueries()` is called after mutations. ✓

- **[I-4] `letter-pdf.service.ts` is large**: 519 lines is above the 500-line Info threshold. The file is a template renderer — the size is mostly HTML template strings. Could be split into `letter-template.ts` + `letter-pdf.service.ts` in a follow-up, but not blocking. ✓

- **[I-5] E2E fix is correct**: The old assertion checked `toHaveCount(0)` on a button named "ยกเลิก", which also matched the status tab label. The fix replaces it with a positive assertion that SALES can access the page and see the heading. Backend cancel permission is already tested via `@Roles` exclusion of SALES, making the E2E check redundant anyway. ✓

- **[I-6] Puppeteer Chromium resolution**: The PDF service resolves Chromium via env var first, then a candidate list (Linux/Docker + macOS paths). Same pattern as the existing `documents.service`. No hardcoded paths that could fail across environments. ✓

---

## Recommendation

**✅ APPROVE**

Clean security posture: proper guards on new endpoint, HTML escaping throughout the PDF template, correct frontend patterns. The large file is an info-level note for future cleanup. E2E fix is correct and the justification is well-documented.

---
_Generated by Pre-Merge Guard — 2026-06-20_
