# Pre-Merge Guard Report — 2026-05-22

Generated by Pre-Merge Guard agent. Reviewed top 3 unmerged feature branches (most recent by commit date, after filtering out stale squash-merged branches).

---

## Summary

| Branch | Author | Files Changed | Recommendation |
|--------|--------|--------------|----------------|
| `feat/ai-menu-separate` | Akenarin Kongdach | menu.ts, package.json, docs (+5 CSV deleted) | 🔴 **BLOCK** |
| `feat/shop-ai-gemini-dual-mode` | Akenarin Kongdach | gemini.provider.ts, gemini.provider.spec.ts | 🟡 **REVIEW** |
| `feat/sp5p2-warranty-check-unify` | Akenarin Kongdach | WarrantyCheckPage.tsx, App.tsx, E2E tests, +3 files, -1 file | 🟡 **REVIEW** |

---

## Branch 1: `feat/ai-menu-separate`

**Author**: Akenarin Kongdach  
**Commits**: 2 (`feat(menu): split AI into its own top-level section` + docs commit)

### File Changes Summary
- `apps/web/src/config/menu.ts` — Move AI group from nested sub-menu under "ตั้งค่า" to its own top-level section in the settings zone (+28/-17)
- `apps/web/package.json` — Version bump 26.5.14 → 26.5.15
- `docs/` — Several HTML audit reports and spec files added
- **`ข้อมูลโปรแกรมเขียว4-7-2026/bestchoice_{member,contract,order,order_detail,order_installment}.csv`** — **DELETED** (6,933 total lines removed)

### Issues

#### 🔴 Critical — PII Data in Git History (PDPA Violation)

**File**: `ข้อมูลโปรแกรมเขียว4-7-2026/bestchoice_member.csv` (and 4 sibling files)  
**Status**: Being deleted in this commit, but previously committed — data persists in git history

The branch contains 5 CSV files that were committed and are now being deleted. These files contain **real Thai customer PII**:
- Full names (first + last)
- Thai national ID numbers (เลขบัตรประชาชน 13 หลัก) — e.g. `4650800006175`
- Phone numbers
- Full addresses (province, district, sub-district, postal code)
- Dates of birth
- ID card issue/expiry dates
- Identity document image paths

Sample from `bestchoice_member.csv`:
```
"id","code","member_name","member_surname","member_nickname","member_address",
"member_districts","member_amphures","member_province","member_zipcode",
"member_tel","member_identity_number",…
"1","M00001","ฐปนนท์","ปรีชาจารย์","เอิร์ธ","319/144 หมู่ 8","160116","97","7",
"15000","0912799788","4650800006175",…
```

**Why this blocks merge**: Even after deletion, `git log` and `git show <commit-hash>` still expose the data to anyone with repo access. This violates PDPA (พ.ร.บ. คุ้มครองข้อมูลส่วนบุคคล) — particularly Section 37 obligations for data controllers regarding appropriate security measures.

**Required remediation before merge**:
1. Run `git filter-repo --path "ข้อมูลโปรแกรมเขียว4-7-2026/" --invert-paths` to expunge the files from all history on this branch
2. Force-push the cleaned branch
3. Notify the data protection officer (if applicable) and assess whether a breach report is required under PDPA Section 40
4. Rotate any credentials that may have been present in the data

#### ℹ️ Info — TS Changes Are Clean

`menu.ts` change is a well-structured menu refactor. AI items move from nested children under "ตั้งค่า" to a standalone `zone: 'settings'` group. No logic changes, no missing types.

### Recommendation: 🔴 **BLOCK**

Must expunge PII from git history before any merge. The TS code changes themselves are fine, but the git history situation is a critical PDPA/security issue.

---

## Branch 2: `feat/shop-ai-gemini-dual-mode`

**Author**: Akenarin Kongdach  
**Commits**: 2 (`wip: dual-mode Gemini` + `fix: bump Gemini default to 2.5-flash`)

### File Changes Summary
- `apps/api/src/modules/sales-bot/providers/gemini.provider.ts` (+119/-91)
- `apps/api/src/modules/sales-bot/providers/gemini.provider.spec.ts` (+302/-204)

### Issues

#### 🟡 Warning — `GEMINI_API_KEY` and `GEMINI_MODEL` Not Documented in `.env.example`

The branch introduces two new env vars that have no entry in `.env.example`:
- `GEMINI_API_KEY` — AI Studio API key (new)
- `GEMINI_MODEL` — replaces the old `VERTEX_GEMINI_MODEL` key

Any developer (or CI environment) that had `VERTEX_GEMINI_MODEL` set will silently fall back to the hardcoded default `gemini-2.5-flash` after this change, because the code now reads from `GEMINI_MODEL`. The old key is no longer read.

**Fix**: Add these to `.env.example`:
```
# Gemini (choose one mode):
# AI Studio: GEMINI_API_KEY=<paid tier key from https://aistudio.google.com/app/apikey>
# Vertex:    GOOGLE_CLOUD_PROJECT=<gcp-project-id>
# Model (optional, default: gemini-2.5-flash):
GEMINI_MODEL=gemini-2.5-flash
```

#### ℹ️ Info — GEMINI_API_KEY in URL Query Parameter

`gemini.provider.ts:118` builds the AI Studio endpoint with the key as a query param:
```ts
`https://generativelanguage.googleapis.com/v1beta/models/${this.model}:generateContent?key=${this.apiKey}`
```

This is the standard Google AI Studio authentication pattern. The URL is not logged by any logger call in the file (error logs only emit `${this.mode}` and the status code). Infrastructure-level request logs (Cloud Run, nginx) typically do NOT log outbound fetch() URLs. Risk is low, but be aware when reviewing any future HTTP proxy or tracing additions.

#### ℹ️ Info — Behavior Change: Default Model Upgraded

`DEFAULT_MODEL` changes from `gemini-2.0-flash-001` → `gemini-2.5-flash`. All deployments without `GEMINI_MODEL` set will use 2.5-flash. This is intentional per the commit message and well-tested.

#### ℹ️ Info — Thinking Mode Disabled for gemini-2.5 Series

```ts
if (this.model.startsWith('gemini-2.5')) {
  generationConfig.thinkingConfig = { thinkingBudget: 0 };
}
```

Well-motivated and tested. Comment explains the empirical rationale (190 hidden reasoning tokens vs 0 with identical quality).

### Code Quality
- Guards: no new controllers — not applicable
- Money fields: no financial calculations — not applicable
- TypeScript: clean typing, no `any` in new code
- Tests: comprehensive coverage added for both AI Studio and Vertex modes, `isReady()` states, error cases, and thinking config

### Recommendation: 🟡 **REVIEW**

One action item before merge: add `GEMINI_API_KEY` and updated `GEMINI_MODEL` entries to `.env.example`.

---

## Branch 3: `feat/sp5p2-warranty-check-unify`

**Author**: Akenarin Kongdach  
**Commits**: 6 (SP5 Phase 2 final cleanup, E2E tests, routing unification, etc.)

### File Changes Summary
- `apps/web/e2e/insurance-warranty-check.spec.ts` — NEW (225 lines)
- `apps/web/e2e/insurance-wizard-exchange.spec.ts` — NEW (174 lines)
- `apps/web/e2e/insurance-wizard-repair.spec.ts` — NEW (159 lines)
- `apps/web/src/App.tsx` — Routing: adds `/insurance/warranty-check`, redirects `/defect-exchange` to wizard
- `apps/web/src/components/DefectExchangeRedirect.tsx` — NEW redirect component (13 lines)
- `apps/web/src/config/menu.ts` — Adds warranty-check link to insurance menu
- `apps/web/src/pages/InsurancePage.tsx` — Minor header changes
- `apps/web/src/pages/insurance/CreateRepairTicketPage.tsx` — **DELETED** (528 lines)
- `apps/web/src/pages/insurance/RepairTicketDetailPage.tsx` — Minor: adds role check via `useAuth()`
- `apps/web/src/pages/insurance/WarrantyCheckPage.test.tsx` — NEW unit tests (215 lines)
- `apps/web/src/pages/insurance/WarrantyCheckPage.tsx` — NEW page (172 lines)

### Issues

#### 🟡 Warning — Placeholder Text Contains Internal TODO Visible to Users

**File**: `apps/web/src/pages/insurance/WarrantyCheckPage.tsx:96`

```tsx
placeholder={
  mode === 'customer'
    ? 'Customer UUID (จาก /customers — รอ integrate autocomplete)'
    : …
}
```

`'รอ integrate autocomplete'` (= "waiting to integrate autocomplete") is a developer note that will be displayed to real users as a form placeholder. Should be replaced with a clean user-facing string such as `'รหัสลูกค้า'` or `'กรุณาเลือกลูกค้า'`.

#### ℹ️ Info — `/defect-exchange` Route Loses Explicit Role Guard

**File**: `apps/web/src/App.tsx:619-625`

Before:
```tsx
<ProtectedRoute roles={['OWNER', 'BRANCH_MANAGER', 'FINANCE_MANAGER', 'SALES']}>
  <DefectExchangePage />
</ProtectedRoute>
```

After:
```tsx
<Route path="/defect-exchange" element={<DefectExchangeRedirect />} />
```

`DefectExchangeRedirect` does `<Navigate to="/insurance/new?intent=exchange">` which has its own `ProtectedRoute`. An unauthenticated user hitting `/defect-exchange` now traverses two redirects before landing at login (via `/insurance/new` → login), rather than one. Functionally equivalent — the end state is the same. Low risk.

#### ℹ️ Info — Backend Warranty Lookup Properly Guarded

Verified `repair-tickets.controller.ts:32,57-58`:
```ts
@UseGuards(JwtAuthGuard, RolesGuard, BranchGuard)
// ...
@Get('warranty-lookup')
@Roles('OWNER', 'BRANCH_MANAGER', 'FINANCE_MANAGER', 'SALES', 'ACCOUNTANT')
```

Matches the frontend `ProtectedRoute roles` declaration exactly. No security gap.

#### ℹ️ Info — API Calls Use Correct Pattern

`WarrantyCheckPage.tsx` uses `api.get()` from `@/lib/api` — correct per frontend rules.

#### ℹ️ Info — Large Page Deletion

`CreateRepairTicketPage.tsx` deleted (528 lines). Functionality appears to be consolidated into `CreateInsuranceWizardPage` (referenced in App.tsx). The unified wizard handles both repair and exchange intents via `?intent=` param. Consistent with the SP5 Phase 2 C.2 spec note in the commit.

### Code Quality
- No `Number()` on financial fields — not applicable (no money calculations in new code)
- No raw `fetch()` — all API calls use `api.get()`
- `useQuery` used for data fetching — correct
- `QueryBoundary` used for error/loading states — correct
- Thai UI text present

### Recommendation: 🟡 **REVIEW**

One fix before merge: clean up the placeholder text on the customer search mode input in `WarrantyCheckPage.tsx:96`.

---

## Additional Notes

### Stale Branch Cleanup (Not Blocking)

The following branches are effectively already merged into `main` via squash commits (PRs #1067, #1068, #1069) but the branch refs haven't been deleted:
- `origin/fix/grounding-guard` (merged as #1067)
- `origin/fix/search-products-stock-and-price` (merged as #1068)
- `origin/fix/soften-price-missing` (merged as #1069)

These should be cleaned up to reduce branch clutter: `git push origin --delete fix/grounding-guard fix/search-products-stock-and-price fix/soften-price-missing`

---

*Report generated: 2026-05-22 | Guard branch: `guard/review-2026-05-22`*
