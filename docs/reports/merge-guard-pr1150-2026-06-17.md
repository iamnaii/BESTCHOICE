# Pre-Merge Guard Report — PR #1150 — 2026-06-17

**Agent**: Pre-Merge Guard (automated, run 2)
**Run date**: 2026-06-17
**Scope**: 1 open GitHub PR (only open PR in the repo)

---

## Summary

| PR | Branch | Last commit | Critical | Warning | Info | Recommendation |
|----|--------|-------------|----------|---------|------|----------------|
| #1150 | `feat/contacts-audit-cleanup` | 2026-06-04 | 0 | 1 | 1 | **APPROVE** (after CI) |

---

## PR #1150 — feat/contacts-audit-cleanup

**Title**: feat(contacts): trade-in seller name on contact card + audit cleanup  
**Author**: iamnaii (Akenarin Kongdach)  
**Opened**: 2026-06-04  
**Base**: `main` @ `3ad5e99c`  
**Head**: `feat/contacts-audit-cleanup` @ `2d3f7428`

### File changes summary (5 files, +54/-8)

| File | +/- | Notes |
|------|-----|-------|
| `apps/web/src/pages/ContactDetailPage.tsx` | +14/-4 | `TradeInTile`: add `sellerName`+`sellerPhone` in 2-col grid |
| `apps/web/src/pages/__tests__/ContactDetailPage.test.tsx` | +37/0 | 2 new tests: seller-name display + summary-failure resilience |
| `docs/superpowers/specs/2026-06-01-contact-rich-fields-A1-design.md` | +1/-1 | Status header: `รออนุมัติ` → `✅ DONE` |
| `docs/superpowers/specs/2026-06-02-contact-360-presentation-redesign-design.md` | +1/-1 | Same |
| `docs/superpowers/specs/2026-06-02-contact-financial-snapshot-C-design.md` | +1/-1 | Same |
| `docs/superpowers/specs/2026-06-02-contact-hardening-design.md` | +1/-1 | Same |

### Critical issues

None.

No new controllers, guards, DTOs, or backend changes. Pure frontend display + tests + docs.

### Warning issues

**W1 — CI was cancelled; PR has been sitting open for 13 days**

Both "Lint & Test" and "E2E Tests (1/2)" checks were cancelled (not failed). The cancellation happened at approximately 2026-06-04 when the PR was opened. This is most likely caused by CI infrastructure instability or the pre-existing test failures that were fixed in `fix/ci-pre-existing-test-failures` (those fixes are now in main as of 2026-06-08+).

The PR's base commit (`3ad5e99c`) predates those CI fixes. The branch should be rebased/updated against current main before merge so CI runs cleanly.

**Action**: Update PR branch against current main (`a420359a`) and verify CI passes before merging.

### Info

**I1 — Fallback when both `sellerName` and `sellerPhone` are null**

```tsx
value={
  tradeIn.sellerName
    ? `${tradeIn.sellerName}${tradeIn.sellerPhone ? ` (${tradeIn.sellerPhone})` : ''}`
    : tradeIn.sellerPhone  // ← could be undefined/null
}
```

When both fields are null, the `Field` component receives `undefined` as value. If `Field` renders `undefined` gracefully (empty string), this is fine. Low risk — data from DB is likely always present for accepted trade-ins.

### Code quality assessment

The change is minimal and well-scoped:
- `TradeInTile` now shows seller identity (name + phone) alongside the date — matches spec §2 intent.
- The 2-column grid layout (`grid-cols-2 gap-3`) is consistent with other card layouts in the codebase.
- Two new tests are meaningful and test real behavior (not mock pass-throughs).
- No `any` types introduced.
- No hardcoded colors or design token violations.
- Thai UI text follows conventions.

### Recommendation: **APPROVE** — after branch update

This is a safe, low-risk PR. The only blocker is stale CI. Once the branch is updated against current main and CI passes, this is ready to merge.

---

## Context: Other unmerged branches

197 branches show as `--no-merged` against `main`, but ALL have `git diff origin/main...<branch>` = 0 lines. These are squash-merge artifacts (commits remain on branch refs but file content is identical to main). There is no hidden unreviewed code among them.

The two large architectural branches (`worktree-feat+sp7.1-dual-prisma-foundation`, `worktree-feat-shop-sales-ai-phase-a`) are orphan branches (no merge base with main) representing future architectural work. They were reviewed in the earlier guard run today (2026-06-17 18:12 UTC) with REVIEW recommendations; no new commits since then.

---

*Guard run 2 of 2 for 2026-06-17. Previous run: `guard/review-2026-06-17` (18:12 UTC).*
