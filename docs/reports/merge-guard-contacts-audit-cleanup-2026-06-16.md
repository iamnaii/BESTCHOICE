# Pre-Merge Guard Report — feat/contacts-audit-cleanup

**Date**: 2026-06-16  
**PR**: [#1150](https://github.com/iamnaii/BESTCHOICE/pull/1150)  
**Branch**: `feat/contacts-audit-cleanup`  
**Author**: iamnaii  
**Base SHA at PR creation**: `3ad5e99c`  
**Current main HEAD**: `a420359a`  

---

## Summary

Single open PR. Small frontend-only change: shows `sellerName` + `sellerPhone` on the TradeIn tile in `ContactDetailPage`, plus 2 new regression tests and 4 spec-header status updates.

**Files changed (6)**

| File | +/- | Type |
|------|-----|------|
| `apps/web/src/pages/ContactDetailPage.tsx` | +14 / -4 | Feature (UI) |
| `apps/web/src/pages/__tests__/ContactDetailPage.test.tsx` | +37 / 0 | Tests |
| `docs/superpowers/specs/2026-06-01-contact-rich-fields-A1-design.md` | +1 / -1 | Docs |
| `docs/superpowers/specs/2026-06-02-contact-360-presentation-redesign-design.md` | +1 / -1 | Docs |
| `docs/superpowers/specs/2026-06-02-contact-financial-snapshot-C-design.md` | +1 / -1 | Docs |
| `docs/superpowers/specs/2026-06-02-contact-hardening-design.md` | +1 / -1 | Docs |

No backend changes. No Prisma schema changes. No new endpoints.

---

## Critical Issues

**None found.**

Checklist:
- [x] No new controllers — `@UseGuards` / `@Roles` N/A
- [x] No `Number()` on money fields — changed fields are `string | null` (name, phone, date)
- [x] No new Prisma queries — `deletedAt: null` N/A
- [x] No hardcoded secrets or API keys
- [x] No `$queryRaw` usage
- [x] `ContactTradeInLink` type already declares `sellerName: string | null` and `sellerPhone: string | null` — TypeScript-safe

---

## Warning Issues

**None found.**

Checklist:
- [x] No new DTOs — validation decorators N/A
- [x] No new service methods — error handling N/A
- [x] No raw `fetch()` — component reads from `contactsApi.detail` (from `@/lib/api/contacts`)
- [x] No mutations in changed code — `invalidateQueries` N/A
- [x] No Thai validation messages needed (no DTO changes)
- [x] Design tokens used correctly: `text-muted-foreground`, `text-foreground` — no hardcoded hex
- [x] Thai text uses `leading-snug` (verified in `Field` component)

---

## Info

- **`ContactDetailPage.tsx` is 542 lines** — slightly over the 500-line guideline. Not a blocker, but worth tracking. The file grew organically from multiple contact role tiles. Consider splitting role tiles into a `ContactTiles/` sub-folder when the next feature touches this file.
- Tests use `as any` mock casts on `contactsApi.detail` — standard vi.fn() pattern for this project's test suite, not a concern in production code.
- The 4 doc status updates (`รออนุมัติ spec` → `✅ DONE`) are accurate — those specs were merged 2026-06-02 per the commit history. Keeping docs current is good hygiene.

---

## Recommendation

**✅ APPROVE**

This is a low-risk, well-scoped patch. The field it adds (`sellerName/sellerPhone`) already exists on the `ContactTradeInLink` TypeScript interface and the API response — this is purely a display gap fix. Both new tests cover the exact scenarios described in the PR body. No backend changes, no money handling, no auth changes.

The PR is safe to merge as-is.

---

*Generated by Pre-Merge Guard — 2026-06-16*
