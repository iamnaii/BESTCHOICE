# Pre-Merge Guard Report — 2026-04-27

Generated by: Pre-Merge Guard agent  
Date: 2026-04-27  
Branches reviewed: 3 (top 3 by recency from 192 unmerged feature branches)

---

## Branch 1: `chore/audit-quick-wins`

**Author**: iamnaii@MacBook-Pro-khxng-Akenarin.local  
**Last commit**: 2026-04-26 20:17:41 +0700  
**Commits ahead of main**: 4

### File Changes Summary
```
13 files changed, 209 insertions(+), 44 deletions(-)
apps/api/prisma/schema.prisma                          +7 (3 compound indexes)
apps/api/prisma/migrations/…add_audit_compound_indexes +15 (idempotent IF NOT EXISTS)
apps/api/src/modules/dashboard/dashboard.service.ts   +121 (groupBy refactor + perf)
apps/api/src/modules/customers/customers.controller.ts +14 (limit caps)
apps/api/src/modules/line-oa/broadcast.controller.ts  +15 (ParseFilePipe validation)
apps/api/src/modules/line-oa/line-oa.controller.ts    +24 (ParseFilePipe validation)
apps/api/src/modules/shop-auth-social/…controller.ts  +11 (ShopBotDefenseGuard)
apps/api/src/modules/shop-installment-apply/…ts       +4  (ShopBotDefenseGuard + Throttle)
apps/api/src/modules/shop-me/shop-me.controller.ts    +15 (DTO typing + capacity limit)
apps/api/src/modules/shop-reservation/…controller.ts  +6  (ShopBotDefenseGuard)
apps/api/src/modules/shop-tracking/…controller.ts     +6  (ShopBotDefenseGuard)
apps/api/src/modules/staff-chat/web-widget.controller  +13 (DTO + Throttle)
apps/api/src/modules/journal/journal.controller.ts    +2  (minor)
```

### Issues

#### Critical
_None._

#### Warning
- **`dashboard.service.ts:139`** — `new Prisma.Decimal(a._sum.sellingPrice ?? 0).toNumber()` stores `totalSales` as a JS float. The branch does use Decimal arithmetic for accumulation (`.add(new Prisma.Decimal(...)).toNumber()`), but the final bucket value is a JS `number` used for `.sort()`. This is a display-only dashboard KPI, not a financial transaction, so precision loss is limited to cosmetic rounding. Still diverges from the project "Decimal for money fields" rule.

#### Info
- `shop-me.controller.ts` calls `this.prisma` directly (pre-existing anti-pattern — branch touches the file but does not introduce it; acceptable to carry forward as-is).
- `overdueRate: Number(((count/total)*100).toFixed(1))` — percentage, not a money field; acceptable.
- 3 new compound indexes use `IF NOT EXISTS` — safe for idempotent re-run.
- File-upload hardening (ParseFilePipe + MaxFileSizeValidator + FileTypeValidator) on broadcast and LINE OA image uploads — good security improvement.

### Recommendation: **APPROVE**

No critical issues. Warning on `totalSales` float storage is acceptable for a dashboard aggregate. Security hardening (bot defense guards, file-upload validation, query-param caps) is straightforward and correct.

---

## Branch 2: `feat/collections-guided-session`

**Author**: iamnaii@MacBook-Pro-khxng-Akenarin.local  
**Last commit**: 2026-04-26 19:57:14 +0700  
**Commits ahead of main**: 52

### File Changes Summary
```
86 files changed, 10193 insertions(+), 3357 deletions(-)
New module: apps/api/src/modules/collections-session/ (controller, 4 services, cron, 4 spec files)
New DTOs: action.dto.ts, skip.dto.ts (collections-session)
New DTO: settings/dto/collections-config.dto.ts
New DTO: auth/dto/update-preferences.dto.ts
Migrations: 20260426000001_rename_daily_assignment_to_snake_case (NEW)
           20260610000000_add_daily_assignment (in branch, also in main)
New schema models: DailyAssignment, enums AssignmentSource/Status/Outcome/SkipReason
Frontend: apps/web/src/pages/… (collections session UI, team dashboard, settings tab)
E2E: apps/web/e2e/collections-session.spec.ts (new)
Docs: 3 large markdown plan files (safe)
```

### Issues

#### Critical

**`20260426000001_rename_daily_assignment_to_snake_case/migration.sql` — Migration ordering bug**

The rename migration has timestamp `20260426` (April 26) which sorts **before** the create migration `20260610000000_add_daily_assignment` (June 10). Prisma applies migrations in lexicographic timestamp order.

On **production** (where `20260610000000` was already applied): the rename migration will run successfully because "DailyAssignment" already exists. ✓

On **fresh databases** (CI, new dev, disaster-recovery restore): Prisma will run `20260426000001` first → `ALTER TABLE "DailyAssignment" RENAME TO "daily_assignments"` fails with `ERROR: table "DailyAssignment" does not exist`. `20260610000000` never runs. The entire migration sequence is stuck. **This breaks CI, dev onboarding, and any fresh restore.**

**Fix**: Rename migration directory from `20260426000001_rename_daily_assignment_to_snake_case` to `20260611000000_rename_daily_assignment_to_snake_case` (any timestamp after `20260610000000`). On production the table already exists so the rename still succeeds; on fresh DBs the create runs first.

#### Warning

- **`team-dashboard.service.ts` (approx. line 1744 in diff)** — `Number(p._sum.amountPaid ?? 0)` converts a Decimal payment sum to JS float before inserting into a display Map (`collectedByCollector`). Used to show team collection totals in dashboard — not written to DB. Violates "Decimal for money fields" rule. Fix: use `new Prisma.Decimal(p._sum.amountPaid ?? 0)` and store as Decimal, or document clearly that this is display-only with a comment.

- **`collections-session.service.ts` — `readNumber()` helper** — Parses setting strings with `Number(raw)`. These values are integer thresholds (cap counts, time durations — not money), but `parseInt(raw, 10)` would be clearer and would handle trailing whitespace/decimals without silent NaN-to-fallback.

#### Info

- `CollectionsSessionController` — correctly has `@UseGuards(JwtAuthGuard, RolesGuard)` at class level with `@Roles()` on every method. ✓
- All new DTOs (ActionDto, SkipDto, CollectionsConfigDto, UpdatePreferencesDto) have class-validator decorators with Thai-language error messages. ✓
- `settings/settings.controller.ts` new `GET /settings/collections` and `PUT /settings/collections` inherit class-level `@Roles('OWNER')` — appropriate for admin config. ✓
- `auth/auth.controller.ts` new `PATCH /auth/me/preferences` has `@UseGuards(JwtAuthGuard)` without `@Roles()` — correct for a user-owned endpoint. ✓
- Cron jobs have Sentry capture on failure. ✓
- Auto-assign service (auto-assign.service.ts) has 220-line test suite. ✓
- `deletedAt: null` filters present on all new queries. ✓

### Recommendation: **BLOCK**

The migration ordering bug (`20260426000001` sorts before `20260610000000`) will break any fresh-database setup including CI. Fix the timestamp before merging. The two Warnings are non-blocking but should be addressed in the same PR to avoid tech debt.

---

## Branch 3: `feat/liff-early-payoff-direct-paysolutions`

**Author**: iamnaii@MacBook-Pro-khxng-Akenarin.local  
**Last commit**: 2026-04-24 17:55:55 +0700  
**Commits ahead of main**: 179 (includes historical parent branch commits; diff is 1 file only)

### File Changes Summary
```
1 file changed, 19 insertions(+), 3 deletions(-)
apps/web/src/pages/liff/LiffEarlyPayoff.tsx
```

### Changes Description
Replaces the 2-hop flow (LIFF → `/line-oa/liff/early-payoff` → `/pay/{token}`) with a direct call to `/paysolutions/create-intent`. The intermediate `PaymentLink` shell creation is bypassed. `installmentNo` is intentionally omitted to skip per-installment amount validation.

### Issues

#### Critical
_None._

#### Warning

- **`LiffEarlyPayoff.tsx:79`** — `amount: Number(quote.totalPayoff)` — `totalPayoff` is already typed as `number` in `LiffEarlyPayoffQuote` (packages/shared/src/liff-types.ts). The `Number()` cast is redundant and harmless (no additional precision loss since JSON already deserializes to JS float), but it signals confusion about the type. Remove the cast or leave a comment.

- **Design: `installmentNo` bypass** — The comment reads _"installmentNo is intentionally omitted so backend skips per-installment amount validation"_. This is a deliberate bypass of a backend guard. Ensure the `/paysolutions/create-intent` endpoint has its own total-amount validation (max payoff sanity check) so a tampered `amount` field cannot trigger an oversized PaySolutions charge. Verify with backend owner.

- **Design: `PaymentLink` model skip** — The old flow created a `PaymentLink` record (audit trail, idempotency key). Verify that `/paysolutions/create-intent` still creates a `Payment` or `PaymentIntent` record before redirecting, so the early payoff is auditable and idempotent (no double-charge on retry).

#### Info

- No new controllers, DTOs, or guards — LIFF page only. ✓
- `useMutation` with `liffApi.post()` — correct API client usage. ✓
- `toast.error()` on `onError` — correct notification pattern. ✓

### Recommendation: **REVIEW**

No critical security issues. The two design-level Warnings (installment validation bypass and PaymentLink audit trail) need backend owner confirmation before merge. Once confirmed (or documented), this branch is safe to ship.

---

## Summary

| Branch | Files | Recommendation | Blocker |
|--------|-------|----------------|---------|
| `chore/audit-quick-wins` | 13 | ✅ APPROVE | — |
| `feat/collections-guided-session` | 86 | 🔴 BLOCK | Migration `20260426000001` timestamp sorts before `20260610000000` — breaks fresh DB |
| `feat/liff-early-payoff-direct-paysolutions` | 1 | 🟡 REVIEW | Design decisions on validation bypass + audit trail need sign-off |
