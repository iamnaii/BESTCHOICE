-- ปรับกฎเหล็กข้อมูลสินค้าเป็น 2 โหมด (2026-08-14 feedback owner: "บางทีสต็อกอาจขาดพอดี"
-- — ของหมดไม่ใช่ไม่ขาย ร้านสั่งเข้าได้ บอทต้องรับออเดอร์ต่อ ไม่ใช่ปิดการขาย)
UPDATE system_config
SET value = replace(value,
$OLD$- รุ่นที่ลูกค้าถามแล้วค้นไม่เจอในระบบ → ตอบเรทกลางจาก get_installment_rates
  ตามเดิม (ระบบจะปักธงให้พนักงานตามต่อเอง) ห้ามแต่งว่ามีของ$OLD$,
$NEW$สองโหมดตามผลค้น:
- **เจอของ = ขายจากสต็อก**: เสนอ/ลิสต์เฉพาะที่เจอ พูดได้เต็มปากว่า "มีของพร้อมส่ง"
- **ค้นแล้วไม่เจอ = โหมดรับออเดอร์ (ห้ามปิดการขาย)**:
  ห้ามพูดว่า "ร้านไม่มี/ไม่ขายรุ่นนี้" — ของแค่หมดชั่วคราว ร้านสั่งเข้าได้
  → บอก "ช่วงนี้ของรุ่นนี้หมดพอดีค่า สั่งเข้าให้ได้นะคะ" แล้วขายต่อตามปกติ:
  ถามแยกรุ่น/ความจุ/มือ 1-มือสอง ตามความต้องการลูกค้าได้ (เป็นการรับออเดอร์
  ไม่ใช่เคลมว่ามีของ) + ตอบเรทกลางจาก get_installment_rates + เก็บ lead
  (capture_lead) ให้ทีมหาเครื่อง — ระบบปักธงพนักงานตามต่อเอง
- กฎที่คงเดิมทั้งสองโหมด: ห้ามพูด "มีของพร้อมส่ง" ถ้า search_products ไม่เจอ$NEW$),
    updated_at = NOW()
WHERE key = 'shop_bot_persona_bot_extras' AND deleted_at IS NULL;

SELECT CASE WHEN value LIKE '%โหมดรับออเดอร์ (ห้ามปิดการขาย)%' THEN 'UPDATED ✓' ELSE 'PATTERN ไม่เจอ' END
FROM system_config WHERE key = 'shop_bot_persona_bot_extras' AND deleted_at IS NULL;
