# เริ่มใช้คลัง + จัดซื้อจริง (ล้างคลังเป็น 0) — Runbook

| | |
|---|---|
| วันที่เขียน | 2026-09-05 |
| Spec | `docs/superpowers/specs/2026-09-05-stock-go-live-design.md` |
| คำตัดสิน | เจ้าของ 2026-09-05 — คลังว่างเปล่า (รวม TTFY 604) · ขายจริงจากคลังจริงขนานกับการเทสส่วนอื่น · marker บน DB เดียว · เก็บซัพพลายเออร์ |
| เครื่องมือ | `npm --prefix apps/api run wipe:stock-go-live` (dry-run เป็นค่าเริ่มต้น — ไม่เขียนอะไรจนกว่าจะใส่ consent ครบ) |
| ย้อนกลับ | SQL ต่อตารางที่ CLI พิมพ์ตอนจบ (`deleted_at = <wipedAt>` ค่าเดียวทั้งรอบ) — ดูขั้น ⑧ |
| สถานะ | ⏳ **ยังไม่ได้รันบน production** — dry-run ผ่านบน dev DB แล้ว 2026-09-05 (พิมพ์แผนครบ เขียนลง DB = 0 แถว) · ด่าน 5 ของ `factory:reset` พิสูจน์แล้วบน dev ว่าปฏิเสธจริง (`สินค้าจริง 47 เครื่อง, ใบรับของจริง 6 ใบ` exit 1) |

> ⚠️ **หลังรันขั้น ⑤ แล้ว `factory:reset` จะปฏิเสธการรัน** (ด่าน 5 — มีของจริงในระบบ)
> ทางล้างข้อมูลทดสอบหลังจากนี้คือ `cleanup:test-pack` + `cleanup:test-contracts` เท่านั้น
> (`cleanup:test-contracts` รันจาก `dist/` ⇒ ต้อง `npm --prefix apps/api run build` ก่อนหนึ่งครั้ง)

---

## วินัย marker — อ่านก่อนเทสด้วยมือหลังจากนี้

ระบบตัดสิน "ทดสอบ" จาก marker เท่านั้น (ไม่มีคอลัมน์ isTest — แหล่งความจริงเดียวคือ
`apps/api/src/utils/test-data-markers.ts`):

| อยากเทส | ต้องทำ |
|---|---|
| ลูกค้าทดสอบ | ที่อยู่ปัจจุบัน = `ข้อมูลทดสอบระบบ — ลบได้` (หรือเบอร์ขึ้นต้น `TEST-`) — แก้ได้ที่หน้าแก้ไขลูกค้า |
| เครื่องทดสอบ | IMEI ขึ้นต้น `TEST-` ตอนรับของ (หรือชื่อขึ้นต้น `ทดสอบระบบ`; อุปกรณ์เสริมที่ไม่มี IMEI: รับผ่าน PO ทดสอบ) |
| ซัพพลายเออร์ทดสอบ | ชื่อขึ้นต้น `ทดสอบระบบ` |
| PO ทดสอบ | เลข PO ระบบออกให้ ตั้งเองไม่ได้ ⇒ ใส่ IMEI `TEST-` ตอนรับของแทน (PO ที่ seed มาใช้ `TEST-PO-*`) |
| โอน/ปรับสต็อกทดสอบ | หมายเหตุขึ้นต้น `[ทดสอบระบบ]` |
| ใบนับสต็อกทดสอบ | เลขใบขึ้นต้น `TEST-COUNT-` (seed เท่านั้น — สร้างมือไม่ได้ ⇒ ใบนับที่คีย์มือ = ของจริง) |

**ของที่ไม่มี marker = ของจริง** — ถูกล้างในรอบนี้ และหลังจากนี้:
- รั้ว (`assertSameTestSide`) ที่ POS / เปิดสัญญา / ใบจอง **จะไม่ให้**ขายเครื่อง `TEST-` ให้ลูกค้าจริง
  และไม่ให้ขายเครื่องจริงให้ลูกค้าทดสอบ — ข้อความ error บอกวิธีแก้ (เลือกลูกค้า/เครื่องอีกฝั่ง หรือแก้
  ที่อยู่ปัจจุบันของลูกค้า) · รั้วนี้ **ไม่มีสวิตช์ปิด**
- เครื่องเทิร์นจากลูกค้าทดสอบได้ชื่อขึ้นต้น `ทดสอบระบบ ` เอง (IMEI ไม่ถูกแตะ)
- รั้ว**ไม่ครอบ**เว็บช็อปสาธารณะ — เครื่องทดสอบที่เปิด "แสดงบนเว็บ shop" ลูกค้าจริงยังเห็นและสั่งได้
  ⇒ ขั้น ④ ต้องปิดเอง

---

## CLI ทำอะไร / ไม่ทำอะไร — รู้ก่อนกดรัน

| ทำ | ไม่ทำ (ต้องตัดสิน/ทำเองต่างหาก) |
|---|---|
| soft-delete **สินค้าบนชั้น**ที่ไม่มี marker (สถานะ `IN_STOCK`/`PHOTO_PENDING`/`QC_PENDING`/`REFURBISHED`/`DAMAGED`/… ) ด้วย `deleted_at` ค่าเดียวทั้งรอบ | **ไม่แตะบัญชี** (GL `S11-2001/2002/2003` ไม่ขยับ) · ไม่แตะ `suppliers` (คำตัดสิน D4) · `reorder_points` · `inspections` · `imported_sales` · `trade_ins` · ราคา/รูปสินค้า · ลูกค้า |
| PO จริง + ใบรับของของ PO นั้น — **เฉพาะ PO ที่ไม่เหลือเครื่องรอดชี้อยู่** (เครื่องรอด = ทดสอบ / ติดด่านถือครอง / ผูกธุรกรรม) | เครื่องที่**สัญญา/ใบจอง/ออเดอร์ทดสอบถืออยู่** → ข้าม+รายงาน ไม่ลบ · เครื่องสถานะ `SOLD_*`/`RESERVED`/`REPOSSESSED` → ไม่แตะ รายงานแยก |
| ใบโอน / ใบปรับ / ใบนับ / แจ้งเตือน ที่ไม่มี marker **ของใบเอง** | ⚠️ **เอกสารเคลื่อนไหวถูกเลือกด้วย marker ของใบ ไม่ใช่ตามเครื่อง** — ประวัติโอน/ปรับ/นับของเครื่องที่รอด (เพราะมีธุรกรรมถืออยู่ หรือเป็นเครื่องทดสอบที่ใบไม่มี marker) **หายไปพร้อมกัน** · เคสที่เจ็บที่สุดคือ**เครื่องระหว่างโอน**: ใบโอนหาย เครื่องยังอยู่ หน้ารับของปลายทางไม่มีอะไรให้รับ — ดูขั้น ③ ข้อ 8 และขั้น ⑥ |
| AuditLog 1 แถว (`action = STOCK_GO_LIVE_WIPE`, `entity_id = <wipedAt>`) + พิมพ์ SQL ย้อนกลับ 12 บรรทัด | JE ตอนรับของ / JE ยอดยกมาสินค้าคงเหลือ — รอผู้สอบ (ดูขั้น ⑦ ท้าย) |

`ONLY_BRANCH_ID=<uuid>` (ถ้าอยากล้างทีละสาขา) จำกัดเฉพาะ **สินค้า · ใบโอน (ต้นทางหรือปลายทาง) ·
ใบนับ · ใบปรับ · แจ้งเตือน** — **PO และใบรับของไม่มีคอลัมน์สาขา จึงพิจารณาทั้ง DB เสมอ**:
PO ที่ยังมีเครื่องรอดอยู่ที่สาขาอื่นจะถูกเก็บไว้ (ตามกติกา "ไม่ปล่อยให้เครื่องชี้ PO ที่ถูกลบ")
ส่วน PO ที่ไม่เหลือเครื่องเลยจะถูกล้างแม้จะระบุสาขา

---

## ขั้นตอน

### ① จุดกู้คืน ⚠️ ห้ามข้าม (PITR ปิด — นี่คือจุดเดียว)

```bash
gcloud sql backups create --instance=bestchoice-db --description="before stock go-live $(date +%F)"

# รอจนขึ้น SUCCESSFUL ก่อนไปขั้นถัดไป
gcloud sql backups list --instance=bestchoice-db --limit=2 --format="table(id,windowStartTime,type,status)"
```

จดเลข backup ไว้ — ถ้าต้องกู้ทั้งฐาน (ขั้น "ถ้าล้างผิด" ท้ายเอกสาร) จะได้แค่ ณ จุดนี้ ไม่มีทางเลือกเวลา

### ② Deploy โค้ดงานนี้ก่อน

รั้ว (POS / เปิดสัญญา / ใบจอง) + auto-mark เทิร์น + ด่าน 5 ของ factory reset ต้องอยู่บน prod
**ก่อน** ล้าง — ไม่งั้นช่วงระหว่างล้างกับ deploy จะขายข้ามฝั่งได้ และ `factory:reset` ยังลบใบรับของจริงได้

```bash
gh run list --workflow deploy-gcp.yml --branch main --limit 3
```

ต้องเขียวที่ commit ที่รวมงานนี้ (branch `feat/stock-go-live-2026-09` หลัง merge เข้า `main`)

### ③ Dry-run (ไม่เขียนอะไร)

หน้าต่างแรก — เปิด proxy ค้างไว้ (สูตรเดียวกับ `scripts/ops/*.sh`):

```bash
cloud-sql-proxy --port 15432 bestchoice-prod:asia-southeast1:bestchoice-db
```

หน้าต่างที่สอง — รันจาก checkout ของ repo ที่ `npm install` แล้ว (CLI รันจาก source ผ่าน `tsx`
ไม่ต้อง build; ใช้ `DATABASE_URL` ตัวเดียว ไม่ต้องตั้ง `DATABASE_URL_FINANCE`):

```bash
export DATABASE_URL="postgresql://<user>:<password>@127.0.0.1:15432/bestchoice?schema=public"
EXPECTED_DB_NAME=bestchoice npm --prefix apps/api run wipe:stock-go-live
```

ต้องเห็นบรรทัด `DRY-RUN mode (default)` บนสุด และ `DRY-RUN — ไม่ได้เขียนอะไรลง DB` ล่างสุด
ถ้าขึ้น `ERROR: ต่อ DB "…" แต่ EXPECTED_DB_NAME="bestchoice"` = ต่อผิดฐาน หยุด

**อ่านให้ครบทุกข้อ** (CLI พิมพ์ตามลำดับนี้):

1. **จะลบ** — จำนวนต่อตาราง + สินค้าแยกสาขา/หมวด/สถานะ + ต้นทุนรวมที่จะหายจากภาพรวมคลัง
   (คาด ~604 เครื่อง TTFY — ถ้าตัวเลขต่างจากที่นับได้จริงมาก อย่ารันต่อ)
2. **เครื่องที่ข้าม (ติดด่านถือครอง)** — เครื่องที่สัญญา/ใบจอง/ออเดอร์ทดสอบถืออยู่ พร้อมเหตุผลรายเครื่อง
   → เคลียร์ผ่านเมนูที่มีจริง: ยกเลิกใบขาย (`/sales` ปุ่มยกเลิก) · ยกเลิกสัญญา
   (`/finance/contract-cancellation`) · ยึดเครื่อง (`/repossessions`) แล้ว dry-run ใหม่
   **หรือ**ยอมรับให้ค้างแล้วรัน CLI ซ้ำภายหลัง (idempotent — รอบสองไม่มีเป้าหมายเหลือ = no-op)
3. **เครื่องสถานะผูกธุรกรรม** (`SOLD_*`/`RESERVED`/`REPOSSESSED`) — ไม่ถูกแตะ ไม่นับในภาพรวมคลัง
   อยู่แล้ว ให้ตัดสินเป็นราย ๆ ด้วยเมนูเดียวกับข้อ 2
4. **PO ที่เก็บไว้** เพราะข้อ 2-3 (หรือเครื่องทดสอบ) ยังชี้อยู่ — ใบพวกนี้จะยังโผล่ที่ `/purchase-orders`
5. **⚠️ เครื่องทดสอบที่ยังเปิดขายออนไลน์** — รายการ IMEI/ชื่อ → จดไว้ทำขั้น ④
6. **⚠️ เครื่องเทิร์นที่จะถูกล้าง** — แถว `trade_ins` และ JE เทิร์นที่เคยโพสต์**ไม่ถูกแตะ**
   (เป็นงานของการล้างสมุดทดสอบ ไม่ใช่ของคลัง)
7. **GL สินค้าคงเหลือ** `S11-2001/2002/2003` — จดตัวเลขไว้เทียบในขั้น ⑥ (ต้องไม่ขยับ)
8. **ใบโอนที่ยังเปิดอยู่** — CLI นับใบโอนรวมในข้อ 1 แต่**ไม่แยกว่าใบไหนยังเปิด** ต้องดูเอง:

   ```sql
   -- ใบโอนจริงที่ยังเปิด — จะถูกล้างทั้งใบ ไม่ว่าเครื่องในใบจะรอดหรือไม่
   SELECT t.batch_number, t.status, p.imei_serial, p.name, p.status AS product_status
   FROM stock_transfers t
   JOIN products p ON p.id = t.product_id
   WHERE t.deleted_at IS NULL
     AND t.status IN ('PENDING', 'IN_TRANSIT')
     AND (t.notes IS NULL OR t.notes NOT LIKE '[ทดสอบระบบ]%');
   ```

   มีแถว → ไปที่ `/stock/transfers` (กรอง "รอจัดส่ง" / "ระหว่างโอนสินค้า") **ปิดให้จบก่อนล้าง**:
   ของถึงแล้วให้รับที่ปลายทาง (`/stock/transfers?view=incoming`) · ยังไม่ส่งให้ "ปฏิเสธ" ·
   เครื่องจริงที่จะถูกล้างอยู่แล้วปล่อยได้ (ใบกับเครื่องหายพร้อมกัน) — **แต่เครื่องทดสอบ / เครื่องที่ข้าม
   ในข้อ 2 ห้ามค้างในใบโอน** เพราะใบจะหายแต่เครื่องยังอยู่ที่สาขาต้นทางในระบบ ทั้งที่ตัวเครื่องอยู่ปลายทาง

### ④ ปิดเครื่องทดสอบที่ยังเปิดขายออนไลน์

ตามรายการข้อ 5 ของ dry-run: ค้น IMEI ที่ `/stock/products` → เปิดหน้าเครื่อง (`/products/<id>`) →
การ์ด **"แสดงบนเว็บ shop"** → ปิดสวิตช์ ทีละเครื่อง (หน้ารายการมีปุ่ม "เปิดแสดงบนเว็บ" แบบกลุ่ม
แต่**ไม่มีปุ่มปิดแบบกลุ่ม** — ต้องเข้าทีละเครื่อง) · CLI ไม่แก้ค่านี้ให้ และรั้วไม่ครอบเว็บช็อป
⇒ ถ้าไม่ปิด ลูกค้าจริงหน้าเว็บจะเห็นและสั่งเครื่องทดสอบได้

### ⑤ รันจริง

แนะนำรัน**ก่อนร้านเปิด** — ไม่ต้องหยุด API (soft-delete ใน transaction เดียว ไม่ถอด FK ต่างจาก
factory reset) แต่ถ้ามีใบขาย commit ชนกันพอดี CLI จะพิมพ์ `FATAL:` แล้วออกโดย**ไม่เขียนอะไรเลย**
— รันซ้ำได้ทันที

```bash
CONFIRM_WIPE_STOCK_GO_LIVE=YES_I_AM_SURE \
ALLOW_PROD_WIPE_STOCK_GO_LIVE=YES_I_AM_SURE \
NODE_ENV=production \
EXPECTED_DB_NAME=bestchoice \
npm --prefix apps/api run wipe:stock-go-live 2>&1 | tee "stock-go-live-$(date +%F).log"
```

> **ทำไมต้องพิมพ์ `NODE_ENV=production` เอง:** ด่าน `ALLOW_PROD_WIPE_STOCK_GO_LIVE` ทำงาน
> **เฉพาะเมื่อ CLI เห็น `NODE_ENV=production` จริง** — shell ที่ต่อ prod ผ่าน proxy ไม่ได้ตั้งค่านี้ให้
> ถ้าลืม ด่านนี้จะ**เงียบไปเลย** (ไม่ error ไม่เตือน) เหลือแค่ด่านชื่อ DB ด่านเดียว
> ดังนั้นสั่งเสมอทั้งสามตัว: consent + prod-consent + `NODE_ENV=production`

**เก็บ output ทั้งก้อน** (ไฟล์ `.log` จาก `tee`) — สองบรรทัดสำคัญคือ
`AuditLog: action=STOCK_GO_LIVE_WIPE entityId=<wipedAt>` และชุด SQL ย้อนกลับ 12 บรรทัดท้ายสุด

ล้างทีละสาขา: เติม `ONLY_BRANCH_ID=<uuid>` (CLI ตรวจว่ามีสาขานั้นจริง) — ข้อจำกัด PO/ใบรับของ
ดูหัวข้อ "CLI ทำอะไร / ไม่ทำอะไร"

### ⑥ ตรวจรับ

- `/stock` ภาพรวมคลัง = 0 ทุกสาขา (เหลือเฉพาะเครื่อง `TEST-` / `ทดสอบระบบ`)
- `/purchase-orders/qc` คิว QC/ถ่ายรูป = 0 · `/purchase-orders` เปิดอยู่เฉพาะ `TEST-PO-*`
  (+ PO ที่ CLI รายงานว่าเก็บไว้ในขั้น ③ ข้อ 4)
- `/stock/alerts` ว่าง · `/stock/transfers` กรอง "รอจัดส่ง" / "ระหว่างโอนสินค้า" ไม่มีใบจริงค้าง
- **เครื่องที่รอดต้องไม่มีใบโอนค้างที่หายไป** — ตรวจด้วย SQL (ต้องได้ 0 แถว):

  ```sql
  -- เครื่องที่ยังอยู่ แต่ใบโอนที่ยังเปิดของมันถูกล้างในรอบนี้
  SELECT p.imei_serial, p.name, p.branch_id, t.batch_number, t.status
  FROM stock_transfers t
  JOIN products p ON p.id = t.product_id
  WHERE t.deleted_at = '<wipedAt>'
    AND t.status IN ('PENDING', 'IN_TRANSIT')
    AND p.deleted_at IS NULL;
  ```

  มีแถว = เครื่องนั้นในระบบยังอยู่สาขาต้นทาง (`branch_id` ไม่ย้ายจนกว่าจะรับที่ปลายทาง) แต่ไม่มีใบ
  ให้รับแล้ว → คืนใบโอน**เฉพาะแถวนั้น**: `UPDATE stock_transfers SET deleted_at = NULL WHERE id = '<id>';`
  แล้วรับตามปกติที่ `/stock/transfers?view=incoming` (หรือปฏิเสธถ้าเครื่องยังอยู่ต้นทางจริง)
- งบทดลอง SHOP (`/shop/accounting`) — `S11-2001/2002/2003` **เท่ากับ**ที่ dry-run พิมพ์ในขั้น ③ ข้อ 7
  (CLI ไม่แตะบัญชี ถ้าขยับ = มีคนโพสต์ JE ระหว่างนั้น ไม่ใช่ CLI)
- จำนวนที่ล้าง = จำนวนที่ CLI รายงาน (บน Windows ต้องวาง option **ก่อน** URL — ดูกล่องในขั้น ⑧):

  ```bash
  psql -A -t -c "SELECT count(*) FROM products WHERE deleted_at = '<wipedAt>';" "$DATABASE_URL"
  ```

- AuditLog มีแถว: `SELECT entity_id, created_at FROM audit_logs WHERE action = 'STOCK_GO_LIVE_WIPE';`

### ⑦ เริ่มใช้จริง

1. ซัพพลายเออร์ที่ `/suppliers` (ตัวเดิมที่คีย์ไว้ยังอยู่ครบ — D4)
2. PO ที่ `/purchase-orders` — หรือปุ่ม **"รับเข้าตรง (supplier)"** เมื่อของมาก่อนใบ
3. รับของ ใส่ **IMEI จริงทุกเครื่อง** (IMEI คือตัวตัดสินว่าเป็นของจริง — ห้ามขึ้นต้น `TEST-`)
4. มือสองเข้า `PHOTO_PENDING` → ถ่ายรูป 6 มุมที่ `/purchase-orders/qc` จึงเข้าคลัง ·
   มือหนึ่ง + อุปกรณ์เสริมเข้า `IN_STOCK` ทันที
5. ราคา: ระบบพยายามเติมจากตารางราคากลางตอนรับของ (เป็น "ของประดับ" — เติมไม่ได้ก็รับเข้าต่อได้
   ไม่มีราคาตั้งต้นให้กรอกทีหลัง) ⇒ ตั้งราคาให้ครบก่อนขาย และให้ผู้มีสิทธิ์เป็นคนตั้ง
   (พนักงานขายตั้งราคาเองไม่ได้)
6. พิมพ์สติกเกอร์ (`/stickers`)
7. ขาย — รั้วทำงานเงียบ ๆ: ของจริง↔ลูกค้าจริงผ่านปกติ

**บัญชี (รู้ไว้ ไม่ต้องทำอะไรในรอบนี้):** รับของ**ไม่โพสต์ JE** ⇒ เริ่มใช้จัดซื้อไม่กระทบสมุด และ
`S11-200x` ไม่ขยับตามการซื้อ · ขายจริง (ขายสด/เปิดสัญญา) **เครดิต** `S11-200x` ⇒ GL สินค้าคงเหลือ
**ติดลบสะสม**จนกว่าจะลง JE ยอดยกมาตาม spec `2026-08-24-shop-opening-balance-design.md`
(สภาพเดียวกับหลัง factory reset — ไม่ใช่ของใหม่) · "JE ตอนรับของ" เป็นฟีเจอร์แยก รอผู้สอบเคาะบัญชี/
จังหวะ — **ห้ามเดา JE**

### ⑧ ย้อนกลับ (ถ้าจำเป็น)

รัน SQL ชุดที่ CLI พิมพ์ตอนจบ — 12 บรรทัด หนึ่งบรรทัดต่อตาราง รูป
`UPDATE "<table>" SET deleted_at = NULL WHERE deleted_at = '<wipedAt>';` (ลำดับ: `products` →
`purchase_orders` → `po_items` → `goods_receivings` → `goods_receiving_items` → `stock_transfers` →
`branch_receivings` → `branch_receiving_items` → `stock_counts` → `stock_count_items` →
`stock_adjustments` → `stock_alerts`)

> 🪟 **บน Windows `psql` เมิน option ที่วางหลัง URL โดยไม่บอก** — `psql "$URL" -c "…"` จะ
> "สำเร็จ" โดยไม่รันอะไรเลย (ตรวจพบบนเครื่องนี้ `C:\Users\<you>\pgsql\bin` ระหว่างทำ Task 3)
> ซึ่งเป็นความล้มเหลวที่แย่ที่สุดสำหรับคำสั่งย้อนกลับ ⇒ **วาง option ก่อน URL เสมอ**:
>
> ```bash
> psql -A -t -c "UPDATE \"products\" SET deleted_at = NULL WHERE deleted_at = '<wipedAt>';" "$DATABASE_URL"
> # …ทำซ้ำอีก 11 บรรทัดตามที่ CLI พิมพ์ แล้วตรวจว่ากลับมาจริง:
> psql -A -t -c "SELECT count(*) FROM products WHERE deleted_at = '<wipedAt>';" "$DATABASE_URL"   # ต้องได้ 0
> ```

ค่า `<wipedAt>` อยู่ในไฟล์ `.log` ของขั้น ⑤ — ถ้า output หาย ดึงจาก AuditLog:

```sql
SELECT entity_id AS wiped_at, created_at
FROM audit_logs
WHERE action = 'STOCK_GO_LIVE_WIPE'
ORDER BY created_at DESC;
```

**ข้อจำกัดของการย้อนกลับ (ตรวจแล้ว):**
- ย้อนกลับได้สะอาดเฉพาะ**ก่อน**รับของจริงเข้ามา — IMEI ถูกกันซ้ำด้วย partial unique index
  (`products_imei_serial_active_unique` เฉพาะแถวที่ `deleted_at IS NULL`) ถ้ารับเครื่อง IMEI เดิม
  เข้ามาใหม่แล้ว บรรทัด `products` จะ**ล้มทั้งบรรทัด** (ไม่คืนสักเครื่อง) — ต้องตัดสินเป็นราย ๆ ก่อน
- AuditLog แถว `STOCK_GO_LIVE_WIPE` ไม่ถูกลบ (เป็นหลักฐานว่าเคยล้างและเคยคืน)
- ย้อนกลับแล้ว `factory:reset` กลับมาปฏิเสธเหมือนเดิม (มีของจริงอีกครั้ง)

---

## ถ้าล้างผิด

1. หยุดรับของ/ขายทันที (ยิ่งเขียนข้อมูลใหม่ ยิ่งย้อนยาก — ดูข้อจำกัด IMEI ข้างบน)
2. **ใช้ขั้น ⑧ ก่อนเสมอ** — คืนได้ตรงจุด ไม่กระทบข้อมูลอื่น
3. กู้ทั้งฐานจาก backup ขั้น ① (`gcloud sql backups restore <BACKUP_ID> --restore-instance=bestchoice-db`)
   **เฉพาะ**เมื่อขั้น ⑧ ใช้ไม่ได้ — ไม่มี PITR จึงเสียทุกอย่างที่เขียนหลัง backup (แชท สัญญา ใบเสร็จ)
