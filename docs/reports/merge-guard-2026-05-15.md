# Pre-Merge Guard Report — 2026-05-15

Generated by: Pre-Merge Guard agent  
Date: 2026-05-15  
Branches reviewed: 3 (most recently updated with shared merge-base against `origin/main`)

---

## Summary

| Branch | Files Changed | Commits | Recommendation |
|--------|--------------|---------|----------------|
| `chore/other-income-v2-1-t4-renumber-validation` | 1 | 1 | ✅ APPROVE |
| `hotfix/expense-form-v4-approvers-shape` | 1 | 1 | ✅ APPROVE |
| `hotfix/expense-form-v4-modal-scroll` | 2 | 2 | ✅ APPROVE |

All three branches are clean. No critical issues found.

---

## Branch 1: `chore/other-income-v2-1-t4-renumber-validation`

**Author**: Akenarin Kongdach  
**Date**: 2026-05-12  
**Commit**: `fc2ec9eb` — `docs(other-income): renumber validation rules to PDF Spec V1-V14 (T4)`

### Files Changed
- `apps/api/src/modules/other-income/services/validation.service.ts` (+68 / -41)

### Summary
Pure refactor of validation rule ordering in `ValidationService.validate()` to align with the accountant's PDF Spec v1.0 (`docs/superpowers/specs/2026-05-12-other-income-v2-1-pdf-gap-fixes-design.md`). Key changes:
- Added comprehensive JSDoc block mapping V1–V15 rules to spec
- Reordered rules from: `V3→V4→V7→V15→V6→V8→V10/V12→V13/V14→V11`  
  to: `V3→V4→V6→V7→V8→V9(comment)→V11→V10/V12→V13/V14→V15`
- Added `V11` attachment-threshold check (was previously at the end, now in spec order)
- Added `V9` comment explaining why it's enforced in `OtherIncomeService.approve()` not here

### Security Issues (Critical)
None.

### Code Issues (Warning)
None. All errors accumulate into an array — rule reordering has no functional effect on the output. Decimal methods used correctly throughout (`.gt()`, `.gte()`, `.eq()`, `.abs()`, `.minus()`). No raw `Number()` on money values.

### Info
- The large JSDoc block is intentional and documents spec alignment — acceptable.
- V11 moving from position 9 to position 5 is purely cosmetic (all rules always evaluated).

### Recommendation: ✅ APPROVE

---

## Branch 2: `hotfix/expense-form-v4-approvers-shape`

**Author**: Akenarin Kongdach  
**Date**: 2026-05-11  
**Commit**: `362937fb` — `fix(expense-form-v4): ApproverSection paginated /users response → 'r?.map is not a function'`

### Files Changed
- `apps/web/src/components/expense-form-v4/ApproverSection.tsx` (+9 / -1)

### Summary
Fixes a runtime crash in `ApproverSection` where the `/users` endpoint returns a paginated response `{ data, total, page, limit }` but the old code treated it as a bare array, causing `r?.map is not a function`. The fix:
- Changes endpoint to `/users?limit=200` (no unsupported `?roles=` param)
- Unwraps paginated response: `res.data?.data ?? (Array.isArray(res.data) ? res.data : [])`
- Filters by role client-side via `APPROVER_ROLES = ['OWNER', 'FINANCE_MANAGER', 'ACCOUNTANT']`

### Security Issues (Critical)
None. Uses `api.get()` correctly, role filtering is a display filter (not an access control boundary — server-side guards enforce actual permissions).

### Code Issues (Warning)
- **Hardcoded `limit=200`** (Info): Silently misses approvers beyond 200 users. For a typical multi-branch phone shop with < 50 staff this is safe, but worth a server-side `?roles=` filter when the users module is extended.

### Info
- The defensive pattern `res.data?.data ?? (Array.isArray(res.data) ? res.data : [])` correctly handles both legacy flat-array and current paginated responses.

### Recommendation: ✅ APPROVE

---

## Branch 3: `hotfix/expense-form-v4-modal-scroll`

**Author**: Akenarin Kongdach  
**Date**: 2026-05-11  
**Commits**: 2 (includes hotfix/expense-form-v4-approvers-shape + modal scroll fix)

### Files Changed
- `apps/web/src/components/expense-form-v4/ApproverSection.tsx` (+9 / -1) — same as Branch 2
- `apps/web/src/components/expense-form-v4/ExpenseFormV4.tsx` (+6 / -6)

### Summary
Two-commit branch that fixes both the approvers shape bug (Branch 2) plus a sticky header/footer scroll regression. The modal previously used `overflow-y-auto` on the outer overlay with `sticky` header/footer — this breaks when the browser doesn't treat the modal container as the scroll parent. Fix replaces with a standard flex-column layout:
- Outer wrapper: `items-center justify-center p-4` (centered, no scrolling on overlay)
- Modal inner: `max-h-[95vh] flex flex-col` (height-capped flex column)
- Header: `flex-none` (removed `sticky top-0`)
- Content: `flex-1 overflow-y-auto` (scroll isolated to content area)
- Footer: `flex-none` (removed `sticky bottom-0`)

### Security Issues (Critical)
None.

### Code Issues (Warning)
None. All Tailwind classes use design tokens (`bg-background`, `border-b`, etc.) — no hardcoded hex colors. Removed `backdrop-blur-sm` from header/footer which is acceptable since the header is no longer floating over scrolled content.

### Info
- This branch is a superset of Branch 2 (`hotfix/expense-form-v4-approvers-shape`). If both are pending, only this branch needs to merge.
- The `flex-column + overflow-y-auto on inner content` pattern is the correct approach for tall modals.

### Recommendation: ✅ APPROVE

---

## Scan Notes

Several other unmerged branches exist but were excluded from this run:
- **No shared merge-base with `origin/main`**: `feat/accounting-expense-fixes`, `phase-a5c-finance-deferred`, and most `feat/` branches appear to be on disconnected histories (possibly feature branches rebased on a diverged base) — `git diff origin/main...` returned no output for these, and `git merge-base` returned no common ancestor. They require manual investigation.
- **Dependency bumps** (`chore/deps-tier3-chunk*`): Excluded — not application code.
- **Guard/watchdog reports** (`guard/review-*`, `watchdog/report-*`): Excluded — generated reports, not source changes.
- **Claude branches** (`claude/*`): Excluded — exploratory/research branches without production code changes.
