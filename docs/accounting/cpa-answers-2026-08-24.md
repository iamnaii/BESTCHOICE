# คำตอบจากผู้สอบบัญชี — 24 สิงหาคม 2569

บันทึกคำวินิจฉัยของผู้สอบบัญชี 13 ข้อ พร้อมผลกระทบต่อระบบที่ **ตรวจสอบกับโค้ดจริงแล้ว**

| | |
|---|---|
| วันที่ตอบ | 2026-08-24 |
| เอกสารคำถาม | `docs/accounting/cpa-questions-2026-08-24.html` (13 ข้อ 3 กลุ่ม) |
| ผู้ถาม | BESTCHOICE (SHOP + FINANCE) |
| สถานะโค้ด | **A1+B4 และ C1 แก้แล้ว** (commit `c29864278`) · อีก 11 ข้อยังไม่ลงมือ · **ยังไม่ได้รันอะไรบน prod** |

> **ทำไมต้องมีไฟล์นี้:** ก่อนหน้านี้เอกสารคำถามอยู่แค่ในโฟลเดอร์ชั่วคราวของเครื่อง ⇒ คำวินิจฉัย
> ที่เปลี่ยนวิธีลงบัญชีจริงไม่มีหลักฐานในระบบเลย ตรวจย้อนหลังไม่ได้ ต่อไปคำถาม–คำตอบทุกรอบ
> ให้ commit ไว้ที่ `docs/accounting/` เสมอ

---

## สรุป 13 ข้อ

| ข้อ | คำตอบ | แปลว่า | สถานะ |
|---|---|---|---|
| **A1** | ต้องมี — ตั้งรหัส **S21-1104 เจ้าหนี้ FINANCE** | เปิดบัญชีเจ้าหนี้ระหว่างกิจการฝั่ง SHOP | ⚙️ ต้องแก้โค้ด |
| **A2** | **ต้องตั้ง** | ตั้งยอดยกมา SHOP ย้อนหลัง (ก่อน 23 มิ.ย. 69) | ⚙️ + ❓ |
| **A3** | ทำคู่มือ / ทำให้บันทึกค่าเริ่มต้นเองได้ | ผู้สอบไม่ให้ตัวเลข — เป็นงาน**ระบบ** ไม่ใช่งานบัญชี | ⚙️ ต้องแก้โค้ด |
| **A4** | ปล่อยไว้ เป็นการทดสอบระบบ | ไม่ออกใบลดหนี้ย้อนหลัง | ✅ ไม่ต้องแก้ |
| **B1** | ถูกต้อง | วิธีสุทธิ (NET) ตอนเปลี่ยนเครื่อง | ✅ ไม่ต้องแก้ |
| **B2** | ถูกต้อง | ลงสต็อกมือสองที่ราคารับซื้อ | ✅ วิธีถูก / **รหัสบัญชีเปลี่ยนตาม B4** |
| **B3** | รับได้ | หักกลบรอบจ่ายเหลือโอนสุทธิ | ✅ ไม่ต้องแก้ |
| **B4** | **ผิด** — ใช้ S21-1104 เจ้าหนี้ FINANCE | `S21-3001` ที่เปิดเมื่อ 19 ส.ค. **ต้องเปลี่ยน** | ⚙️ + ❓ |
| **C1** | ต้องตั้ง เพราะเป็นรายได้หน้าร้าน **S41-1201** | SHOP ต้องตั้งค่าคอมให้ตรง FINANCE | ⚙️ + ❓ |
| **C2** | ปิดสัญญาก่อน → ชำระล่วงหน้าลดยอดค้าง + **บันทึกเป็นรายได้** | เงินพักที่เหลือรับรู้เป็นรายได้ | ⚙️ + ❓ ยังไม่ระบุบัญชี |
| **C3** | ปัจจุบันถูกแล้ว | เงินพักลดฐานส่วนลดปิดยอด | ✅ ไม่ต้องแก้ |
| **C4** | แก้ไปข้างหน้าอย่างเดียว | ใบขายจากใบจอง — ไม่บันทึกย้อนหลัง | ⚙️ + ❓ |
| **C5** | **ต้องแยกบันทึก** เพราะเป็นค่าใช้จ่าย SHOP | ค่าใช้จ่ายต้องอยู่ในสมุด SHOP | ⚙️ + ❓ ตีความได้ 2 ทาง |

**✅ ยืนยันไม่ต้องแก้ 5 ข้อ · ⚙️ ต้องแก้ 8 ข้อ · ❓ ต้องถามผู้สอบเพิ่ม 6 ข้อ**

---

## A1 + B4 + C5 — เป็นเรื่องเดียวกัน: บัญชี S21-1104

ผู้สอบตอบสามข้อนี้ด้วยรหัสบัญชีเดียว จึงต้องทำพร้อมกัน

**หลักการเบื้องหลัง:** เราตั้งบัญชีตาม **วัตถุประสงค์** (`S21-3001` = "เจ้าหนี้ค่าเครื่องรับคืน")
ผู้สอบให้ตั้งตาม **คู่สัญญา** (`S21-1104` = "เจ้าหนี้ FINANCE" เฉย ๆ) ซึ่งเป็นหลักผังบัญชีมาตรฐาน —
เจ้าหนี้จัดกลุ่มตาม **"ติดหนี้ใคร"** ไม่ใช่ **"ติดหนี้เพราะอะไร"** (เหตุผลอยู่ในคำอธิบายรายการ)

นี่คือเหตุผลที่ C5 (จ่าย ปกส./ภาษีหัก ณ ที่จ่าย แทนกัน) ปลดล็อกทันทีเมื่อเปิดบัญชีนี้ — บัญชีตาม
คู่สัญญารองรับหนี้ระหว่างกันได้ **ทุกประเภท** ส่วนบัญชีตามวัตถุประสงค์ต้องเปิดใหม่ทุกเรื่อง

### ตรวจแล้ว: รหัส S21-1104 ว่างจริง และเข้ากลุ่มได้พอดี

| | FINANCE | SHOP |
|---|---|---|
| 21-1101 | เจ้าหนี้-หน้าร้าน | เจ้าหนี้ - ซัพพลายเออร์มือถือ |
| 21-1102 | เจ้าหนี้ค่าคอม-หน้าร้าน | เจ้าหนี้ - อุปกรณ์เสริม |
| 21-1103 | เงินรับล่วงหน้า | เจ้าหนี้ค่าใช้จ่ายสาขา |
| 21-1104 | เจ้าหนี้ค่าใช้จ่ายกิจการ | **← ว่าง (S21-1104 ใหม่)** |

กลุ่ม `S21-11XX` ของสองฝั่ง **ไม่ได้ mirror กันอยู่แล้ว** (S21-1101 คนละความหมายกับ 21-1101)
⇒ เลข 1104 ซ้ำคนละฝั่ง **ไม่เป็นปัญหา** เพราะตัวอักษร `S` เป็นตัวแบ่งพาร์ทิชัน
และยังสมมาตรด้วย: FINANCE มี "เจ้าหนี้-หน้าร้าน" ฝั่ง SHOP มี "เจ้าหนี้ FINANCE" เป็นคู่กระจกกัน

### ขนาดงานจริงของ B4

`S21-3001` **ไม่ได้อยู่ในค่าคงที่กลาง** — เป็น string เปล่ากระจาย **17 จุดในโค้ดโปรดักชัน 7 ไฟล์**
+ 9 ไฟล์เทส + CSV + เอกสาร (ตรวจแล้ว `shop-account-resolver.service.ts` และ `account-role.service.ts`
ไม่มี `S21` เลยแม้แต่ตัวเดียว)

| จุด | ไฟล์ |
|---|---|
| ขา Cr ของ A.4 (ผู้สร้างรายการ) | `shop-exchange-return.template.ts:103` |
| `C2_REDIRECTS` + `TYPED_LENS_ACCOUNTS` | `contract-cancellation.template.ts:104,105,123,285` |
| ตัวรับ redirect ฝั่ง exchange | `contract-exchange-cancel.service.ts:365` |
| typed-balance helper ×2 | `interco-typed-balance.ts:84,139` |
| raw SQL เลนส์คิวจ่าย ×3 | `interco-pending.service.ts:226,373,531` |
| raw SQL รายงานอายุ ×4 | `interco-aging.service.ts:546,849,856,884` |
| guard + recall-cash + หักกลบ ×3 | `interco-settlement.service.ts:282,1512,1669` |

**กับดัก:** `redirectedTotals` ถูก key ด้วย **บัญชีปลายทาง** (`exchange-cancel-reversal.template.ts:146`)
⇒ ถ้าแก้ `C2_REDIRECTS` แต่ลืมแก้จุดที่อ่าน `redirectedTotals['S21-3001']` ค่าจะกลายเป็น **0 เงียบ ๆ**
แล้วไปตกด่าน cross-check สองสมุด ⇒ **การยกเลิกสัญญาแบบ C-2 จะ rollback ทุกใบ** ต้องแก้เป็นชุดเดียว

### ต้องถามผู้สอบก่อนลงมือ

1. **ย้ายรายการเก่าหรือไม่?** `journal_lines.account_code` เป็น text ไม่มี FK ⇒ UPDATE ได้
   มีแบบอย่างทั้งสองทาง: `2026-05-11-reclassify-sso-21-1104-to-21-3105.sql` (ย้าย) กับ A.4 เอง
   ตอนเปลี่ยนมาเป็น S21-3001 (ไม่ย้าย ไปข้างหน้าอย่างเดียว)
   **การย้าย = เขียนทับสมุดงวดที่ปิดแล้ว → เป็นคำตัดสินของผู้สอบ ไม่ใช่ของทีมพัฒนา**
2. ~~**ตอนนี้ prod มีรายการใน S21-3001 กี่บรรทัด?**~~ — **ตอบแล้ว 2026-08-24 จาก preflight บน prod
   (`shop-preflight-20260824.txt` §1.3): `S21-1104` = 0 และ `S21-3001` = 0 — prod ไม่มีทั้งสองรหัส**
   ⇒ **B4 ไม่ต้อง migrate ข้อมูลเลย** เป็นการแก้โค้ด+CSV ล้วน · SQL ปลดระวางจะเข้าสาขา
   `n_old_total = 0` แล้วรายงานว่า "ไม่ต้องทำอะไร" · เหลือแค่ **deploy → `seed:coa`**
   > ⚠️ **อย่าอ่านบรรทัดสรุปของ preflight ผิด** — มันพิมพ์ว่า *"ไม่มีทั้งคู่ — ผัง SHOP ยังไม่ถูก seed"*
   > แต่บัญชี SHOP อื่น ๆ (S11-xxxx / S41-xxxx / S50-xxxx) **มีครบ** ⇒ ผัง SHOP **ถูก seed แล้ว**
   > เพียงแต่เป็นเวอร์ชันก่อน 2026-08-19 · **ห้ามไป reseed แบบล้างของเก่า**
3. **กลุ่ม `S21-30XX` ที่จะว่างลง** — ยุบทิ้ง หรือเก็บไว้รอขาคู่ฝั่ง SHOP ของ 11-2107 (interco spec §11)
4. **S21-1104 รับหนี้ระหว่างกันทุกประเภทเลยไหม** (เครื่องรับคืน + เรียกคืน + ปกส./ภาษีจ่ายแทน)
   ถ้าใช่ ต้องมีวิธีแยกประเภทใน `metadata` ไม่งั้นรายงานอายุหนี้แยกสาเหตุไม่ได้
5. **ชื่อบัญชีเป๊ะ ๆ** — "เจ้าหนี้ FINANCE" ตามที่เขียนมา หรือ "เจ้าหนี้ - FINANCE" ให้เข้ารูปแบบเพื่อนบ้าน
6. **C5 ตีความได้ 2 ทาง**
   - (ก) ห้ามจ่ายรวม ต้องแยกจ่ายคนละบัญชีธนาคาร
   - (ข) จ่ายรวมได้ แต่ค่าใช้จ่ายต้องลงสมุด SHOP โดยมี S21-1104 เป็นสะพาน

   **เราอ่านว่า (ข)** เพราะเขียนว่า "แยก**บันทึก**" ไม่ใช่ "แยก**จ่าย**" และเหตุผลที่ให้
   ("เพราะเป็นค่าใช้จ่าย SHOP") เป็นเรื่อง**การจัดประเภท** ไม่ใช่เรื่องการเงิน — แต่ต้องยืนยันเป็น
   ลายลักษณ์อักษร เพราะถ้าเป็น (ก) แปลว่า **ไม่ต้องแก้อะไรเลยฝั่งเงินเดือน**

---

## C1 — ค่าคอมต้องขึ้นสองสมุด

**คำตอบ:** *"ทำไมต้องตั้ง เพราะเป็นรายได้ หน้าร้าน S41-1201 รายได้ - ค่าคอมจาก FINANCE"*
⇒ เลือกทางเลือก (ก) ให้ SHOP ตั้งให้ตรงกับ FINANCE (ไม่ใช่ให้ FINANCE เลิกตั้ง)

### ยืนยันข้อบกพร่องแล้ว — และสูตร 10% ซ้ำกัน **6 ชุด** (สำรวจรอบแรกนับได้ 4)

| ฝั่ง | ไฟล์ | ทำอะไร |
|---|---|---|
| FINANCE | `contract-activation-1a.template.ts:47-50` | `storeCommission ?? financed × 0.10` |
| SHOP (เปิดสัญญาปกติ) | `contract-workflow.service.ts:570-572` | `storeCommission ? … : Decimal(0)` ❌ |
| SHOP (เปลี่ยนเครื่อง) | `contract-exchange.service.ts:837-839` | `storeCommission ? … : Decimal(0)` ❌ |
| แผนเปลี่ยนเครื่อง | `exchange-plan.util.ts:26` | `financedAmount.times('0.10')` |
| FINANCE (สัญญาใหม่จาก swap) | `exchange-new-contract-1a.template.ts:47` | เหมือน 1A ทุกบรรทัด (**พบทีหลัง**) |
| คำนวณตารางผ่อน | `compute-installment-breakdown.ts:64` | เหมือน 1A ทุกบรรทัด (**พบทีหลัง**) |

**✅ แก้แล้ว 2026-08-24:** ยุบเป็น helper เดียว **`resolveStoreCommission`**
(`apps/api/src/utils/store-commission.util.ts`) ใช้ร่วมกันทั้ง 6 จุด · ปักด้วยเทส 15 ตัวที่ util
+ 2 ตัวที่ `contract-workflow.service.spec.ts` (ไม่ระบุค่าคอม → SHOP ได้ 1800 ไม่ใช่ 0 · ระบุ 0 → คง 0)

> **กับดักที่สองที่เจอระหว่างแก้ — เกือบ "แก้" ของที่ถูกอยู่แล้ว:** สาขา legacy fallback ของ
> `approvePriced` เขียน `storeCommission: Decimal(0)` เมื่อสัญญาเดิมเป็น null ซึ่งดูเหมือนบั๊ก
> (บรรทัดถัดไปมีคอมเมนต์ว่า `vatAmount` ต้อง pass null through) แต่ **จงใจและ load-bearing**:
> สาขานั้น clone `monthlyPayment` แล้วถอดหลังหา `interestTotal` ⇒ `gross = financed + commission
> + interest` จะเท่ากับยอดที่ลูกค้าผ่อนจริง **ก็ต่อเมื่อ commission = 0** · ปล่อย fallback ทำงาน =
> ลูกหนี้เกินยอดผ่อนจริง 10% · ใส่คอมเมนต์กันคนรุ่นหลัง "จัดระเบียบ" ไว้แล้ว

> ⚠️ **กับดัก:** `newCommission` ที่ `contract-exchange.service.ts:837` ถูกใช้ **สองที่** — ส่งเข้า
> template ฝั่ง SHOP (`:896`) และเป็น fallback ราคารับซื้อ (`:842`
> `buyback = request.buybackPrice ?? newFinanced.plus(newCommission)`) ⇒ ถ้าแก้ ternary ตรง ๆ
> **ราคารับซื้อของ swap ที่ไม่ระบุค่าคอมจะเปลี่ยนจาก `financed` เป็น `financed × 1.10` เงียบ ๆ**
> แล้วไหลต่อไปที่ 11-2107 / S21-1104 และยอดหักกลบรอบจ่าย
> **ต้องแยกตัวแปรใหม่ ใช้เฉพาะขาที่ส่งเข้า template**

### ❓ คำถามสำคัญที่สุดของทั้งรอบ — รายการแก้ย้อนหลังหน้าตาอย่างไร

สัญญาที่ **จ่ายรอบจ่ายไปแล้ว**: FINANCE เครดิตธนาคารตัวเอง `ยอดจัด + ค่าคอม fallback`
แต่ SHOP เดบิตธนาคารแค่ `ยอดจัด` ⇒ **SHOP บันทึกต่ำไปทั้งเงินสดและรายได้**

⇒ รายการแก้ควรเป็น **`Dr S11-1201 ธนาคาร / Cr S41-1201`** (รับรู้รายได้คู่กับเงินที่รับจริง)
หรือ **`Dr S11-3002 ลูกหนี้ / Cr S41-1201`** (ตั้งลูกหนี้ที่ไม่มีวันได้รับ เพราะรับเงินไปแล้ว)?
**เปลี่ยนว่าสินทรัพย์ตัวไหนถูกแสดง — ทีมพัฒนาตัดสินเองไม่ได้**

คำถามรอง: ลงงวดไหน · สูตร fallback ควรอ่านจาก config (`store_commission_pct`) แทน hardcode 10% ไหม ·
หรือควรเลิกมี fallback แล้ว **บังคับกรอกค่าคอมตอนเปิดสัญญา** ไปเลย (ผู้สอบตอบว่า "SHOP ต้องตั้ง"
ไม่ได้ตอบว่า "10% เป็นตัวเลขที่ถูก" — ตอนนี้สองสมุดจะตรงกันที่ตัวเลขที่**ไม่มีใครเลือก**)

---

## C2 — เงินพักปรับดิวที่เหลือตอนปิดสัญญาก่อนกำหนด

**คำตอบ:** *"กรณีปิดสัญญาก่อน นำมาคำนวนเป็นชำระล่วงหน้า เพื่อลดยอดค้างชำระ และบันทึกทางบัญชี
เป็นรายได้ (เหมือนชำระค่างวดล่วงหน้า)"*

**ครึ่งแรกทำอยู่แล้ว:** `computePayoffQuote` นับเงินพักเป็นเครดิตลูกค้า ลดยอดที่ต้องจ่ายจริง
และมีขา `Dr 21-1103` (parkRelief) ทั้งใน JP4 และ JP5 → ตรงกับ "ชำระล่วงหน้าเพื่อลดยอดค้าง"

**ครึ่งหลังยังไม่มี:** ส่วนที่ **เหลือ** หลังปิดสัญญา (ตัวอย่างคำนวณ: พัก 354 ดูดซับ 188.58
⇒ **เหลือ 165.42**) ยังค้างใน 21-1103 ไม่มีรายการรับรู้รายได้ ⇒ ต้องเพิ่มรายการกวาดเข้ารายได้ตอนปิดสัญญา

> ❓ **ผู้สอบต้องระบุบัญชีรายได้** — ตอนนี้ **ไม่มีบัญชีค่าธรรมเนียมปรับดิวในผัง**
> ตัวเลือกที่มีจริง: `41-1101` รายได้ดอกเบี้ย (VAT ใช่) · `42-1103` ค่าปรับชำระล่าช้า (VAT ไม่ ชื่อคนละเรื่อง)
> หรือ **เปิดบัญชี 42-11XX ใหม่**
>
> คำถามพ่วง: ยอดนี้เสีย VAT ไหม (ตอนรับเงินเข้ามาไม่มีขา VAT) · ครอบคลุมกรณี **ผ่อนครบงวด**
> ที่ยังเหลือเงินพักด้วยไหม (ผู้สอบตอบเฉพาะ "ปิดก่อน") · ครอบคลุมกรณี **ตัดหนี้สูญ** ไหม
> (เงินที่ลูกค้าจ่ายล่วงหน้าหักกลบหนี้ที่ตัดไปแล้วได้หรือไม่ = คำถามกฎหมาย)

**C3 สอดคล้องกับ C2:** ผู้สอบตอบ C3 ว่า "ปัจจุบันถูกแล้ว" ⇒ `rescheduleAdvanceApplied` คงสูตรเดิม
(ตรวจแล้วมีจริงและต่อครบทั้ง JP4/JP5/preview) แปลว่าส่วนที่เหลือ 165.42 **มีอยู่จริงโดยตั้งใจ**
และ C2 คือคำสั่งว่าจะทำอย่างไรกับมัน — สองคำตอบประกอบกันพอดี

---

## C4 — ใบขายจากใบจอง (แก้ไปข้างหน้า)

**ยืนยันข้อบกพร่องแล้ว และ *ใหญ่กว่า* ที่เขียนในคำถาม**

`bookings.service.ts:613` สร้างใบขายด้วย `tx.sale.create({...})` **ตรง ๆ** ไม่ผ่าน `sale-writer`
(คอมเมนต์ที่ `:572` บอกเองว่า "inline the SalesService.createCashSale invariants the original
`tx.sale.create` skipped" — เคยมีคนสังเกตแล้วยกมาบางส่วน **แต่ไม่ได้ยกขาลงบัญชีมาด้วย**)

**สิ่งที่คำถามไม่ได้บอกผู้สอบ:** โมดูลใบจอง **ไม่โพสต์รายการบัญชีเลยแม้แต่ใบเดียว** ⇒

- **เงินมัดจำที่รับจริงไม่เคยขึ้นสมุด** (ไม่ใช่แค่ใบขายที่หาย)
- `S21-2002 เงินรับล่วงหน้า-มัดจำสินค้า` มีในผังแต่ **ไม่มีผู้สร้างรายการที่ไหนเลย** — บัญชีตาย
- ใบจองที่ยกเลิก/หมดอายุแล้วคืนเงิน ก็ไม่มีรายการเงินออก

> ⚠️ **ข้อค้นพบที่ขัดสัญชาตญาณ:** ถ้าแก้ให้โพสต์รายการ ด่าน G8 (ห้ามยกเลิกใบขายที่มาจากใบจอง)
> จะ **จำเป็นมากขึ้น ไม่ใช่น้อยลง** — เพราะการยกเลิกจะกลับรายการเป็น `Cr เงินสด` ทั้งที่
> **ไม่มีเมนูคืนมัดจำอยู่จริง** ⇒ สมุดจะอ้างว่ามีเงินออกที่ไม่เคยออก
> จากเดิมที่เป็นแค่ "รายงานขาด" กลายเป็น "สมุดบอกเท็จ"

❓ ต้องถามต่อ: บันทึกมัดจำตอนรับ (ต้องมีบัญชีรายได้ริบมัดจำ ซึ่ง **ยังไม่มีในผัง**) หรือรอบันทึก
ทีเดียวตอนแปลงเป็นใบขาย · "ไปข้างหน้า" นับจากวันไหน · หมายเหตุประกอบงบเขียนที่ไหน

---

## A2 — ยอดยกมา SHOP (ต้องตั้ง)

ประชากรที่กระทบ = สัญญาที่ `legacyNoShop = true` (GL ฝั่ง SHOP `S11-3001 + S11-3002 = 0`)
คือสัญญาที่เปิดก่อน 2026-06-23 (commit `bbcfa7a3`) — **ตัดสินจาก GL ไม่ใช่จากวันที่** เพราะ
`Contract.createdAt` เป็นวันสร้างร่าง และ `updatedAt` ขยับทุกครั้งที่แก้อะไรก็ตาม

📄 คิวรีนับประชากร + ยอด: **`docs/accounting/shop-opening-balance-enumerate-2026-08.sql`** (อ่านอย่างเดียว)

> **อัปเดต 2026-08-24 (งานคู่ขนาน):** มีแผนตั้งยอดตั้งต้นสมุด SHOP ทั้งชุด **ตัดยอด 29 ส.ค. 2569** แล้ว
> — spec: `docs/superpowers/specs/2026-08-24-shop-opening-balance-design.md`
> ⇒ คำถามข้อ "รวมเงินดาวน์/สินค้าคงเหลือ/เจ้าหนี้ซัพพลายเออร์ไหม" **ตัดสินแล้ว: รวม** (นับของจริง)
> ⇒ ที่ **ยังบล็อกจริง** เหลือเฉพาะ **ส่วนระหว่างกิจการ** (`S11-3001`/`S11-3002`/`S21-1104`)
> ซึ่ง spec นั้น **จงใจตัดออก** ด้วยเหตุผลเชิงโครงสร้าง (เลนส์อ่านรายสัญญา ⇒ ยอดยกมาก้อนรวมที่ไม่มี
> เลขสัญญาจะกลายเป็น finding `ACCOUNT_DRIFT` ทุกเดือนตลอดไป) แล้วเขียนหมายเหตุประกอบงบแทน
>
> 🔴 **เส้นตาย:** บัญชี `S21-1104` + `S21-4101` ที่ยอดยกมาต้องใช้ อยู่บน branch
> `feat/cpa-answers-2026-08-24` **ยังไม่มีบน `main`** ⇒ ต้อง merge + deploy + `seed:coa`
> **ก่อน 27 ส.ค.** ไม่งั้น JE เช้าเสาร์ล้มด้วย `รหัสบัญชีไม่ถูกต้องหรือไม่อยู่ในผังบัญชี`

❓ ต้องถามต่อก่อนโพสต์:

1. **ลงเฉพาะงบดุล หรือรื้อ P&L ด้วย?** ถ้าลงแต่ลูกหนี้/สินค้าคู่กับส่วนของเจ้าของ P&L ของ SHOP
   จะเงียบเรื่องยอดขาย พ.ค.–มิ.ย. · ถ้าลงรายได้/ต้นทุนด้วย ผลประกอบการงวดก่อนจะไปโผล่ในงวดที่ลงรายการ
2. **คู่บัญชีเป็น S32-1101 (กำไรสะสม) หรือ S33-1101 (กำไรปีปัจจุบัน)?** ช่วงที่กระทบอยู่ **ในปีบัญชีปัจจุบัน**
   ⇒ S32-1101 ถูกก็ต่อเมื่อผู้สอบถือว่าช่วงก่อน 23 มิ.ย. เป็น "ก่อนเปิดสมุด"
3. **ค่าคอมใช้ตัวเลขไหน** — ตาม GL ฝั่ง FINANCE (สองสมุดตรงกัน) หรือตามฟิลด์ในสัญญา (คงช่องว่าง C1 ไว้)
4. **ลงวันที่อะไร** — วันเปิดสัญญาจริง (ต้องเปิดงวดที่ปิดแล้ว) / วันตัดยอดวันเดียว / วันที่โพสต์
5. **เงินดาวน์ของลูกค้ารวมด้วยไหม** — สัญญากลุ่มนี้ก็ไม่มีขา `Cr S21-2001` และไม่มีเงินสด SHOP รับดาวน์
   ⇒ เงินสด SHOP ต่ำกว่าจริงอีกก้อน ซึ่ง **ไม่ได้อยู่ในคำถาม A2**
6. **สินค้าคงเหลือกับเจ้าหนี้ซัพพลายเออร์รวมด้วยไหม** — `purchase-orders` ไม่โพสต์รายการเลย และ
   `S21-1101/S21-1102` ไม่มีผู้สร้างรายการ ⇒ `S11-2001` มีแต่ขาเครดิต งบทดลอง SHOP ที่สมเหตุผล
   เกือบแน่นอนต้องรวมของในคลัง **แต่ใหญ่กว่าคำถามที่ถามไป**

---

## A3 — ยอดตั้งต้นตาม บอจ.5 (ทำคู่มือ / ให้บันทึกเองได้)

ผู้สอบ **ไม่ให้ตัวเลข** แต่ให้เราทำเครื่องมือ ⇒ กลายเป็น **งานระบบ ไม่ใช่คำวินิจฉัยบัญชี**

โมดูล Equity ที่มีอยู่ทำ `CAP_INIT` ได้ (ทุน/ค่าหุ้นค้างชำระ) แต่ยอดยกมาเต็มชุดต้องมี
**เงินสด/ธนาคาร + กำไรสะสม + ทุกยอดคงเหลืออื่น** ซึ่ง CAP_INIT สร้างขาเหล่านั้นไม่ได้
และโมดูล Equity เป็น **FINANCE-only โดยโครงสร้าง** (ฝั่ง SHOP ใช้ไม่ได้เลย)

⇒ ทางที่เหมาะคือ **หน้าบันทึกรายการปรับปรุงทั่วไป (JV) + คู่มือ** ครอบทั้งสองสมุด

❓ ต้องตัดสินใจ: FINANCE อย่างเดียวหรือทั้งสองสมุด · ลงวันไหน · ใบเดียวหรือหลายใบ ·
เปิดหน้า JV ถาวรให้ OWNER/FM/ACC หรือทำเป็นหน้าครั้งเดียวปิดด้วย feature flag
(หน้า JV ถาวร = ขยายคนที่เขียนรายการอะไรก็ได้ลงสมุดได้ตลอดไป)

---

## ข้อที่ยืนยันแล้ว — ไม่ต้องแก้โค้ด

| ข้อ | ตรวจแล้วว่าโค้ดทำตรงตามที่บอกผู้สอบ |
|---|---|
| **B1** วิธีสุทธิ | `metadata.method = 'NET'` · ผลขาดทุน 126.68 ใน integration fixture ✓ |
| **B2** ราคารับซื้อเป็นต้นทุนมือสอง | `shop-exchange-return.template.ts` → `Dr S11-2002 [buyback]` ✓ **แต่ขา Cr เปลี่ยนรหัสตาม B4** |
| **B3** หักกลบรอบจ่าย | `buildFinanceLines`/`buildShopLines` + ข้ามบรรทัดธนาคารเมื่อสุทธิ = 0 ✓ |
| **C3** เงินพักลดฐานส่วนลด | `rescheduleAdvanceApplied` ต่อครบ JP4/JP5/preview ✓ |
| **A4** ใบลดหนี้ RT-202608-00006 | เอกสารระบุเป็น forward-only อยู่แล้ว ✓ (ควร SELECT ยืนยันบน prod ว่ายังขาดจริง) |

> **B1 หมายเหตุ:** ตัวเลขในสมุดงาน CPA คือ 126.64 แต่ fixture คือ 126.68 — ส่วนต่าง 4 สตางค์
> มาจากการล้างลูกหนี้ตาม GL จริง เทียบกับการคำนวณเส้นตรง **ไม่ใช่ข้อบกพร่อง** ควรระบุตอนลงนามรับรอง

---

## ข้อค้นพบระหว่างตรวจ — ไม่ได้อยู่ใน 13 ข้อ แต่ควรรู้

1. **`shop-books-preflight-2026-08.sql` รันบน prod ไม่ผ่าน** (แก้แล้ว 2026-08-24) —
   `chart_of_accounts` ใช้คอลัมน์ **`"deletedAt"` ตัวเล็กใหญ่ปนกัน** (ไม่มี `@map` ใน schema และ
   migration `20260801100000` บรรทัด 47/58 `DROP COLUMN "deleted_at"` แล้ว `ADD COLUMN "deletedAt"`)
   ขณะที่ `journal_lines`/`journal_entries` เป็น `deleted_at` ⇒ ทุกจุดที่เขียน `coa.deleted_at`
   error `column does not exist` (5 จุด แก้แล้ว — และอีก 1 จุดใน `shop-opening-balance-enumerate`)
2. **`createExternalFinanceSale` ไม่โพสต์รายการฝั่ง SHOP เลย** — `sale-writer.service.ts:513` มี
   `// TODO: Implement perpetual inventory journal` ⇒ ช่องว่างประเภทเดียวกับ C4 **แต่ประชากรใหญ่กว่า**
3. **ตัวตรวจ `missing_cogs` ไม่เคยตรวจอะไรเลย** — `data-audit-checks.service.ts:328-355` หา
   `reference_type = 'CONTRACT_COGS'` ซึ่ง **ไม่มีผู้สร้างค่านี้ที่ไหนในระบบ**
4. **JE 4 ใบลงผิดสมุดบน prod** (พบจาก preflight 2026-08-24 §3.2) — ใบที่ผูกบริษัท **SHOP**
   แต่ใช้ **รหัสบัญชี FINANCE** (เลขล้วน): `JE-202605-00003` · `JE-202605-00005` ·
   `JE-202607-00041` · `JE-202607-00054` (ค่าใช้จ่าย EX-xxxx รวม 642 บาท + ภาษีซื้อ 42 บาท)
   `POST /journal` ตรวจแค่ว่ารหัสมีจริง **ไม่ตรวจว่าตรงกับบริษัทของใบ** ⇒ ใบพวกนี้
   **หายจากรายงานทั้งสองฝั่ง** โดยงบทดลองยังสมดุลปกติ = ไม่มีสัญญาณเตือน
   ⇒ ต้องตัดสินว่าจะย้ายบริษัทของใบ หรือกลับรายการแล้วลงใหม่ (คำถามผู้สอบ)
5. **`EXCHANGE_FINALIZED` / `EXCHANGE_DEVICE_RETURNED_TO_SHOP` ไม่เคยเขียนแถว audit จริง** —
   `AuditService.log` return ทันทีเมื่อไม่มี `userId` และเส้นทาง `activate()` ไม่มี actor ส่งต่อ ⇒
   การเปลี่ยนกรรมสิทธิ์ + ตีราคาเครื่องใหม่ (ที่ผู้สอบเพิ่งรับรองใน B2) **ไม่มีชื่อคนทำ**
   ถ้าผู้สอบถามว่า "ใครตีราคาเครื่องนี้เป็น 8,000" วันนี้ตอบไม่ได้

---

## ลำดับที่แนะนำ

```
1. ✅ รัน shop-books-preflight บน prod แล้ว (2026-08-24 → shop-preflight-20260824.txt
     — **ไฟล์อยู่ในเครื่องเท่านั้น ไม่ commit** เพราะมีชื่อ+อีเมลส่วนตัวของพนักงาน · gitignore แล้ว)
     └─ S21-3001 = 0 บรรทัด ⇒ B4 ไม่ต้อง migrate · พบ JE ลงผิดสมุด 4 ใบ (ดูล่าง)
2. รัน shop-opening-balance-enumerate-2026-08.sql        ← ได้ตัวเลขประชากร A2
3. ส่งคำถามรอบ 2 — `cpa-questions-round2-2026-08-24.html` (12 ข้อ)   ← บล็อกงานที่เหลือเกือบหมด
     รวมคำถามจาก spec ยอดยกมา §8 เข้ามาแล้ว — **อย่าส่งแยกสองชุด**
4. ทำงานที่ไม่ต้องรอคำตอบ:
     ├─ B4 เปลี่ยนรหัสบัญชี (ถ้า prod ยังไม่มีรายการ = แก้โค้ดล้วน)
     ├─ C1 helper resolveStoreCommission (ระวังกับดัก buyback)
     └─ A3 หน้า JV + คู่มือ
5. งานที่ต้องรอคำวินิจฉัยเพิ่ม:
     A2 ยอดยกมา · C2 บัญชีรายได้ · C4 ขามัดจำ · C1 รายการแก้ย้อนหลัง
```

**ห้ามเดารายการบัญชีเพื่อปิดช่องว่างเอง** — หลักการเดิมของโปรเจกต์ยังใช้ทุกข้อ
(ดู `.claude/rules/accounting.md` → interco spec §11 opening-balance gap)

---

# คำตอบรอบ 2 — 25 สิงหาคม 2569

> **หัวใจของรอบนี้:** *"การบันทึกข้อมูลทั้งหมดที่ผ่านมาเป็นการทดสอบโปรแกรม
> เมื่อมีการใช้จริง ข้อมูลที่เคยบันทึกไว้จะโดนล้างออก"*
> ⇒ **งานตั้งยอดยกมา/รายการแก้ย้อนหลังทั้งหมดเป็นโมฆะ** — เริ่มใหม่เหมือนเปิดบริษัทใหม่

| ข้อ | คำตอบ | ผล |
|---|---|---|
| **A1** ย้ายรายการ S21-3001 | เป็นข้อมูลทดสอบ จะล้างออกหมด | ✅ โมฆะ (prod ไม่มีบัญชีนี้อยู่แล้ว) |
| **A2** บัญชีรายได้ค่าธรรมเนียมเลื่อนงวด | *"คือค่างวดที่ลูกค้าจ่ายล่วงหน้า (ไม่ควรมียอดค้าง 165.42)"* | ⚠️ **ยังบล็อก** — ดูด้านล่าง |
| **A3** รายการแก้ค่าคอมย้อนหลัง | ไม่ต้องปรับปรุง จะล้างข้อมูล | ✅ โมฆะ |
| **A4** ยอดยกมาหน้าร้าน | ล้างของเดิมทั้งหมด · ดึงจากระบบเดิม เหมือนเปิดบริษัทใหม่ | ✅ เปลี่ยนวิธีทั้งหมด |
| **A5** เงินมัดจำใบจอง | **ตอนรับเงิน** | ⚙️ ต้องเขียนโค้ด |
| **A6** เงินเจ้าของออกให้ | **หนี้สิน** · `S21-4101` ชื่อเหมาะสม · **ระยะสั้น ไม่คิดดอกเบี้ย** | ✅ ทำแล้ว |
| **B1** ปกส./ภาษี จ่ายรวมได้ไหม | **(ก) ห้ามจ่ายรวม — ต้องแยกจ่ายจากบัญชีธนาคาร/เงินสดของแต่ละส่วน** | ✅ **ไม่ต้องแก้อะไรเลย** |
| **B2** S21-1104 รับทุกประเภท | ถูกต้อง · **ต้องแยกแสดง** · เพิ่มรหัสประเภทเองได้ในอนาคต · ชื่อ = **"เจ้าหนี้ FINANCE"** | ⚙️ ต้องทำ metadata แยกประเภท |
| **B3** ยอดตั้งต้นฝั่งไฟแนนซ์ | ล้างเหมือนกัน เริ่มต้นบริษัทใหม่ | ✅ เปลี่ยนวิธีทั้งหมด |
| **C1** ขายผ่านไฟแนนซ์ภายนอก | แก้ไปข้างหน้าเหมือนกัน | ⚙️ ต้องเขียนโค้ด |
| **C2** ค่าคอมสำรอง 10% | **มีต่อไปทุกสัญญาที่เป็น FINANCE ของเราเอง · ตั้งอัตราสำรองได้ (แก้ไขได้)** | ⚙️ ต้องทำให้ config ได้ |
| **C3** 126.64 vs 126.68 | *(ไม่ได้ตอบ)* | ❓ ค้าง |
| **C4** JE 4 ใบลงผิดสมุด | *(ไม่ได้ตอบ — น่าจะโมฆะเพราะจะล้างข้อมูล)* | ❓ ควรยืนยัน |

## ⚠️ A2 ยังบล็อก — คำตอบขัดกับ C3 ของรอบแรก

คำตอบรอบ 2 บอกว่า **ไม่ควรมียอดค้าง 165.42** แต่รอบแรกข้อ C3 ตอบว่า
**"ปัจจุบันถูกแล้ว"** ซึ่งคือพฤติกรรมที่*สร้าง* 165.42 ขึ้นมา

> ⚠️ **ที่มาของตัวเลข 354/188.58/165.42 — ตรวจแล้ว 2026-08-25:** เป็น **ตัวอย่างคำนวณ**
> จาก fixture ใน `contract-payment.early-payoff-park.spec.ts` (หัวไฟล์เขียนว่า "Fixture
> (ตั้งใจให้ฐานเงินสดของ JE = ยอดปิดของ quote พอดี)") **ไม่ใช่สัญญาจริงบน production**
> — บรรทัด "เคส CPA prod" ใน `.claude/rules/accounting.md` เคยอ้างว่าเป็น prod ซึ่ง
> **ไม่มีหลักฐานรองรับ** และถูกส่งต่อเข้าเอกสารถามผู้สอบรอบ 1-2 ในคำว่า "ตัวอย่างจริง"
> (แก้แล้วทุกไฟล์). **กลไกและเลขคณิตถูกต้อง** — เป็นสูตรตายตัวจาก park + ส่วนลด + VAT

ที่มาของตัวเลข (`compute-payoff-quote.ts:161-165`):
```
rescheduleAdvanceApplied = payoffBeforeLateFees(ไม่มีพัก) − payoffBeforeLateFees(มีพัก)
                         = 188.58   ← เงินพักลดยอดที่ลูกค้าจ่ายจริงแค่นี้
ยอดค้างใน 21-1103        = 354 − 188.58 = 165.42
```
เงินพักลด `remainingBalance` → ลด gross profit → **ลดส่วนลดดอกเบี้ยลงด้วย**
⇒ ลูกค้าจ่ายน้อยลงไม่เต็ม 354

**สองคำตอบประกอบกันได้ ถ้าตีความว่า:** ปลดหนี้ 21-1103 เต็ม 354 →
ส่วน 188.58 ไปลดเงินที่ลูกค้าจ่าย · ส่วน **165.42 รับรู้เป็นรายได้**
(ตรงกับรอบแรกข้อ C2 ที่ว่า *"บันทึกทางบัญชีเป็นรายได้"*)

**แต่ยังลงมือไม่ได้** เพราะ **ยังไม่ได้ระบุว่ารายได้ตัวไหน** — ผังไม่มีบัญชี
ค่าธรรมเนียมเลื่อนงวด และถ้าถือเป็น "ค่างวดจ่ายล่วงหน้า" จริง ก็ต้องรู้ว่า
แตกเป็นเงินต้น/ดอกเบี้ย/VAT อย่างไร ⇒ **ต้องถามสั้น ๆ อีกครั้ง**

## งานที่ถูกยกเลิกจากคำตอบรอบนี้

- รายการแก้ค่าคอมย้อนหลัง (A3) · ตั้งยอดยกมาแบบย้อนหลัง (A4/B3) · ย้ายรายการ S21-3001 (A1)
- **การจ่ายนำส่ง ปกส./ภาษี รวมฝั่งเดียว (B1 = ห้ามจ่ายรวม)** ⇒ `S21-1104` **ไม่ต้อง**
  รองรับการจ่ายแทนกัน · โค้ด payroll ปัจจุบัน (แยกฝั่งอยู่แล้ว) **ถูกต้องตามคำวินิจฉัย**

## งานใหม่ที่เพิ่มเข้ามา

- **A5** — บันทึกเงินมัดจำใบจอง **ตอนรับเงิน** (`Dr เงินสด / Cr S21-2002`) + ล้างตอนแปลงเป็นใบขาย
  · ยังต้องขอ**บัญชีรายได้ริบมัดจำ** กรณีลูกค้าไม่มารับของ (ยังไม่มีในผัง)
- **C1** — ขายผ่านไฟแนนซ์ภายนอกต้องโพสต์ JE ฝั่ง SHOP (แก้ไปข้างหน้า)
- **C2** — เปลี่ยนอัตราค่าคอมสำรอง 10% จาก hardcode เป็น **ตั้งค่าได้**
- **B2** — ต้องมี metadata แยกประเภทหนี้บน `S21-1104` เพื่อให้แยกแสดงในรายงานได้
