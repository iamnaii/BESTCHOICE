# Pre-Merge Guard Report — 2026-07-05

Generated by Pre-Merge Guard agent. Reviewed 3 most recently updated unmerged branches.

---

## Branch 1: `fix/reschedule-qa-test-slip-contract`

**Author:** iamnaii  
**Last commit:** 2026-07-04 19:11 +0700  
**Commits:** 5 (since divergence from main)

### File Changes Summary
19 files changed, 900 insertions(+), 56 deletions(-)

Key files:
- `apps/api/src/modules/users/users.controller.ts` — new `GET /users/approvers` endpoint
- `apps/api/src/modules/receipts/services/receipt-void.service.ts` — approver server-side validation (SoD)
- `apps/api/src/modules/payments/services/payment-query.service.ts` — PAID/waived late-fee split logic
- `apps/web/src/pages/PaymentsPage/index.tsx` — "ชำระครบ" tab (paid-installments view)
- `apps/web/src/components/payment/ReceiptVoidDialog.tsx` — approver picker UI
- Multiple test files (`.spec.ts`, `.test.tsx`)

### Issues Found

#### Critical — None

#### Warning — None

#### Info
- **`payment-query.service.ts:320`** — `netStoredFee` helper uses a closure type annotation with `Prisma.Decimal | null` for `waivedAmount` — correctly handled. No issue.
- **Test coverage** — new `PaymentTable.paid-mode.test.tsx` (120 lines) + `ReceiptVoidDialog.test.tsx` (147 lines) provide good coverage for added UI paths.
- **`GET /users/approvers`** — class-level `@UseGuards(JwtAuthGuard, RolesGuard)` already applies. Method has `@Roles('OWNER','BRANCH_MANAGER','FINANCE_MANAGER','ACCOUNTANT','SALES')`. Returns only `{ id, name, role }` (no PII). Correct.
- **Receipt void approver validation** — server now validates approver exists, is active, and has an allowed role. Previously SoD control was client-side only — this is a positive hardening.

### Recommendation: **APPROVE** ✅

Clean diff. Adds server-side SoD enforcement for receipt void (positive security improvement), fixes PAID-tab late-fee display, and adds meaningful test coverage. No critical or warning issues found.

---

## Branch 2: `fix/late-fee-split-reschedule-collect-first`

**Author:** iamnaii  
**Last commit:** 2026-07-02 21:11 +0700  
**Commits:** 1 (since divergence from main — base is post-PR-#1313)

### File Changes Summary
33 files changed, 2,244 insertions(+), 273 deletions(-)

Key files:
- `apps/api/src/modules/payments/services/reschedule-collect.service.ts` — new service (424 lines), implements "เงินไม่เข้า ดิวไม่เลื่อน" atomic reschedule
- `apps/api/src/modules/paysolutions/services/paysolutions-intent.service.ts` — new `createRescheduleQR` method
- `apps/api/src/modules/paysolutions/services/paysolutions-confirmation.service.ts` — webhook handling for `purpose=RESCHEDULE` links
- `apps/api/src/modules/payments/payments.controller.ts` — 2 new endpoints (`GET /reschedule-quote`, `POST /:id/reschedule-qr`)
- `apps/api/prisma/schema.prisma` — new `purpose` + `metadata` fields on `PartialPaymentLink`
- `apps/api/prisma/migrations/20260979000000_partial_link_purpose_metadata/migration.sql` — `ADD COLUMN IF NOT EXISTS` (safe, non-destructive)
- `apps/web/src/pages/PaymentsPage/components/RescheduleOverlay.tsx` — overlay redesign (362 lines changed)

### Issues Found

#### Critical — None

#### Warning
1. **`paysolutions-confirmation.service.ts`** — `amount: Number(link.amount)` converts a `Prisma.Decimal` from the DB to a JS `number` before passing to `rescheduleWithCollect`. The service immediately re-wraps it as `d(input.amount)` (i.e. `new Prisma.Decimal(…)`), so DB precision is preserved for amounts within JS float safe range (~900 trillion baht). For practical installment amounts this is safe, but the round-trip `Decimal → Number → Decimal` is unnecessary. Recommend passing `link.amount.toString()` instead to avoid any theoretical precision loss.
   - File: `apps/api/src/modules/paysolutions/services/paysolutions-confirmation.service.ts`
   - Pattern to fix: `amount: Number(link.amount)` → `amount: link.amount.toNumber()` (same but explicit) or restructure signature to accept `string | Prisma.Decimal`.

2. **`reschedule-collect.service.ts` input type** — `amount` parameter typed as `number` (line 32). For webhook path the caller passes `Number(link.amount)`. For cashier path the UI sends a string-parsed float. Consider typing as `number | string | Prisma.Decimal` and normalising at the top of the function to avoid relying on implicit string coercion in `d(input.amount)`.
   - This is a correctness-risk type annotation mismatch, not a runtime bug today.

#### Info
- `reconstruct-prior.ts` (88 lines, new) — pure util for JE reconstruction, no DB calls. Clean.
- `reschedule-quote.util.ts` (54 lines, new) — pure Decimal arithmetic, covered by 104-line spec. Good.
- `reschedule-qr.flex.ts` (75 lines, new) — LINE Flex message builder. No auth/security surface.
- New migration uses `ADD COLUMN IF NOT EXISTS` — idempotent and safe. ✓
- `paysolutions-confirmation.service.ts` correctly returns HTTP 200 to PaySolutions on reschedule execution failure and fires a `level: 'fatal'` Sentry alert — matches existing partial-orphan alarm pattern. ✓
- Test coverage: `reschedule-collect.service.spec.ts` (304 lines), `reschedule-quote.util.spec.ts` (104 lines), expanded `payments.controller.spec.ts` (+91 lines). Good baseline.

### Recommendation: **REVIEW** ⚠️

Two warnings around `Decimal → Number → Decimal` round-trip in the webhook confirmation path. Not a current runtime bug (amounts are within JS float safe range), but violates the project convention of never using `Number()` on financial fields. Fix the two callers before merging to keep the pattern consistent and guard against future edge cases.

---

## Branch 3: `feat/ai-hardening-followups`

**Author:** iamnaii  
**Last commit:** 2026-07-02 15:24 +0700  
**Commits:** 5

### File Changes Summary
30 files changed, 672 insertions(+), 111 deletions(-)

Key files:
- `apps/api/src/modules/staff-chat/cron/embedding-backfill.cron.ts` — poison-row isolation + systemic-outage breaker
- `apps/api/src/modules/sales-bot/sales-bot.service.ts` — error classification + token tracking on throw
- `apps/api/src/modules/ai-usage/ai-pricing.ts` — longest-prefix rate lookup + `gemini-2.5-flash-lite` rate
- `apps/api/src/modules/staff-chat/services/ai-auto-reply.service.ts` — rolling-window reply cap (24h)
- `apps/web/src/pages/AiSettingsPage.tsx` — label update ("per session" → "per 24h rolling window")
- Test files across all touched services
- `.gitignore` — removes accidentally committed `node_modules` symlinks

### Issues Found

#### Critical — None

#### Warning — None

#### Info
- **`AiSettingsPage.tsx:165`** — `Number(e.target.value)` for `maxRepliesPerSession` slider. This is a UI integer control (not a money field), so `Number()` is correct here. Not a financial precision issue.
- **`.gitignore` change** — adds `node_modules` symlink exclusion. Good hygiene.
- **`embedding-backfill.cron.ts`** — poison-row EMBED_FAILED marker approach is sound. The `EMBED_FAILED_MARKER` prevents infinite retry loops. Systemic-outage rethrow correctly avoids marking good rows as permanently failed. ✓
- **`ai-pricing.ts` longest-prefix match** — fixes a silent bug where an exact model ID match could fail if not explicitly listed. Well-covered by 9-line spec addition. ✓
- **`ai-auto-reply.service.ts` rolling window** — changes from per-session counter to 24h rolling count. No DB schema change (reuses existing query patterns). ✓
- Test coverage: `embedding-backfill.cron.spec.ts` (123 lines, new), `ai-auto-reply.service.spec.ts` (+38 lines), `sales-bot.service.spec.ts` (+65 lines). Solid.

### Recommendation: **APPROVE** ✅

Clean AI hardening diff. No new controllers or DB schema changes. All changes are internal service improvements (poison-row isolation, error classification, token accounting on failures, rolling-window cap). Good test coverage across all changed services. No security or financial precision issues.

---

## Summary

| Branch | Changes | Critical | Warning | Recommendation |
|--------|---------|----------|---------|----------------|
| `fix/reschedule-qa-test-slip-contract` | 19 files, 900+/56- | 0 | 0 | ✅ APPROVE |
| `fix/late-fee-split-reschedule-collect-first` | 33 files, 2244+/273- | 0 | 2 | ⚠️ REVIEW |
| `feat/ai-hardening-followups` | 30 files, 672+/111- | 0 | 0 | ✅ APPROVE |

### Action Required

**`fix/late-fee-split-reschedule-collect-first`** — Fix before merging:
1. In `paysolutions-confirmation.service.ts`, replace `amount: Number(link.amount)` with `amount: parseFloat(link.amount.toString())` (or restructure `executeWithCollect` signature to accept `Prisma.Decimal` directly) to preserve Decimal semantics through the webhook flow.
2. Consider narrowing the `RescheduleCollectInput.amount` type or normalising early to `Prisma.Decimal` so future callers cannot accidentally pass a floating-point number.
