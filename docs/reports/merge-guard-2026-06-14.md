# Pre-Merge Guard Report — 2026-06-14

Generated by: Pre-Merge Guard agent (automated)
Branches reviewed: 3

---

## Summary

| Branch | Commits ahead | Recommendation |
|--------|--------------|----------------|
| `fix/ci-pre-existing-test-failures` | 2 | ✅ APPROVE |
| `feat/payroll-employee-link` | 9 | ⚠️ REVIEW (2 warnings) |
| `feat/payroll-backfill` | 3 | ✅ APPROVE |

---

## Branch 1: `fix/ci-pre-existing-test-failures`

**Author**: Akenarin Kongdach  
**Latest commit**: 2026-06-08 (77f12aed, 528ca9d1)  
**Scope**: Test-only changes — no production code modified

### File Changes Summary

| File | Change |
|------|--------|
| `apps/api/src/modules/contracts/contract-signing-workflow.spec.ts` | Add missing `findUniqueOrThrow` mock + `createdAt` field (mock drift fix) |
| `apps/api/src/utils/env-validation.spec.ts` | Remove 2 stale `ENCRYPTION_KEY` tests (2FA removed in #1169) |
| `apps/api/e2e/approval-workflow.e2e-spec.ts` | Add warning comment about exclusion |
| `apps/api/e2e/jest-e2e.json` | Exclude incomplete `approval-workflow.e2e-spec.ts` via `testPathIgnorePatterns` |

### Issues

**Critical**: None

**Warning**: None

**Info**:
- The `approval-workflow.e2e-spec.ts` is excluded rather than fixed. Filed as #1192. The PR message states the approval flow works in prod — this is an incomplete test harness problem only. Acceptable short-term.

### Recommendation: ✅ APPROVE

Pure test-repair commit. Unblocks the CI merge gate for all open PRs without touching production code. The `ENCRYPTION_KEY` removal is correctly motivated (2FA was removed in #1169). `approval-workflow` exclusion is self-documented and tracked.

---

## Branch 2: `feat/payroll-employee-link` (PR-C)

**Author**: Akenarin Kongdach  
**Latest commit**: 2026-06-05 (ca3c8e0f)  
**Scope**: Link `PayrollLine → EmployeeProfile` via nullable `userId` FK; derive employee snapshot server-side; PII masking; SSO config endpoint; EmployeeCombobox

### File Changes Summary (unique commits)

| Commit | Files | Description |
|--------|-------|-------------|
| 45021c3e | Migration + schema | Add `PayrollLine.userId` nullable FK |
| f31fd9ae | `create-payroll.dto.ts` | Add optional `userId` field (legacy employeeName stays) |
| 363070c1 | `expense-documents.service.ts` (+94 lines), 2 specs | Resolve employee snapshot from registry on create; PII mask on read |
| d0d66466 | `payroll.template.spec.ts` | JE anti-regression — userId doesn't affect journal math |
| 2caedc10 | `sso-config.controller.ts` + spec | New `GET /sso-config/effective` endpoint |
| d53eb4af | `employees.ts`, `ssoConfig.ts` (new API clients) | Frontend API clients |
| 8b77d262 | `EmployeeCombobox.tsx` + test | New combobox component |
| c22eb56c | `PayrollLinesSection.tsx` + test | Wire EmployeeCombobox; SSO pre-fill on employee pick |

### Issues

**Critical**: None

**Warning**:

1. **`@IsString()` on `userId` FK instead of `@IsUUID()`**  
   File: `apps/api/src/modules/expense-documents/dto/create-payroll.dto.ts`  
   The `userId` field on `PayrollLineInput` uses `@IsString() @IsOptional()`. While the service correctly validates the userId against `employeeProfile` (returning a Thai `BadRequestException` when not found), a non-UUID string (e.g. `"DROP TABLE users"`) passes DTO validation and hits the DB query unnecessarily. Should use `@IsUUID('4') @IsOptional()`.  
   **Risk**: Low (Prisma parameterizes the query, no SQL injection risk; service rejects unknown IDs). But misleading error message for callers.

2. **`parseFloat()` for SSO pre-fill in UI**  
   File: `apps/web/src/components/expense-form-v4/PayrollLinesSection.tsx` (line ~30)  
   ```ts
   const base = emp.baseSalary != null ? parseFloat(emp.baseSalary) : NaN;
   ```
   `parseFloat` is used for client-side SSO pre-fill form display only (the backend derives the authoritative value from `userId` on save — see commit message for 363070c1). However, the frontend rule cautions against JS float for money. The value is stored as a `string` in the form state and sent to the backend as `baseSalary: String(base)`. Not a ledger precision bug, but worth using `new Prisma.Decimal` or keeping it as a string to match the design pattern.  
   **Risk**: Very low (UI display pre-fill only; backend ignores client-sent `baseSalary` when `userId` is present per spec §4.2).

**Info**:

- `expense-documents.service.ts` is now **2774 lines** — well over the 500-line guideline. This is a pre-existing condition, not introduced by this PR. Consider splitting off `payroll-link.service.ts` as a follow-up.
- `EmployeeCombobox` correctly uses `useQuery` + `api.get()` via the new `employeesApi` client. ✓
- `SsoConfigController` correctly has `@UseGuards(JwtAuthGuard, RolesGuard)` at class level + `@Roles(...)` on the endpoint. ✓
- `findMany` in service has `deletedAt: null` filter. ✓
- No hardcoded secrets or hardcoded hex colors found. ✓
- `$queryRaw` not present in this branch. ✓
- `round2` in `PayrollLinesSection` uses `Math.round` — acceptable for UI display pre-fill only (not submitted to backend as a Decimal field directly).

### Recommendation: ⚠️ REVIEW

No blockers. Two minor warnings worth addressing before merge, especially the `@IsUUID` swap (one-line change). Otherwise code quality is high: server-side snapshot derivation is correctly implemented, PII masking present, JE anti-regression test confirms userId has zero impact on journal math.

**Suggested fix before merge**:
```ts
// apps/api/src/modules/expense-documents/dto/create-payroll.dto.ts
@IsUUID('4', { message: 'รูปแบบ userId ไม่ถูกต้อง' })
@IsOptional()
userId?: string;
```

---

## Branch 3: `feat/payroll-backfill` (PR-D)

**Author**: Akenarin Kongdach  
**Latest commit**: 2026-06-05 (b92ecc24)  
**Scope**: Two one-time CLI scripts for Employee Master data migration (builds on PR-C)

### File Changes Summary

| Commit | Files | Description |
|--------|-------|-------------|
| 1d0d17dc | `docs/superpowers/plans/...` | PR-D implementation plan (doc only) |
| 1c60c048 | `backfill-employee-profiles.cli.ts` + spec | Provision `EmployeeProfile` rows for active Users |
| b92ecc24 | `backfill-payroll-user-fk.cli.ts` + spec | Link legacy `PayrollLine.userId` from taxId/name match |

### Issues

**Critical**: None

**Warning**: None

**Info**:
- CLI scripts use `PrismaClient` directly (not `PrismaService`) — appropriate for standalone CLI context. ✓
- `$queryRaw` usage is a tagged template literal with no user input: `SELECT current_database()`. Safe — no injection risk. ✓
- No `Number()` on money fields — CLIs work with IDs and string names only. ✓
- Safety guards in place:
  - `EXPECTED_DB_NAME` must match `current_database()` ✓
  - Production requires `ALLOW_PROD_BACKFILL=YES_I_AM_SURE` ✓
  - 5-second Ctrl+C window before any writes on prod ✓
  - Idempotent via `updateMany` with `userId: null` guard ✓
  - Tier-2 (name-match) requires `BACKFILL_ACTOR_USER_ID` (audit trail for risky matches) ✓
- `resolvePayrollMatch` is pure + fully unit-tested (7 cases including ambiguous duplicates). ✓
- No HTTP controllers added — no guard review needed. ✓
- `backfill-employee-profiles.cli.ts` correctly skips ALL existing profiles (including soft-deleted) to prevent P2002 conflicts. The `// F4` comment explains the design decision. ✓
- `matched-by-name.csv` output: written to working directory — make sure Cloud Run Job mounts a volume or uses stdout if a review artifact is needed in prod.

### Recommendation: ✅ APPROVE

Well-guarded, idempotent, tested one-time migration CLIs with correct safety controls. The tier-1/tier-2 classification design is solid. No production code paths changed.

---

## Checklist: Issues Requiring Action

| # | Severity | Branch | File | Issue | Action |
|---|----------|--------|------|-------|--------|
| 1 | ⚠️ Warning | `feat/payroll-employee-link` | `dto/create-payroll.dto.ts` | `@IsString` on `userId` FK — use `@IsUUID('4')` | Fix before merge |
| 2 | ℹ️ Info | `feat/payroll-employee-link` | `PayrollLinesSection.tsx` | `parseFloat()` for UI pre-fill | OK as-is (UI only), consider `Number(str)` → keep as string |
| 3 | ℹ️ Info | `feat/payroll-employee-link` | `expense-documents.service.ts` | 2774 lines (pre-existing) | Track as tech debt, split in follow-up |
| 4 | ℹ️ Info | `fix/ci-pre-existing-test-failures` | `e2e/jest-e2e.json` | approval-workflow excluded | Tracked as #1192 |

---

*Report generated by Pre-Merge Guard agent • 2026-06-14 • BESTCHOICE monorepo*
