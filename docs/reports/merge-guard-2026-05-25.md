# Pre-Merge Guard Report — 2026-05-25

Generated by: Pre-Merge Guard Agent  
Date: 2026-05-25  
Branches reviewed: 3 (most recently updated unmerged feature/fix branches)

---

## Branch 1 — `feat/canned-response-channel-tabs`

**Author:** Akenarin Kongdach (iamnaii@bestchoice)  
**Commits:** 2 unique commits  
**Scope:** Frontend only (`apps/web/src/pages/canned-response-admin/`)  
**Files changed:** 5 files · +277 / −20

| File | Change |
|------|--------|
| `BubbleList.tsx` | +channelFilter prop, client-side filtering, DnD reorder extracted |
| `ChannelTabs.tsx` (new) | Channel tab UI with per-tab bubble count badges |
| `TemplateEditorPane.tsx` | Wires `ChannelTabs` + updated `BubbleList` |
| `bubble-reorder-logic.ts` (new) | Pure reorder helper extracted from DnD handler |
| `bubble-reorder-logic.test.ts` (new) | 7 unit tests covering edge cases |

### Critical Issues
_None_

### Warning Issues
_None_

### Info
- `onCountsChange` is listed in `useEffect` deps — currently safe because the caller passes `setBubbleCounts` (a stable React state setter). If the prop interface is reused in future with an inline callback, it would cause unnecessary effect re-runs. Consider documenting this in the prop's JSDoc or adding a `useCallback` note at the call site.
- Pre-existing `(r: any)` type cast in `queryFn` (not introduced by this branch) — existing tech debt.
- Test coverage is thorough: includes cross-channel reorder preservation, universal bubble coexistence, and DnD no-op paths.

### Recommendation: ✅ APPROVE

---

## Branch 2 — `feat/data-deletion-page`

**Author:** Akenarin Kongdach (iamnaii@bestchoice)  
**Commits:** 1 unique commit  
**Scope:** Frontend only (`apps/web/`)  
**Files changed:** 2 files · +125 / +0 deletions

| File | Change |
|------|--------|
| `App.tsx` | +1 lazy-loaded public route `/privacy/data-deletion` |
| `DataDeletionPage.tsx` (new) | Static PDPA data-deletion instructions page |

**Context:** This page satisfies Meta/Facebook's requirement for a "Data Deletion Instructions URL" (Facebook App Settings → Basic) for PDPA/GDPR compliance. It is intentionally public (no `ProtectedRoute`).

### Critical Issues
_None_

### Warning Issues
- **Hardcoded contact info:** Owner email `akenarin.ak@gmail.com` and phone `095-567-8887` are baked into source. Any update requires a code deploy. Acceptable for now (single-point contact, unlikely to change frequently), but consider pulling from `SystemConfig` if more dynamic contact management is needed in the future.

### Info
- Route is correctly **unprotected** (public `<Route>` outside `ProtectedRoute`) — required for Meta to access without authentication.
- Correctly lazy-loaded with `React.lazy()` ✅
- Uses design tokens throughout (`bg-background`, `text-foreground`, `text-muted-foreground`, `bg-muted`) — no hardcoded colors ✅
- `leading-snug` used consistently for Thai text ✅
- Hardcoded date "24 พฤษภาคม 2569" (last updated) — standard for static policy pages; update manually when policy changes.
- No API calls, no auth, no PII transmission — purely static informational page.

### Recommendation: ✅ APPROVE

---

## Branch 3 — `fix/exchange-pdpa-clone`

**Author:** Akenarin Kongdach (iamnaii@bestchoice)  
**Commits:** 1 unique commit  
**Scope:** Backend only (`apps/api/src/modules/contract-exchange/`)  
**Files changed:** 2 files · +79 / −5

| File | Change |
|------|--------|
| `contract-exchange.service.ts` | Clones `PDPAConsent` row instead of reusing old consent ID |
| `contract-exchange.service.spec.ts` | +3 test cases for clone / no-consent / ID independence |

**Context:** `Contract.pdpaConsentId` has a `@unique` constraint. Previously the exchange flow tried to assign the old contract's consent row to the new contract, which would cause a unique constraint violation at runtime. Fix clones the consent row inside the existing `$transaction`.

### Critical Issues
_None_

### Warning Issues
- **`grantedAt` copied from original:** The cloned consent preserves the original `grantedAt` timestamp. This is semantically defensible (the customer consented once; the exchange carries the same consent forward) but creates an audit ambiguity — the cloned row's `grantedAt` predates the contract it's attached to. Consider adding a `clonedFromId` or `clonedAt` metadata field if the PDPA auditor needs to trace clone provenance. Low risk for now.

### Info
- Clone operation is fully atomic — runs inside the existing `$transaction` alongside `contract.create` ✅
- Null guard: when `old.pdpaConsentId` is null, `findUnique` and `create` are skipped and `pdpaConsentId: null` flows through correctly ✅
- `findUnique` followed by `create` is two sequential DB calls inside the tx — fine at this scale; no race condition risk because the transaction serializes the reads.
- Tests cover: (1) clone is created and new contract uses cloned ID not original, (2) `pdpaConsentId=null` path skips clone, (3) original test updated to match new behavior (no stale assertions left).
- No money fields touched — no `Decimal` / `Number()` concern.

### Recommendation: ✅ APPROVE

---

## Summary

| Branch | Files Changed | Critical | Warning | Info | Decision |
|--------|--------------|----------|---------|------|----------|
| `feat/canned-response-channel-tabs` | 5 (web only) | 0 | 0 | 2 | ✅ APPROVE |
| `feat/data-deletion-page` | 2 (web only) | 0 | 1 | 4 | ✅ APPROVE |
| `fix/exchange-pdpa-clone` | 2 (api only) | 0 | 1 | 3 | ✅ APPROVE |

All three branches are clear to merge. No blocking issues found. The two warnings are low-risk observations (hardcoded contact info + PDPA audit trail clarity) that can be addressed in follow-up if needed.
