# Pre-Merge Guard Report — 2026-06-22 Run 2

**Generated**: 2026-06-22 ~12:00 UTC  
**Prior run today**: `guard/review-2026-06-22` (09:10 UTC) — reviewed `fix/ci-pre-existing-test-failures`, `feat/payroll-employee-link`, `feat/payroll-backfill`

---

## Scan Summary

No new branches were pushed between 09:10 UTC and this run.

**New remote tracking branches** fetched for the first time today:
- `origin/worktree-feat+sp7.1-dual-prisma-foundation` — tip 2026-05-19, **2,609 commits ahead of main** (dev sandbox, not a pending-merge PR)
- `origin/worktree-feat-shop-sales-ai-phase-a` — tip 2026-05-20, **2,609 commits ahead of main** (dev sandbox, not a pending-merge PR)

These branches are long-running feature development worktrees pushed to remote for backup/collaboration. Their unique content (shop-ai flows, PromptPay QR capture_lead wiring, TIKTOK adapter stub, dual-Prisma foundation) has not been surface-reviewed in prior guard reports. **No action required now** — these will be reviewed when PRs are opened against `main`.

---

## Open PR Status

### PR #1150 — `feat/contacts-audit-cleanup` → `main`

**Status**: Still open, branch unchanged (tip `2d3f7428` — same as reviewed in `guard/review-2026-06-21-run2`).

**Prior finding W1 still applies:**

The PR branch contains `ผู้ขาย` in 4 locations in `ContactDetailPage.tsx` that `main` now has as `ผู้จัดจำหน่าย` (changed by commit `3530d33c` after the PR was opened). A **squash merge** would silently revert this rename.

```
ROLE_LABELS.SUPPLIER           "ผู้ขาย"   ← branch  vs  "ผู้จัดจำหน่าย"  ← main
CardTitle                      "ผู้ขาย"   ← branch  vs  "ผู้จัดจำหน่าย"  ← main
CardLink label                 "...ผู้ขาย" ← branch  vs  "...ผู้จัดจำหน่าย" ← main
Empty-state text               "...ผู้ขาย" ← branch  vs  "...ผู้จัดจำหน่าย" ← main
```

**Required before merge**: Rebase `feat/contacts-audit-cleanup` onto current `main` OR use **"Create a merge commit"** (not squash) in GitHub.

**No new critical or blocking issues found** — 0 Critical, 1 Warning (carry-over from prior run), 0 Info new.

**Recommendation: REVIEW** (same as prior run — rebase first, then merge safely)

---

## Carry-Over Block Status (from 09:10 UTC run)

| Branch | Status | Reason |
|--------|--------|--------|
| `feat/payroll-employee-link` | **BLOCK** | C1: `TwoFactorController` missing `RolesGuard`; C2: migration conflict |
| `feat/payroll-backfill` | **BLOCK** | Inherits C1 + C2 from parent branch |
| `fix/ci-pre-existing-test-failures` | **REVIEW** | W1: no `ClosePeriodDto`; W2: orphaned `totp.util.ts`; W3: 14 E2E specs deleted |

---

## Action Items Outstanding

1. **PR #1150** — Rebase onto main before merge (or use merge commit instead of squash).
2. **`feat/payroll-employee-link`** — Add `RolesGuard` + `@Roles()` to `TwoFactorController`; resolve 2FA migration conflict.
3. **`feat/payroll-backfill`** — Unblock after parent branch is fixed.
4. **`fix/ci-pre-existing-test-failures`** — Add `ClosePeriodDto`, remove or activate `totp.util.ts`, confirm deleted E2E coverage.

---

*Generated by Pre-Merge Guard — automated quality review. Compared against `origin/main` @ `2b62007b` (2026-06-22).*
