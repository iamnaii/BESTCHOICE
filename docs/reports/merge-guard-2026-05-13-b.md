# Pre-Merge Guard Report — 2026-05-13 (Run B)

Generated by Pre-Merge Guard agent. Reviewed 3 branches against `origin/main`.
Complementary to `merge-guard-2026-05-13.md` (Run A, reviewed different branches).

---

## Summary

| Branch | Files Δ | Lines Δ | Verdict |
|--------|---------|---------|---------|
| `feat/expense-documents-all` | 96 | +16,624 / -3,931 | ✅ APPROVE |
| `feat/other-income-v2-2-pr1-override-jv-pagination` | 56 | +5,924 / -478 | ✅ APPROVE |
| `chore/other-income-v2-1-t4-renumber-validation` | 1 | +68 / -41 | ✅ APPROVE |

**No Critical blockers found on any branch.**

---

## Branch 1: `feat/expense-documents-all`

### Description
New Expense Documents module: full-lifecycle expense management including ACCRUAL, SAME-DAY, Credit Note, Payroll, and Vendor Settlement document types. Adds 2 new controllers (`ExpenseDocumentsController`, `ExpenseTemplatesController`), 20+ new service/DTO/template files, and corresponding React UI pages.

### Checklist

#### Critical — ALL PASS
- [x] **JwtAuthGuard** — `ExpenseDocumentsController` has `@UseGuards(JwtAuthGuard, RolesGuard, BranchGuard)` at class level. `ExpenseTemplatesController` has `@UseGuards(JwtAuthGuard, RolesGuard)`. Every route method has `@Roles(...)`.
- [x] **No `Number()` on money** — All arithmetic uses `Prisma.Decimal` / `new Decimal(...)` throughout service and template files. Zero hits in diff.
- [x] **`deletedAt: null` in queries** — `expenseTemplate.findMany` builds `const where = { deletedAt: null }` as base. `expenseDocument` service uses `const where: Prisma.ExpenseDocumentWhereInput = { deletedAt: null }` before any filters. Queries in test files (admin user seed lookup) are intentional test setup, not production paths.
- [x] **No hardcoded secrets** — Zero findings.
- [x] **No raw `$queryRaw`** — Zero occurrences.
- [x] **No SQL injection** — All DB access via Prisma typed query builder.

#### Warning — ALL PASS
- [x] **DTO validation** — `CreateExpenseDocumentDto`, `CreatePayrollDto`, `CreateSettlementDto`, `CreateCreditNoteDto`, `ListExpenseDocumentsQueryDto` all have full `class-validator` decorators with Thai messages and `@IsOptional()` on optional fields.
- [x] **Error handling** — Service methods throw `NotFoundException`, `BadRequestException`, `ConflictException` appropriately; no swallowed exceptions.
- [x] **Frontend `api.*` usage** — All new React components (`CreditNoteForm`, `PayrollForm`, `SettlementForm`, `ExpenseFavoritesPage`, `ExpenseDailySummaryPage`) use `api.get()` / `api.post()` from `@/lib/api`. No raw `fetch()` or bare `axios` calls found.
- [x] **`invalidateQueries` after mutations** — All 5 mutations have `queryClient.invalidateQueries()` in `onSuccess`. `ExpensesPage` uses an `invalidateAll()` helper that fires multiple key invalidations.

#### Info
- ℹ️ `expense-documents.service.ts` is **897 lines** (threshold: 500). Acceptable for a module combining 4 document subtypes under one service. Candidate for sub-service split in a future cleanup pass.
- ℹ️ `ExpensesPage.tsx` is **938 lines**. Large page with inline form dialogs. Could be decomposed into sub-pages/components but not a blocking concern.
- ℹ️ `any` types in test mock objects (`let prisma: any`, `let tx: any`) — standard Jest mock pattern, not production code.
- ℹ️ `TODO` comment in `expense-templates.service.ts` notes cursor-pagination cap at 200 — explicitly documented, acceptable for current scale.

### Recommendation: **APPROVE**

---

## Branch 2: `feat/other-income-v2-2-pr1-override-jv-pagination`

### Description
Adds: (1) Override JV (manual JE edit before POST with `JV_OVERRIDDEN` audit trail), (2) Maker-Checker toggle for Other Income approval flow, (3) Reopen Period workflow with `ReopenPeriodModal`, (4) Settings 5-tab consolidation (`/settings#company|vat|periods|attachment|users`), (5) `PaginationBar` component with `usePaginationParams` hook, (6) `ReopenedPeriodBanner` for open periods. Includes design docs for PR-2 and PR-3.

### Checklist

#### Critical — ALL PASS
- [x] **JwtAuthGuard** — `OtherIncomeController` has `@UseGuards(JwtAuthGuard, RolesGuard)` + class-level `@Roles(...)`. New endpoints `PUT maker-checker` and `GET maker-checker/pending-ready-count` both have `@Roles('OWNER')`. New `AccountingController` endpoint `GET periods/reopened` has `@Roles('OWNER', 'FINANCE_MANAGER', 'ACCOUNTANT')`.
- [x] **No `Number()` on money** — `JournalOverrideService` uses `new Decimal(...)` throughout; `other-income.service.ts` uses `Prisma.Decimal`. Zero occurrences.
- [x] **`deletedAt: null` in queries** — `pendingReadyCount()` correctly queries `{ status: 'READY', deletedAt: null }`. `accountingPeriod.findMany` intentionally omits `deletedAt` because `AccountingPeriod` has no `deletedAt` field (status-driven lifecycle, not soft-deleted). `journalEntry.findMany` in monthly-close service includes `deletedAt: null`.
- [x] **No hardcoded secrets** — None found.
- [x] **No raw `$queryRaw`** — Zero occurrences.

#### Warning — ALL PASS
- [x] **DTO validation** — `ToggleMakerCheckerDto` and `ReopenPeriodDto` have `class-validator` decorators. `list-other-income-query.dto.ts` updated with `@IsOptional` and `@Type`.
- [x] **Frontend `api.*` usage** — `ReopenPeriodModal` is a pure presentation component (callbacks only, no direct API calls). `MakerCheckerToggle`, `VatTab`, `AttachmentTab`, `CompanyTab`, `UsersTab` all use `api.post/put/get` via `@/lib/api`. No raw `fetch()`.
- [x] **`invalidateQueries` after mutations** — Settings tab mutations invalidate `['settings']`. `MakerCheckerToggle` invalidates `['other-income', 'maker-checker']` and `['other-income-maker-checker-enabled']`. `PeriodClosePage` invalidates `['accounting-periods', 'reopened']` for banner refresh.

#### Info
- ℹ️ `other-income.service.ts` has grown to **1249 lines** — largest service in the module. Planning a split into domain-specific sub-services is recommended but not urgent.
- ℹ️ `AuditLogsPage.tsx` adds 44 lines — minor feature addition, no issues.

### Recommendation: **APPROVE**

---

## Branch 3: `chore/other-income-v2-1-t4-renumber-validation`

### Description
Pure refactor of `validation.service.ts` — reorders validation rule execution to match PDF Spec V1–V14 numbering. Adds V9 placeholder comment. Moves V11 (attachment threshold check) before V10/V12 (reconciliation). Moves V15 (BESTCHOICE extension: 42-1102 VAT exemption) to end as a named extension block. No logic changes — identical runtime behaviour.

### Checklist

#### Critical — ALL PASS
- [x] No new controllers, no auth changes.
- [x] No money arithmetic changes.
- [x] No new DB queries.

#### Warning — ALL PASS
- [x] Validation rule coverage unchanged — V3, V4, V6, V7, V8, V10, V11, V12, V13, V14, V15 all present and functionally equivalent.
- [x] V15 bank-interest VAT exemption check preserved (moved to BESTCHOICE extension section at end).

#### Info
- ℹ️ Added JSDoc block documenting all V1–V15 rules and their applicability notes — improves maintainability for future contributors.

### Recommendation: **APPROVE**

---

## Branches Skipped (out of scope)

| Branch | Reason |
|--------|--------|
| `chore/deps-tier3-chunk*` (8 branches) | Dependency version bumps only |
| `chore/optimize-ci-minutes` | CI YAML only (2 files, 13 lines) |
| `watchdog/report-*` (4 branches) | Automated reports, not code |
| `claude/*` branches (10+) | Planning/doc branches — 0 diff lines against main |
| `E2E-TEST` | Test-only branch |
| `chore/card-reader-paths-filter` | 0 diff lines against main |
| `chore/quickbuy-step1-reorder` | 0 diff lines against main |
| `chore/shop-phase3-followup` | 0 diff lines against main |
| `chore/trade-in-orchestrator-rebrand` | 0 diff lines against main |
| `chore/audit-quick-wins` | 0 diff lines against main |

---

*Report generated: 2026-05-13*
