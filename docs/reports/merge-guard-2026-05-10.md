# Pre-Merge Guard Report — 2026-05-10

Generated by Pre-Merge Guard agent. Reviewed top 3 branches by recency.

---

## Branches Reviewed

| Branch | Files Changed | Insertions | Deletions | Author |
|--------|--------------|------------|-----------|--------|
| `feat/expense-documents-pr6` | 95 | +15,926 | -3,899 | Akenarin Kongdach |
| `fix/2a-cron-auto-consume-advance` | 13 | +2,489 | -114 | Akenarin Kongdach |
| `feat/late-fee-and-postpone` | 2 | +234 | -3 | Akenarin Kongdach |

---

## Branch 1: `feat/expense-documents-pr6`

**Description**: Major architectural restructuring — moves expense management out of `AccountingModule` into a new dedicated `ExpenseDocumentsModule`. Adds Credit Note, Payroll, and Vendor Settlement document types. Introduces new frontend pages (`ExpensesPage`, `ExpenseDailySummaryPage`, `ExpenseFavoritesPage`) and form components.

### Critical Issues — None

- ✅ New `ExpenseDocumentsController` has `@UseGuards(JwtAuthGuard, RolesGuard, BranchGuard)` at class level
- ✅ Every method in `ExpenseDocumentsController` carries a `@Roles(...)` decorator
- ✅ No `Number()` casts on financial fields in any added (non-test) lines
- ✅ No raw `fetch()` in frontend components — all API calls use `api.get()`/`api.post()`
- ✅ No hardcoded secrets or API keys
- ✅ `$executeRawUnsafe` in `wipe-expenses.cli.ts` uses only static SQL literals (no user-controlled interpolation)
- ✅ All soft-delete queries include `deletedAt: null`

### Warning Issues

**W1 — PR size makes thorough review difficult**
- 95 files, 15,926 insertions is extremely large for a single PR
- Several test files exceed 500 lines (e.g., `expense-documents.service.spec.ts` = 365 lines, `full-lifecycle.integration.spec.ts` = 249 lines, `settlement-lifecycle.integration.spec.ts` = 184 lines)
- Risk: reviewer fatigue may allow subtle bugs to pass unnoticed
- **Recommendation**: future PRs in this sequence (pr1–pr5 already exist) should be kept smaller

**W2 — `ExpenseDetailInput` DTO has minimal validation**
- File: `apps/api/src/modules/expense-documents/dto/create.dto.ts`
- `ExpenseDetailInput` only validates `@IsString() category` — missing validators for any amount/description fields that may be added later
- Low immediate risk (only `category` is currently used) but sets a weak precedent

**W3 — `@Post(':id/pay')` accepts plain object without DTO**
- File: `apps/api/src/modules/accounting/accounting.controller.ts`
- `markPaid` now accepts `@Body() body: { paymentDate?: string; depositAccountCode?: string } = {}`
- Plain interface, not a class-validator DTO — `paymentDate` format and `depositAccountCode` cash-account allow-list are not validated at the controller layer
- Service may handle this gracefully, but input should be validated at the boundary

### Info

- I1: `wipe-expenses.cli.ts` follows the same guard pattern as `wipe-accounting.cli.ts` (3 env vars, 5-second abort window) ✅
- I2: Several new integration test suites are comprehensive (lifecycle coverage for CN, payroll, settlement)

### Recommendation: **REVIEW** (no blocking issues, but W3 should be addressed)

---

## Branch 2: `fix/2a-cron-auto-consume-advance`

**Description**: Adds auto-consume-advance logic to the `InstallmentAccrual2A` template — when a contract has an advance balance (21-1103) at accrual time, the cron automatically clears it against the new receivable (11-2103) within the same transaction. Also adds `recordExpenseAccrual` method and a new `/accrue` endpoint.

### Critical Issues — None

- ✅ New `@Post(':id/accrue')` endpoint has `@Roles('OWNER', 'FINANCE_MANAGER', 'ACCOUNTANT')`
- ✅ Existing `AccountingController` class-level guards unchanged
- ✅ Advance-consume JE uses `Prisma.Decimal` / `Decimal.min()` — correct precision
- ✅ All new queries include `deletedAt: null` filters
- ✅ New templates (`ExpenseClearanceTemplate`, `ExpenseReverseTemplate`) use `NotFoundException` / `BadRequestException` with Thai messages

### Warning Issues

**W1 — `Number(voided.totalAmount)` in structured log**
- File: `apps/api/src/modules/accounting/accounting.service.ts`, line 301
- `totalAmount: Number(voided.totalAmount)` passed to `structuredLogger.log()`
- Not a DB write (logging only), but violates the project rule of never coercing financial Decimals to JS `Number`
- For values > 2^53 this would silently lose precision; for realistic Thai Baht amounts (≤ 12 digits, 2dp) it is safe in practice
- **Fix**: use `voided.totalAmount.toString()` instead

**W2 — `/pay` endpoint body lacks DTO class validation**
- Same as Branch 1 Warning W3 — the `@Body() body: { paymentDate?: string; depositAccountCode?: string }` pattern is repeated here
- `depositAccountCode` should be validated against the 6-value allow-list (`11-1101`…`11-1203`) at the controller boundary

### Info

- I1: The advance-consume logic is well-documented with inline comments explaining the atomicity guarantee and the business rationale
- I2: New `InstallmentAccrual2ATemplate` spec adds 128 test cases

### Recommendation: **REVIEW** (fix W1 before merge; W2 should be tracked)

---

## Branch 3: `feat/late-fee-and-postpone`

**Description**: Adds `calcLateFee(overdueDays)` and `calcPostponeFee(monthlyPayment, daysToShift)` helper functions to `PaymentReceipt2BTemplate`. Extends the `PaymentReceiptInput` interface with an optional `lateFee` field. Late fee is posted as `Cr 42-1103` (ค่าปรับชำระล่าช้า, excluded from VAT per owner policy). Postpone fee uses `ROUND_DOWN` per CPA Policy A.

### Critical Issues — None

- ✅ No new controllers
- ✅ `calcLateFee` returns `Prisma.Decimal` — no `Number()` cast
- ✅ `calcPostponeFee` uses `Decimal.ROUND_DOWN` per CPA spec
- ✅ Late fee included in cash debit, excluded from installment total for tolerance check
- ✅ Rounding logic correctly isolates late fee before computing tolerance delta

### Warning Issues — None

### Info

- I1: Test suite covers 8 `calcLateFee` cases, 6 `calcPostponeFee` cases, and 5 integration scenarios (on-time, 1–2 days, 3+ days, late fee + overpay, tolerance rejection) — thorough
- I2: `calcPostponeFee` round-DOWN comment correctly documents why `HALF_UP` would produce off-by-0.01 errors

### Recommendation: **APPROVE**

---

## Summary

| Branch | Recommendation | Blocker |
|--------|---------------|---------|
| `feat/expense-documents-pr6` | **REVIEW** | None — address W3 (DTO validation on `/pay`) |
| `fix/2a-cron-auto-consume-advance` | **REVIEW** | None — fix W1 (`Number()` in log → `.toString()`) |
| `feat/late-fee-and-postpone` | **APPROVE** | — |

### Shared Action Items (apply before merging pr6 + 2a-cron)

1. Replace `Number(voided.totalAmount)` → `voided.totalAmount.toString()` in `accounting.service.ts`
2. Create a `MarkExpensePaidDto` class (class-validator) for the `/pay` endpoint body, validating `paymentDate` as `@IsDateString()` and `depositAccountCode` against the CASH_ACCOUNT_CODES allow-list
