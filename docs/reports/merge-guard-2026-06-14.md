# Pre-Merge Guard Report — 2026-06-14

Generated by: Pre-Merge Guard agent  
Date: 2026-06-14  
Branches reviewed: 3 (selected by recency + commit count < 20 ahead of main)

---

## Summary

| Branch | Commits Ahead | Author | Recommendation |
|--------|--------------|--------|----------------|
| `feat/canned-response-postback-routing` | 4 | Akenarin Kongdach | ✅ APPROVE |
| `feat/canned-response-channel-tabs` | 3 | Akenarin Kongdach | ✅ APPROVE |
| `fix/letters-e2e-sales-assertion` | 7 | Akenarin Kongdach | ⚠️ REVIEW |

---

## Branch 1: `feat/canned-response-postback-routing`

**Author**: Akenarin Kongdach <akenarin.ak@gmail.com>  
**Commits**: 4 ahead of main

### Commits
```
57b23560 fix(canned-response): Phase 5 — review issues C1/C2/W4/W5/W6/W7
3711808e feat(canned-response): Phase 5b — POSTBACK template picker in QuickReplyEditor
4cff2b4d feat(canned-response): Phase 5 — Quick Reply postback routing (backend)
b8e00b0d feat(chat): Message Template Picker + Admin Redesign + Multi-bubble Rich Content (#1095)
```

### Files Changed (key files)
- `apps/api/src/modules/staff-chat/services/canned-response-sender.service.ts` — new
- `apps/api/src/modules/staff-chat/services/quick-reply-postback-router.service.ts` — new
- `apps/api/src/modules/staff-chat/services/quick-reply-postback-router.service.spec.ts` — new
- `apps/api/src/modules/chat-adapters/facebook-webhook.controller.ts` — modified
- `apps/api/src/modules/line-oa/line-oa-chatbot.controller.ts` — modified
- `apps/web/src/pages/CannedResponseAdminPage.tsx` — modified
- `apps/web/src/pages/canned-response-admin/QuickReplyEditor.tsx` — modified

### Issues Found

#### Critical
_None_

#### Warning
- **W1 — System user plaintext password**: `CannedResponseSenderService.getSystemUserId()` creates the system bot user with `password: 'NEVER_LOGIN_SYSTEM_USER'` stored as plaintext (unencrypted). The user is `isActive: false` and cannot authenticate, but the password field is not bcrypt-hashed.
  - **Mitigating factors**: Pre-existing pattern — `collections-foundation.seed.ts` already stores `'__NO_LOGIN__'` as plaintext for the same system user. The `upsert` uses `update: {}` so it won't overwrite an already-seeded row. The field name and `isActive=false` guard make accidental login impossible.
  - **Recommendation**: Acceptable as-is given existing precedent. Consider hashing as tech debt if the User model enforces bcrypt elsewhere.

#### Info
- `QuickReplyPostbackRouterService.recentSends` is an in-memory Map — rate limit resets on app restart, not persisted. This is documented in the code as intentional ("fine for a defensive guard"). Acceptable.
- The in-process `WINDOW_MS = 10_000` / `MAX_PER_WINDOW = 5` loop guard is per-process, so scaled deployments (multiple Cloud Run instances) could each allow 5 sends/10s. Documented as known limitation.

### Security Check
- `facebook-webhook.controller.ts` — intentionally public, annotated in code: `"This controller is intentionally public (no JwtAuthGuard)"`. Listed in the allowed-public exceptions in `security.md`.
- `line-oa-chatbot.controller.ts` — uses `@UseGuards(LineWebhookGuard)` (webhook signature guard). Correct for LINE webhook endpoints.
- All `chatRoom.findFirst` calls include `deletedAt: null` ✅
- All `cannedResponse.findFirst` calls include `deletedAt: null, isActive: true` ✅
- FB postback `chatRoom.findFirst` includes `orderBy: { lastMessageAt: 'desc' }` (C2 fix) ✅
- System user creation uses `prisma.user.upsert` (atomic — no P2002 race) ✅

### Recommendation: ✅ APPROVE
All critical patterns followed. W1 is pre-existing precedent, acceptable. Tests included (6 unit tests for router service).

---

## Branch 2: `feat/canned-response-channel-tabs`

**Author**: Akenarin Kongdach <akenarin.ak@gmail.com>  
**Commits**: 3 ahead of main (includes shared base commit `b8e00b0d`)

### Commits
```
d7b6c4bf fix(canned-response): Phase 2b — review issues C/W1/W2 channel tabs
bc4603d3 feat(canned-response): add per-channel tabs in template editor
b8e00b0d feat(chat): Message Template Picker + Admin Redesign + Multi-bubble Rich Content (#1095)
```

### Files Changed (key files)
- `apps/web/src/pages/canned-response-admin/ChannelTabs.tsx` — new
- `apps/web/src/pages/canned-response-admin/BubbleList.tsx` — modified
- `apps/web/src/pages/canned-response-admin/TemplateEditorPane.tsx` — modified
- `apps/web/src/pages/canned-response-admin/bubble-reorder-logic.ts` — extracted helper
- `apps/web/src/pages/canned-response-admin/bubble-reorder-logic.test.ts` — new (7 unit tests)

### Issues Found

#### Critical
_None_

#### Warning
_None_

#### Info
- `ChannelTabs` component correctly uses CSS variable tokens (`bg-primary`, `text-primary-foreground`, `bg-muted`, `text-muted-foreground`, `bg-background`, `border-border`) — no hardcoded hex colors ✅
- `leading-snug` used on Thai text in button spans ✅
- DnD reorder logic extracted to pure function with 7 unit tests — good refactor
- Badge suppression logic (don't show count when it equals the ALL-tab count) documented and tested

### Recommendation: ✅ APPROVE
Frontend-only change. No security surface. Clean design token usage. Good unit test coverage for the reorder algorithm.

---

## Branch 3: `fix/letters-e2e-sales-assertion`

**Author**: Akenarin Kongdach <akenarin.ak@gmail.com>  
**Commits**: 7 ahead of main

### Commits
```
8f3439b6 fix(letters): E2E SALES assertion was matching CANCELLED tab button
3672bc39 fix(letters): add /letters menu entry for SALES and ACCOUNTANT roles (#1100)
9350aedc chore(letters): add letters-mock seed for local testing
69f56192 feat(letters): /letters management page (#1099)
696964e7 feat(canned-response): per-channel tabs in template editor (#1096)
c772d0ab feat(canned-response): Phase 5 — Quick Reply postback routing (#1097)
b8e00b0d feat(chat): Message Template Picker + Admin Redesign + Multi-bubble Rich Content (#1095)
```

### Files Changed (key files)
**Backend**
- `apps/api/src/modules/overdue/contract-letter.service.ts` — new service
- `apps/api/src/modules/overdue/contract-letter.service.spec.ts` — new tests
- `apps/api/src/modules/overdue/dto/bulk-dispatch-letters.dto.ts` — new DTO
- `apps/api/src/modules/overdue/overdue.controller.ts` — modified (new letter endpoints)

**Frontend**
- `apps/web/src/pages/LettersPage/index.tsx` — new page
- `apps/web/src/pages/LettersPage/hooks/useBulkDispatch.ts` — new hook
- `apps/web/src/pages/LettersPage/hooks/useLettersList.ts` — new hook
- `apps/web/src/pages/LettersPage/components/*.tsx` — 5 new components
- `apps/web/src/config/menu.ts` — /letters added for SALES, ACCOUNTANT, FM, OWNER
- `apps/web/e2e/letters-page.spec.ts` — new E2E test

### Issues Found

#### Critical
_None_

#### Warning
- **W1 — SALES role can dispatch letters**: New endpoints `@Roles('OWNER', 'FINANCE_MANAGER', 'BRANCH_MANAGER', 'ACCOUNTANT', 'SALES')` allow SALES to dispatch legal collection letters. Dispatching letters creates legal obligations and paper trail — consider whether SALES should be limited to view-only. **Needs business owner sign-off.**

- **W2 — LetterDispatchDialog uses direct `api.post()` without `useMutation` wrapper** for PDF upload calls (lines 130, 314 use raw `api.post` inside component callbacks rather than `useMutation`). The new `LettersPage/hooks/useBulkDispatch.ts` correctly uses `useMutation` + `invalidateQueries`, but the legacy `LetterDispatchDialog` in CollectionsPage mixes patterns.

#### Info
- `overdue.controller.ts` class-level `@UseGuards(JwtAuthGuard, RolesGuard, BranchGuard)` ✅
- All new endpoints have `@Roles(...)` decorator ✅
- DTO validation uses Thai messages: `'ต้องเลือกอย่างน้อย 1 ฉบับ'`, `'ส่งครั้งละไม่เกิน 50 ฉบับ'` ✅
- `ArrayMaxSize(50)` prevents abuse of bulk endpoint ✅
- `ContractLetterService.cancel()` validates reason length min 5 chars ✅
- `ContractLetterService.list()` uses `getBranchScope()` for branch-scoped filtering ✅
- `ContractLetterService.createIfNotExists()` is idempotent via DB unique constraint `@@unique([contractId, letterType])` ✅
- New `useBulkDispatch` hook: `useMutation` + `qc.invalidateQueries({ queryKey: ['letters'] })` ✅
- New `useLettersList` hook: `useQuery` with `api.get` ✅
- E2E test fix (`8f3439b6`) correctly uses `.first()` to avoid ambiguity with CANCELLED tab ✅
- No `Number()` usage on financial fields in new service code ✅

### Recommendation: ⚠️ REVIEW
W1 (SALES dispatching letters) needs explicit owner/PM decision before merge — this is a business logic gate, not a code quality issue. W2 (raw `api.post` in LetterDispatchDialog) is pre-existing pattern in that component and not introduced by this PR, but worth noting. If W1 is intentional, branch is ready to merge.

---

## Methodology Notes

- 377 total unmerged branches detected. Selected 3 with lowest commit count ahead of main (4, 3, 7 respectively) and most recent commit dates.
- Reviewed: guards, `@Roles` decorators, `Number()` on financial fields, `deletedAt: null` in queries, raw `fetch()` in React, `invalidateQueries` after mutations, Thai validation messages, hardcoded colors.
- Base commit `b8e00b0d` (large PR merge) is shared by branches 1–3 and is not re-reviewed individually.
