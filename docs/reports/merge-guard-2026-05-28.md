# Pre-Merge Guard Report — 2026-05-28

Generated by: Pre-Merge Guard agent
Date: 2026-05-28
Branches reviewed: 3

---

## Branch 1 — `fix/fb-webhook-integration-config`

**Author**: Akenarin Kongdach  
**Commit**: `72da1b25` — fix(facebook-webhook): resolve verify token + app secret from IntegrationConfig  
**Files changed**: 3

| File | +/- |
|------|-----|
| `apps/api/src/modules/chat-adapters/chat-adapters.module.ts` | +7 / -0 |
| `apps/api/src/modules/chat-adapters/facebook-webhook.controller.ts` | +25 / -11 |
| `apps/api/src/modules/chat-adapters/facebook-webhook.controller.spec.ts` | +68 / -0 |

### Summary

Migrates Facebook webhook `FB_VERIFY_TOKEN` and `FB_APP_SECRET` from hard-coded env-var reads (`configService.get()`) to `IntegrationConfigService.getConfig('facebook')` with env as fallback. `verifySignature()` and `verifyWebhook()` become `async`. Adds `IntegrationsModule` to `ChatAdaptersModule` imports. 68 new test lines cover the verify-token path (match / mismatch / empty → fail-closed).

### Critical Issues

None.

- Controller remains intentionally public (no `JwtAuthGuard`) — listed in `security.md` as an approved exception.
- `@SkipCsrf()` decorators are present on all 4 endpoints, consistent with the previous implementation.
- No financial/Decimal fields touched.
- No raw SQL (`$queryRaw`).
- No hardcoded secrets.

### Warnings

**W1 — Per-request DB lookup on hot webhook path**  
`getAppSecret()` calls `integrationConfig.getConfig('facebook')` (a DB round-trip) on every `POST /webhooks/facebook` request (signature verification) and on every `GET` verification request. At low traffic this is fine; at high volume it adds latency on each webhook hit. Consider a short-lived in-memory cache (TTL 60s) in `IntegrationConfigService.getConfig()` for the Facebook config key, or memoize per-request lifecycle.  
This is a scalability concern, not a security bug.

### Info

- New test suite covers the three key cases: token matches → 200 + challenge; token wrong → 400; empty configured token → 400 (fail-closed). Good coverage.

### Recommendation

**APPROVE** — no blocking issues. Address W1 in a follow-up if webhook volume warrants it.

---

## Branch 2 — `fix/letters-e2e-sales-assertion`

**Author**: Akenarin Kongdach  
**Commit**: `8f3439b6` — fix(letters): E2E SALES assertion was matching CANCELLED tab button  
**Files changed**: 1

| File | +/- |
|------|-----|
| `apps/web/e2e/letters-page.spec.ts` | +8 / -4 |

### Summary

Fixes a flaky E2E assertion: the original test checked `getByRole('button', { name: 'ยกเลิก', exact: true }).toHaveCount(0)` for SALES role, but the "CANCELLED" status tab also renders a button/tab with the text "ยกเลิก", causing false failures. The fix changes the assertion to verify page access (heading visible + URL contains `/letters`) instead of checking for button absence.

### Critical Issues

None.

### Warnings

**W1 — Reduced behavioral coverage for SALES role**  
The original test was checking a meaningful security-adjacent behavior: SALES users should not see individual "Cancel" buttons on letter rows. The replacement test only verifies page access, not the absence of the cancel UI. The commit comment correctly notes this is backed by:
1. Backend role test (`POST /overdue/letters/:id/cancel` → 403 for SALES)
2. `LetterTable` component unit test

However, the E2E layer no longer exercises this particular path end-to-end. If the LetterTable component is refactored, the gap may not be caught until production. Consider adding a more targeted selector in a future test (e.g., target row-level action buttons by `data-testid` to avoid ambiguity with tab labels).

### Info

None.

### Recommendation

**APPROVE** — the fix is correct (the original test was genuinely broken). The trade-off in coverage is acceptable given backend enforcement, and is well-documented in the commit.

---

## Branch 3 — `feat/canned-response-channel-tabs`

**Author**: Akenarin Kongdach  
**Commits**: 2 (`bc4603d3`, `d7b6c4bf`) — feat + phase 2b review fixes  
**Files changed**: 5

| File | +/- |
|------|-----|
| `apps/web/src/pages/canned-response-admin/BubbleList.tsx` | +50 / -20 |
| `apps/web/src/pages/canned-response-admin/ChannelTabs.tsx` | +63 / -0 (new) |
| `apps/web/src/pages/canned-response-admin/TemplateEditorPane.tsx` | +17 / -3 |
| `apps/web/src/pages/canned-response-admin/bubble-reorder-logic.ts` | +31 / -0 (new) |
| `apps/web/src/pages/canned-response-admin/bubble-reorder-logic.test.ts` | +100 / -0 (new) |

### Summary

Adds per-channel tab filtering to the canned-response template editor. `BubbleList` gains a `channelFilter` prop and an `onCountsChange` callback for tab badge counts. Drag-to-reorder logic is extracted into a pure `reorderBubbles()` function that operates on `allBubbles` (not the filtered subset) to preserve cross-channel sort order. `ChannelTabs` is a new presentational component. 100-line test suite for `reorderBubbles` covers normal, filtered, edge, and universal-bubble scenarios.

### Critical Issues

None.

- All API calls use `api.get()` / `api.post()` from `@/lib/api` ✓
- `queryClient.invalidateQueries()` called after `createMut` and `reorderMut` success ✓
- Design tokens used throughout (`bg-primary`, `text-primary-foreground`, `bg-muted`, `text-muted-foreground`, `border-border`, `hover:bg-muted/70`) ✓
- `leading-snug` on Thai text ✓
- `aria-pressed` on `ChannelTabs` buttons ✓
- No raw `fetch()` ✓
- No hardcoded hex colors ✓

### Warnings

**W1 — `onCountsChange` as `useEffect` dependency may be fragile**  
In `BubbleList.tsx`:
```ts
useEffect(() => {
  if (!onCountsChange) return;
  ...
  onCountsChange(counts);
}, [allBubbles, onCountsChange]);
```
`onCountsChange` is `setBubbleCounts` (a `useState` setter) in the current call site — React guarantees setter stability, so no infinite loop today. However, if a future caller passes an unstable arrow function, this will loop on every render. Recommend wrapping with `// eslint-disable-next-line react-hooks/exhaustive-deps` comment documenting the stability assumption, or using `useCallback` at the call site as a convention note.

### Info

**I1 — Two pre-existing `: any` casts** in `BubbleList.tsx` (`(r: any) => r.data` on axios response, `(e: any)` on mutation error). These are consistent with the existing codebase pattern for untyped axios responses and are not new.

**I2 — Test coverage is thorough**: 7 test cases including cross-channel drag with hidden bubbles, identity/no-op scenarios, and universal-bubble coexistence.

### Recommendation

**APPROVE** — clean implementation with good test coverage. Address W1 as a low-priority follow-up (add a stability comment or `useCallback` at the call site).

---

## Summary Table

| Branch | Files | Critical | Warning | Info | Recommendation |
|--------|-------|----------|---------|------|----------------|
| `fix/fb-webhook-integration-config` | 3 | 0 | 1 | 1 | **APPROVE** |
| `fix/letters-e2e-sales-assertion` | 1 | 0 | 1 | 0 | **APPROVE** |
| `feat/canned-response-channel-tabs` | 5 | 0 | 1 | 2 | **APPROVE** |

All three branches are clear to merge. No blocking issues found.
