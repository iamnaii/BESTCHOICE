# ล้างข้อมูลทดสอบก่อนเริ่มใช้งานจริง — Runbook

| | |
|---|---|
| วันที่เขียน | 2026-08-25 |
| คำสั่ง | เจ้าของ 2026-08-25 — *"ล้างเฉพาะข้อมูลที่สร้างขึ้นมาเทสเท่านั้น"* |
| เก็บไว้ | **การตั้งค่าทั้งหมด + ทะเบียนลูกค้า/สินค้า + ประวัติแชท** (69 ตาราง) |
| ล้าง | **รายการเคลื่อนไหว + log** (129 ตาราง) |
| เครื่องมือ | `apps/api/src/cli/factory-reset.cli.ts` → `npm run factory:reset` |
| สถานะ | **ยังไม่ได้รันบน production** |

> **ที่มา:** ผู้สอบบัญชีตอบรอบ 2 (2026-08-25) ว่าข้อมูลที่บันทึกมาทั้งหมดเป็นการทดสอบโปรแกรม
> และจะล้างออกก่อนใช้จริง ⇒ งานตั้งยอดยกมาย้อนหลัง/รายการแก้ย้อนหลังทั้งหมดตกไป
> ดู `docs/accounting/cpa-answers-2026-08-24.md` หัวข้อ "คำตอบรอบ 2"

---

## ⚠️ ขอบเขตถูกแก้ระหว่างทาง — อย่าอ่านเวอร์ชันเก่า

| | รอบแรก (ตกไปแล้ว) | **รอบที่ใช้จริง** |
|---|---|---|
| เก็บ | นิติบุคคล + สาขา + ผู้ใช้ (3 ตาราง) | **69 ตาราง** — ตั้งค่า เทมเพลต ลูกค้า สินค้า แชท |
| ต้องตั้งค่าใหม่หลังล้าง | ทุกอย่าง (ดอกเบี้ย ค่าคอม ผังบัญชี บัญชีเงินสด) | **ไม่ต้อง** — ตั้งค่าอยู่ครบ |
| ผังบัญชี | ต้อง `seed:coa` ใหม่ | **เก็บไว้** ไม่ต้อง seed |
| สินค้าคงเหลือ | หายไปพร้อมกัน | **ยังอยู่** ⇒ เกิดงานใหม่ ดูหัวข้อถัดไป |

คำสั่งเจ้าของฉบับเต็ม:

> *"อันนี้ยังไม่ต้องล้าง สาขา → ตั้งบัญชีผู้ใช้ → ตั้งดอกเบี้ย/ค่าคอม/งวด และก็ประวัติแชท
> ล้างเฉพาะ ข้อมูลที่สร้างขึ้นมาเทส เท่านั้น"*

บวกกับที่ยืนยันเพิ่ม: เก็บ **ทะเบียนลูกค้า** และ **สินค้าในสต็อก** ทั้งหมด

---

## 🔴 ผลข้างเคียงที่เกิดจากขอบเขตใหม่ — ต้องรู้ก่อนตัดสินใจ

### สินค้าคงเหลือจะไม่มีมูลค่าในบัญชี

รอบแรกล้างสินค้าไปด้วย จึงไม่มีปัญหานี้ **รอบนี้สินค้ายังอยู่บนชั้นแต่รายการบัญชี
(`journal_entries` / `journal_lines`) ถูกล้างทั้งหมด** ⇒

```
ตารางสินค้า      : มีเครื่องอยู่ N เครื่อง  ✅
งบทดลอง S11-2001 : 0.00 บาท               ❌  ← ไม่ตรงกัน
```

**ต้องตั้งยอดยกมาสินค้าคงเหลือ** (`Dr S11-2001 สินค้าคงเหลือ / Cr ส่วนของเจ้าของ S32-1101`)
ก่อนเริ่มบันทึกรายการจริง ไม่งั้นตอนขายเครื่องเหล่านี้ ระบบจะตัดต้นทุนจากบัญชีที่ยอดเป็น 0
⇒ สินค้าคงเหลือติดลบ

เรื่องเดียวกันนี้ครอบถึง: **เงินสด/ธนาคารคงเหลือ · เจ้าหนี้การค้า · เงินมัดจำลูกค้าที่ยังไม่คืน**

> ⛔ **ยังลงมือไม่ได้** — `.claude/rules/accounting.md` หัวข้อ "ห้ามลง JV มือในบัญชีที่เลนส์
> inter-co อ่านต่อสัญญา" ระบุว่า `S11-2001` เป็นบัญชีที่ **ปลอดภัย** สำหรับ JV มือ ✅
> แต่ยอดตั้งต้นต้องมาจากการนับสต็อกจริง + ราคาทุนจริง ซึ่งเป็นงานของเจ้าของ/ผู้สอบ
> **ห้ามให้ระบบเดายอดให้เอง**

### สถานะสินค้าที่เคยขายตอนเทส

เครื่องที่ขายไปตอนเทสจะค้างสถานะ `SOLD_*` โดยไม่มีใบขายอยู่จริง สคริปต์คืนให้ **3 สถานะ**:

| สถานะ | ทำอะไร | เหตุผล |
|---|---|---|
| `SOLD_CASH` · `SOLD_INSTALLMENT` | → `IN_STOCK` อัตโนมัติ | คลาสเดียวกับ `SaleVoidService` (ยกเลิกใบขาย) — ตอนขายบังคับว่าต้องเป็น `IN_STOCK` มีราคาอยู่ก่อน และไม่มี flow ไหนแตะราคาระหว่างขาย ⇒ เครื่องกลับมาพร้อมราคาของตัวเอง |
| `RESERVED` | → `IN_STOCK` อัตโนมัติ | กติกาเรียกว่า "benign โดยโครงสร้าง" — การปลดจองคือคืนสภาพเดิม |
| `REPOSSESSED` · `REFURBISHED` · `SOLD_RESELL` | **ไม่แตะ** รายงานให้ดูอย่างเดียว | เครื่องยึด/เปลี่ยนเครื่องถือ **ราคาเครื่องใหม่** ติดตัวมา ต้องตีราคาใหม่ก่อนขาย — flip ให้เองคือการข้ามด่านราคาทั้งคลัง (`.claude/rules/database.md` → "ประตูเข้า IN_STOCK") |

กรรมสิทธิ์ (`ownedByCompanyId`) ที่ย้ายไป FINANCE ตอนเปิดสัญญา ถูกคืนให้ SHOP ทั้งหมด

---

## ทำไมเปลี่ยนจาก "เก็บรายการสั้น ที่เหลือล้างหมด" เป็น "จำแนกครบสองทาง"

รอบแรกใช้การกลับด้าน (ล้างทุกตารางที่ไม่อยู่ใน keep list) ซึ่ง**ถูกต้องสำหรับขอบเขตนั้น**:
ตารางที่ลืมจำแนก = ถูกล้าง = ตรงเจตนาอยู่แล้ว

ขอบเขตใหม่พลิกข้อนี้ — รายการที่เก็บโตเป็น 69 ตาราง และในนั้นมี **11 ตารางที่ seed อยู่ใน
migration เท่านั้น ไม่มี seed script ใด ๆ**:

```
account_role_map · payment_method_configs · sso_config · bank_accounts · reverse_reasons
notification_templates · template_categories · supplier_payment_methods
```

ล้างแล้ว **สร้างกลับไม่ได้** (ต้องไปรื้อ migration มารันมือ) ⇒ ผลของการ "ลืมจำแนก"
เปลี่ยนจาก *ปลอดภัย* เป็น *สูญหายถาวร*

ตอนนี้ทุกตารางถูกระบุครบสองทาง (`KEEP_TABLES` 69 + `WIPE_TABLES` 129 = 198) และสคริปต์
**หยุดพร้อมรายชื่อ** ถ้าเจอตารางที่ไม่อยู่ในลิสต์ไหนเลย (model ใหม่ที่เพิ่มวันหลัง)
แทนที่จะเดาแล้วล้างทิ้ง

---

## ข้อเท็จจริงที่ตรวจแล้ว (ไม่ใช่การเดา)

### 1. `TRUNCATE` ผ่าน trigger กันลบของ `audit_logs` ได้

```sql
-- migration 20260520300000_audit_log_archive_immutable
CREATE TRIGGER audit_logs_no_delete
BEFORE DELETE ON "audit_logs"
FOR EACH ROW                       -- ← row-level
EXECUTE FUNCTION audit_logs_block_delete();
```

PostgreSQL **ไม่จุด row-level trigger ตอน TRUNCATE** และ grep แล้วไม่มี
`BEFORE TRUNCATE` / `FOR EACH STATEMENT` ที่ไหนเลย ⇒ `DELETE` ถูกบล็อก แต่ `TRUNCATE` ผ่าน

**ไม่ต้อง drop trigger** — drop แล้วลืมสร้างกลับ = เกราะกันแก้หลักฐานหายถาวร

> **ข้อค้นพบด้านความปลอดภัย (แยกเรื่อง):** คอมเมนต์ใน migration เขียนว่า *"Any code path
> (ORM, raw SQL, migration) that attempts DELETE will raise"* แต่ **TRUNCATE ไม่ใช่ DELETE**
> ⇒ ใครมีสิทธิ์ TRUNCATE ลบ audit trail ได้ทั้งตาราง · ถ้าต้องการปิดช่องนี้ ให้เพิ่ม
> `BEFORE TRUNCATE ... FOR EACH STATEMENT` **หลังล้างเสร็จ** (ใส่ก่อนจะบล็อกงานนี้เอง)

### 2. 🔴 โซ่ที่เกือบทำประวัติแชทหายทั้งหมด

การตรวจ FK อัตโนมัติจับได้ 4 เคสที่ตาคนมองไม่เห็น — เคสร้ายที่สุด:

```
chat_rooms.attribution_id → ads_attributions.contract_id → contracts
                                                            ↑ ต้องล้าง
```

`ads_attributions` เก็บไม่ได้ (ชี้ต่อไปที่ `contracts`) แต่ถ้าปล่อยให้
`TRUNCATE ads_attributions CASCADE` ทำงาน มันจะลบ `chat_rooms` **และ `chat_messages`
(`onDelete: Cascade`)** ทั้งหมด = **ประวัติแชทที่เจ้าของสั่งให้เก็บหายเกลี้ยงแบบเงียบ ๆ**

แก้ด้วย `PRE_TRUNCATE_NULLIFY` — NULL `chat_rooms.attribution_id` ก่อน TRUNCATE
(คอลัมน์ nullable อยู่แล้ว) เสียแค่สายโยงว่าแชทนี้มาจากโฆษณาตัวไหน ซึ่งผูกกับ
**สัญญาทดสอบ**ที่กำลังจะล้างอยู่แล้ว

อีก 3 เคสที่จับได้:

| FK | ผลถ้าไม่จับได้ | ทางแก้ที่ใช้ |
|---|---|---|
| `products → inspections` | ลบสินค้า**ทั้งคลัง** | เก็บ `inspections` (closure สะอาด ชี้ไป template/user ที่เก็บอยู่แล้ว) |
| `products → purchase_orders` | ลบสินค้า**ทั้งคลัง** | เก็บ `purchase_orders` + `po_items` (เป็นที่มาของสินค้าที่เก็บไว้) |
| `kyc_verifications → contracts` | — | **ล้าง** — FK บังคับ (ไม่ nullable) เก็บไม่ได้แม้อยากเก็บ |

ด่านนี้รัน **สดทุกครั้ง** ไม่พึ่งการตรวจครั้งเดียว — FK ใหม่ที่เพิ่มวันหลังจะเปิดช่องนี้ทันที

### 3. `_prisma_migrations` ต้องเก็บ

ล้างแล้ว `migrate deploy` ครั้งถัดไปจะพยายามรัน migration ทั้งหมดซ้ำบน schema ที่มีตารางอยู่แล้ว

---

## ขั้นตอน

### ① ก่อนเริ่ม — ยืนยันจุดกู้คืน ⚠️ ห้ามข้าม

Cloud SQL มี automated backup + PITR (`.claude/CLAUDE.md`) — **ยืนยันว่ามีจุดกู้คืนที่ใช้ได้จริง
ก่อนรันอะไรทั้งสิ้น** ถ้าล้างผิดแล้วไม่มี backup คือจบ

### ①.5 ปลดล็อกเครื่องที่ MDM + หยุด API ⚠️ ห้ามข้าม

**ก) ปลดล็อกเครื่องที่สั่งล็อกไว้ตอนทดสอบ — ทำ *ก่อน* ล้าง**

`mdm_lock_requests` เป็นบันทึกฝั่งเรา ส่วนคำสั่งล็อกจริงอยู่ที่ **PJ-Soft** ⇒ ล้างแล้ว
เครื่องยัง **ล็อกค้างอยู่ที่ผู้ให้บริการ** และระบบไม่มีบันทึกว่าเคยล็อกเครื่องไหนไว้
= ปลดไม่ได้ ตามไม่เจอ

```sql
-- ดูว่ามีเครื่องไหนถูกล็อกค้างอยู่บ้าง (รันก่อนล้าง)
SELECT id, contract_id, imei, status, created_at
FROM mdm_lock_requests
WHERE status NOT IN ('UNLOCKED', 'FAILED', 'CANCELLED')
ORDER BY created_at DESC;
```

มีแถว → ปลดล็อกผ่านหน้าจอ MDM ให้หมดก่อน แล้วค่อยล้าง

**ข) หยุด API ก่อนล้าง**

cron เขียนข้อมูลตลอดเวลา (00:01 ตั้งค้างรับรายวัน · 00:30 ECL · 02:00 VAT 60 วัน ·
09:07 อายุลูกหนี้ · 09:15 ออกจดหมาย ฯลฯ) ถ้ารันทับตอนล้าง จะได้ข้อมูลผีที่เกิดหลัง
TRUNCATE และด่านตรวจผล "ทุกตารางต้องเป็น 0" จะ fail

```bash
gcloud run services update bestchoice-api --region asia-southeast1 --min-instances=0 --max-instances=0
# ล้างเสร็จแล้วค่อยเปิดกลับ
```

### ② ดูก่อนว่าจะลบอะไร (ไม่เขียนอะไรเลย)

```bash
cloud-sql-proxy --gcloud-auth --port 5432 bestchoice-prod:asia-southeast1:bestchoice-db

DRY_RUN=1 EXPECTED_DB_NAME=bestchoice \
  DATABASE_URL="postgresql://<user>@127.0.0.1:5432/bestchoice" \
  npm --prefix apps/api run factory:reset
```

จะพิมพ์: ตารางที่จะล้างพร้อมจำนวนแถว · **ตารางที่เก็บพร้อมจำนวนแถว** · สถานะสินค้าที่ค้าง

**ตรวจตรงนี้ว่า `customers` / `products` / `chat_messages` / `system_config` มีแถวตามที่ควรจะเป็น**
ถ้าเป็น 0 แปลว่ามีอะไรผิด — อย่ารันต่อ

### ③ รันจริง

```bash
CONFIRM_FACTORY_RESET=YES_I_AM_SURE \
  ALLOW_PROD_RESET=YES_I_AM_SURE \
  NODE_ENV=production \
  EXPECTED_DB_NAME=bestchoice \
  DATABASE_URL="postgresql://<user>@127.0.0.1:5432/bestchoice" \
  npm --prefix apps/api run factory:reset
```

มี **หน่วง 10 วินาที** ให้กด Ctrl+C ยกเลิกได้ · สคริปต์ตรวจผลเอง **สองทาง**:
ทุกตารางที่ล้างต้องเป็น 0 **และ** ทุกตารางที่เก็บต้องยังมีข้อมูล (กัน CASCADE เล็ดลอด)

### ④ หลังล้าง

| # | ทำอะไร | ไม่ทำแล้วเกิดอะไร |
|---|---|---|
| 1 | **ตั้งยอดยกมา**: สินค้าคงเหลือ · เงินสด/ธนาคาร · เจ้าหนี้ | ขายแล้วสินค้าคงเหลือติดลบ · งบไม่ตรงของจริง |
| 2 | ตรวจสถานะสินค้าที่ไม่ได้คืนอัตโนมัติ (`REPOSSESSED`/`REFURBISHED`/`SOLD_RESELL`) | เครื่องค้างขายไม่ได้ |
| 3 | เปิดงวดบัญชีของเดือนที่จะเริ่มใช้จริง | โพสต์ JE ติดด่านงวดปิด |
| 4 | รัน `docs/accounting/shop-books-preflight-2026-08.sql` | ไม่รู้ว่าพร้อมจริงหรือยัง |

> **ไม่ต้องทำแล้ว** (ต่างจากรอบแรก): `seed:coa` · ตั้ง `Branch.shopCashAccountCode` ·
> ตั้ง `User.defaultCashAccountCode` · ตั้งดอกเบี้ย/ค่าคอม — ทั้งหมดถูกเก็บไว้
>
> **ยกเว้น** ถ้าผู้สอบอนุมัติบัญชีใหม่ (`S11-3101` / `S51-1106`) แล้ว ต้องรัน `seed:coa`
> เพื่อเพิ่มบัญชีนั้นเข้าผัง (เป็น upsert ไม่ทับค่า `peakCode` ที่เจ้าของตั้งไว้)

### ⑤ ตรวจรับ

- งบทดลองทั้งสองฝั่งเป็น 0 (ก่อนตั้งยอดยกมา) แล้วสมดุลหลังตั้งยอดยกมา
- ไม่มี JE ลงผิดสมุด
- ผังบัญชียังครบ (SHOP ~56 + FINANCE 111)
- จำนวนสินค้าในระบบ = จำนวนที่นับได้จริง

---

## สิ่งที่ **ไม่** ถูกล้าง (อยู่นอก DB)

- **ไฟล์ใน S3** (สลิป เอกสารสัญญา ลายเซ็น ใบเบิกจ่าย) — แถวใน DB หายแต่ไฟล์ค้างเป็นขยะถาวร
  (`e_documents.fileUrl` · `contract_documents.fileUrl` · `contract_letters.fileUrl` ฯลฯ)
  **รูปสินค้าไม่กระทบ** เพราะ `product_photos` ถูกเก็บไว้
- **คำสั่งล็อกเครื่องที่ PJ-Soft** — ดูขั้น ①.5 ต้องปลดก่อนล้าง
- **`audit_logs_seq`** — เป็น standalone sequence (`CREATE SEQUENCE`, migration 20260525100000)
  ไม่ได้ผูกกับคอลัมน์ ⇒ `TRUNCATE ... RESTART IDENTITY` **ไม่รีเซ็ตให้** เลขจะเดินต่อจากเดิม
  ไม่พังอะไร (แค่เลขไม่เริ่มจาก 1) — ถ้าอยากรีเซ็ต: `ALTER SEQUENCE audit_logs_seq RESTART WITH 1;`
- **ข้อความ LINE ที่ส่งไปแล้ว** — ลูกค้ายังเห็นในแชท
- **เอกสารที่พิมพ์ออกไปแล้ว** (ใบเสร็จ ใบกำกับภาษี สัญญา)
- **แบบภาษีที่ยื่นไปแล้ว** ถ้าเคยยื่น

> ⚠️ **ถ้าเคยยื่น ภ.พ.30 หรือออกใบกำกับภาษีจากข้อมูลชุดนี้** ต้องปรึกษาผู้สอบก่อน —
> พ.ร.บ.การบัญชี กำหนดให้เก็บเอกสาร ≥ 5 ปี · ผู้สอบยืนยันแล้วว่าข้อมูลชุดนี้เป็น
> การทดสอบโปรแกรม แต่ถ้ามีการยื่นแบบจริงเกิดขึ้น ต้องแจ้งผู้สอบก่อนล้าง

---

## ถ้าล้างผิด

1. หยุดใช้งานระบบทันที (ยิ่งเขียนข้อมูลใหม่ ยิ่งกู้ยาก)
2. กู้ผ่าน **Cloud SQL PITR** ไปยังเวลาก่อนรันสคริปต์
3. ตรวจว่าข้อมูลกลับครบก่อนเปิดใช้งาน

**สคริปต์ไม่มีปุ่ม undo** — จุดกู้คืนใน ① คือทางเดียว
