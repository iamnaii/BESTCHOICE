# Merge Guard Report — `claude/fix-contract-details-mismatch-rGvPp`

**Date**: 2026-04-17  
**Reviewer**: Pre-Merge Guard (automated)  
**Branch**: `claude/fix-contract-details-mismatch-rGvPp`  
**Author**: Claude <noreply@anthropic.com>  
**Status relative to main**: 1 commit ahead · 616 commits behind  
**Latest commit**: `8e0fb90f` (2026-04-04)

---

## File Changes Summary

1 file changed, 2 insertions / 2 deletions:

| File | Change |
|------|--------|
| `apps/api/src/modules/contracts/documents.service.ts` | Lines 210 & 415: replace hardcoded `'STORE_DIRECT'` with `contract.planType \|\| 'STORE_DIRECT'` |

### The fix
```diff
-  const resolved = await this.resolveTemplate('STORE_DIRECT', documentType);
+  const resolved = await this.resolveTemplate(contract.planType || 'STORE_DIRECT', documentType);
```
Applied in two places: `generateDocumentPdf()` and `generateContractPdf()`.

---

## Issues by Severity

### ⛔ CRITICAL

None.

### ⚠️ WARNING

None.

### ℹ️ INFO

**I-1: Fix already exists in `origin/main`**
- `origin/main:apps/api/src/modules/contracts/documents.service.ts` lines 214 and 452 already contain `contract.planType || 'STORE_DIRECT'`.
- The branch is **616 commits behind main** and its single commit has already been incorporated.

---

## Recommendation

### 🗑️ STALE — Close & Delete Branch

The bug fix has already been merged into `origin/main`. There is nothing new to review or merge. The branch should be deleted.

```bash
git push origin --delete claude/fix-contract-details-mismatch-rGvPp
```

---

*Generated by Pre-Merge Guard on 2026-04-17*
