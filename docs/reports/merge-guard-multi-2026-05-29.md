# Pre-Merge Guard Report — 2026-05-29

Generated by: Pre-Merge Guard agent  
Reviewed: 3 most-recently-updated unmerged branches (654 total unmerged branches exist)

---

## Branch 1: `fix/fb-webhook-integration-config`

**Author:** Akenarin Kongdach  
**Commit:** `72da1b25` — fix(facebook-webhook): resolve verify token + app secret from IntegrationConfig  
**Last updated:** 2026-05-28

### File changes (3 files, +96 / -11)
```
apps/api/src/modules/chat-adapters/chat-adapters.module.ts     |  3 +
apps/api/src/modules/chat-adapters/facebook-webhook.controller.spec.ts | 68 +
apps/api/src/modules/chat-adapters/facebook-webhook.controller.ts      | 36 +/- 11
```

### What this branch does
Migrates `FacebookWebhookController` from reading `FB_VERIFY_TOKEN` / `FB_APP_SECRET` hard-coded
env vars to reading from `IntegrationConfigService` (Settings → Integrations UI), with the env
var still serving as a fallback. `verifySignature` and `verifyWebhook` both become `async`.
Three new spec blocks cover: challenge match, token mismatch, and fail-closed on empty token.

### Critical
_None._

### Warning
1. **No error handling around `getConfig` in `verifyWebhook`** — if the DB is unreachable when Facebook calls the verification endpoint, `integrationConfig.getConfig('facebook')` will throw an unhandled exception and NestJS returns a 500 (Facebook sees a failure and retries). Adding a `try/catch` that falls back to the env var or returns 400 would be safer. Low likelihood in production but worth noting.

### Info
1. `FacebookWebhookController` is intentionally public (no `JwtAuthGuard`) — this is an existing condition, not introduced by this branch. However, it is **not listed** in `security.md`'s "Intentionally Public Endpoints" list. Consider adding it alongside `sms-webhook` and `paysolutions`.
2. The `verifyWebhook` signature change (sync → async) required updating the call site at `handleWebhook` (`!(await this.verifySignature(...))`). The diff correctly handles this in all three call sites.

### Recommendation: ✅ APPROVE

The security posture is improved (secrets now DB-managed, fail-closed on empty token). The Warning
is low-risk for this specific endpoint. Safe to merge.

---

## Branch 2: `fix/letters-e2e-sales-assertion`

**Author:** Akenarin Kongdach  
**Commit:** `8f3439b6` — fix(letters): E2E SALES assertion was matching CANCELLED tab button  
**Last updated:** 2026-05-26

### File changes (1 file, +8 / -4)
```
apps/web/e2e/letters-page.spec.ts | 12 +/-
```

### What this branch does
Replaces a flaky E2E assertion (`expect(button[name=ยกเลิก]).toHaveCount(0)`) that was matching
the "CANCELLED" status tab (also labelled ยกเลิก) with a simpler page-reachability check:
assert the `จัดการจดหมาย` heading is visible and the URL contains `/letters`. The comment explains
the role-enforcement is tested at the API level and in unit tests.

### Critical
_None._

### Warning
_None._

### Info
1. The new assertion tests "SALES can reach the page" rather than "SALES cannot see Cancel buttons". The role-enforcement claim (backend returns 403 for SALES on `POST /overdue/letters/:id/cancel`) is mentioned in the comment but not verified here. Confirm this backend guard exists — if it does, the assertion strategy is sound.

### Recommendation: ✅ APPROVE

The original test was a false negative (testing the wrong element). The replacement is a valid
smoke-test that matches what the backend actually enforces.

---

## Branch 3: `feat/canned-response-channel-tabs`

**Author:** Akenarin Kongdach  
**Commits:** 2 commits  
**Last updated:** 2026-05-25

### File changes (5 files, +277 / -20)
```
apps/web/src/pages/canned-response-admin/BubbleList.tsx            | 86 +/- 20
apps/web/src/pages/canned-response-admin/ChannelTabs.tsx           | 63 +  (new)
apps/web/src/pages/canned-response-admin/TemplateEditorPane.tsx    | 17 +/-
apps/web/src/pages/canned-response-admin/bubble-reorder-logic.test.ts | 100 + (new)
apps/web/src/pages/canned-response-admin/bubble-reorder-logic.ts   | 31 +  (new)
```

### What this branch does
Adds per-channel tab filtering to the canned-response template editor (LINE / Facebook / ALL).
Channel tabs show badge counts and filter the visible bubble list. New bubbles created while a
channel tab is active are automatically scoped to that channel. DnD reorder logic is extracted
into a pure `reorderBubbles()` helper that operates on the full bubble array so hidden (filtered)
bubbles preserve their sort order — this is unit-tested with 7 cases.

### Critical
_None._

### Warning
_None._

### Info
1. **`useEffect` stability** — `onCountsChange` in `BubbleList`'s effect dependency array is `setBubbleCounts` from `useState` in the parent. State setters are stable references; no infinite-loop risk. If `BubbleList` is ever reused with an inline callback (`onCountsChange={() => ...}`), the effect will re-run on every render. Consider wrapping in `useCallback` at the call site if this becomes a pattern.
2. **Design tokens** — `ChannelTabs.tsx` uses `bg-primary`, `text-primary-foreground`, `bg-muted`, `text-muted-foreground`, `bg-background` throughout. No hardcoded hex colors. ✓
3. **`r: any` in queryFn** — `api.get(...).then((r: any) => r.data)` follows the existing pattern in this codebase. Acceptable.
4. The `bubble-reorder-logic.test.ts` provides good coverage including the tricky "hidden bubbles from other channel tabs preserve position" case. The logic is correct.

### Recommendation: ✅ APPROVE

Clean frontend feature with no security or financial issues. Pure logic extracted and well-tested.
Design token usage is correct throughout.

---

## Summary

| Branch | Files Changed | Critical | Warning | Info | Verdict |
|---|---|---|---|---|---|
| `fix/fb-webhook-integration-config` | 3 | 0 | 1 | 2 | ✅ APPROVE |
| `fix/letters-e2e-sales-assertion` | 1 | 0 | 0 | 1 | ✅ APPROVE |
| `feat/canned-response-channel-tabs` | 5 | 0 | 0 | 3 | ✅ APPROVE |

No branches are blocked. All three are safe to merge as-is.

The one Warning (`fix/fb-webhook-integration-config` — no DB error handling on `verifyWebhook`)
can be addressed as a follow-up if desired; it does not block merge.
