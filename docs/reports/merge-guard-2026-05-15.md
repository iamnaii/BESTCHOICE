# Pre-Merge Guard Report — 2026-05-15

Generated by Pre-Merge Guard agent. Reviewed the 3 most recently active unmerged branches (selected by latest commit timestamp from 388 total unmerged branches).

---

## Branch 1: `feat/asset-ui-polish-pr2b`

**Author**: Akenarin Kongdach  
**Last commit**: 2026-05-15 15:22 BKK  
**Commits on branch**: 8 commits  

### File Changes Summary
```
12 files changed, 1019 insertions(+), 94 deletions(-)

apps/web/src/pages/assets/AssetEntryPage.tsx           +43
apps/web/src/pages/assets/AssetRegisterPage.tsx        +117
apps/web/src/pages/assets/AssetSummaryReportPage.tsx   +120
apps/web/src/pages/assets/AssetsListPage.tsx           +25
apps/web/src/pages/assets/components/AssetEntrySection2Cost.tsx +4
apps/web/src/pages/assets/components/AssetStatusBadge.tsx +22
apps/web/src/pages/assets/__tests__/no-hardcoded-account-name.test.ts (NEW +63)
apps/web/src/pages/assets/hooks/useAssetCalculation.ts +50
apps/web/src/pages/assets/hooks/useDisposalCalculation.ts +60
apps/web/src/pages/depreciation/DepreciationPage.tsx   +9
docs/plans/2026-05-15-asset-ui-polish-pr2b.md          (NEW +392)
docs/plans/2026-05-15-asset-ui-polish-pr2b-design.md   (NEW +208)
```

### Issues Found

#### Critical
_None._

#### Warning
_None._

#### Info

| # | File | Issue |
|---|------|-------|
| I1 | `apps/web/src/pages/assets/AssetSummaryReportPage.tsx` | File is 312 lines — within acceptable range but growing. |
| I2 | `docs/plans/*.md` | Two large plan docs (392 + 208 lines) committed to `docs/plans/`. These are design-phase artifacts; fine to keep but verify they are not blocking future branch clarity. |

### Positive Highlights
- **Anti-regression test** added (`no-hardcoded-account-name.test.ts`) using Vite `?raw` import to statically assert that `useAssetCalculation` and `useDisposalCalculation` hooks never use hardcoded `accountName` strings — prevents regression of the P13 SSOT fix.
- No hardcoded hex colors introduced (`text-gray-*`, `bg-white`, `#RRGGBB` all absent).
- No raw `fetch()` calls — API calls route through `api.get()`/`api.post()`.
- No new controllers → guard/role check N/A.

### Recommendation: ✅ APPROVE

---

## Branch 2: `fix/payments-6-critical-gl-bugs`

**Author**: Akenarin Kongdach  
**Last commit**: 2026-05-15 01:07 BKK  
**Commits on branch**: 7 commits  

### File Changes Summary
```
24 files changed, 1020 insertions(+), 203 deletions(-)

apps/api/src/modules/payments/payments.service.ts      +206 (now 2026 lines total)
apps/api/src/modules/paysolutions/paysolutions.service.ts +124
apps/api/src/modules/paysolutions/paysolutions.controller.ts +38
apps/api/src/modules/receipts/receipts.service.ts      +52
apps/api/src/modules/payments/payments.service.spec.ts +177
apps/api/src/modules/paysolutions/paysolutions.service.spec.ts net -31
apps/api/src/modules/installments/reschedule.service.ts +22
apps/api/src/modules/journal/cpa-templates/early-payoff-jp4.template.ts +22
apps/api/src/modules/journal/cpa-templates/payment-receipt-2b.template.ts +23
apps/api/src/modules/journal/cpa-templates/vat-60day-reversal.template.ts +53
apps/api/src/assets/fonts/NotoSansThai-VF.ttf          (NEW — binary, 218 KB)
apps/api/src/assets/fonts/Sriracha-Regular.ttf         (NEW — binary, 319 KB)
apps/api/src/assets/fonts/embedded-fonts.ts            (NEW +60)
apps/api/src/utils/thai-date.util.ts                   +89
apps/api/src/utils/thai-date.util.spec.ts              (NEW +86)
apps/web/src/pages/PaymentsPage/index.tsx              +6
apps/web/src/components/RecordPaymentWizard.tsx        +6
```

### Issues Found

#### Critical
_None._

#### Warning

| # | File | Issue |
|---|------|-------|
| W1 | `apps/api/src/modules/receipts/receipts.service.ts` | `toNumber()` called on Decimal values for PDF generation: `const total = toDec(receipt.amount).toNumber()`. The code routes through `toDec()` helper for arithmetic (Decimal + Decimal), then converts to `number` for display/HTML string output only. This is safe for PDF rendering at `.toFixed(2)` precision but reviewers should verify no arithmetic is performed on the `number` result post-conversion. **Comment in code explains the intent** (I2 fix) — acceptable but warrants explicit sign-off. |
| W2 | `apps/api/src/modules/payments/payments.service.ts` | File is now **2026 lines** — significantly over the 500-line info threshold. No new feature added (all fixes), but this file should be split in a dedicated refactor ticket. |

#### Info

| # | File | Issue |
|---|------|-------|
| I1 | Multiple spec files | `(l: any)` type in lambda expressions inside test files (`payments.service.spec.ts`). Acceptable in test context — not production code. |
| I2 | `apps/api/src/assets/fonts/` | Two binary font files committed (218 KB + 319 KB). These are intentional — required for Thai PDF rendering without external network calls in Cloud Run (C5 fix). Large binary check passes. |

### Positive Highlights
- `paysolutions.controller.ts` now re-throws on JE failure + captures to Sentry with `module=paysolutions, action=payment_callback_je_failure` tags — critical for manual reconciliation when PaySolutions exhausts 3 retries.
- New `waiveLateFee`, `getActivePartialQr`, `cancelPartialQr` endpoints all retain existing `@Roles()` decorators — no guard regression.
- Branch-access bypass fix (W1 explicit `validateBranchAccessByPayment` on paymentId-keyed routes) closes a real security gap where BranchGuard at class level did not fire for routes carrying `paymentId` instead of `branchId`.
- `deletedAt: null` correctly added to previously-missing query (`I6 fix: include deletedAt: null` comment present).
- Thai font self-hosting avoids 8-second `networkidle0` stall on Cloud Run — good operational improvement.

### Recommendation: 🔍 REVIEW

> **W1** requires explicit reviewer sign-off: confirm that post-`toNumber()` values in `receipts.service.ts` are used **only** for string formatting (`.toFixed(2)`, template literals) and not for any arithmetic that feeds back into DB writes.
> **W2** file size should be tracked in a follow-up refactor ticket.

---

## Branch 3: `fix/expenses-6-critical-gl-bugs`

**Author**: Akenarin Kongdach  
**Last commit**: 2026-05-15 01:03 BKK  
**Commits on branch**: 7 commits  

### File Changes Summary
```
23 files changed, 1822 insertions(+), 111 deletions(-)

apps/api/src/modules/expense-documents/expense-documents.service.ts  +362 (now 1517 lines)
apps/api/src/modules/journal/cpa-templates/vendor-settlement.template.ts +104
apps/api/src/modules/journal/journal-auto.service.ts   +32
apps/api/src/modules/journal/utils/wht-form-type.ts    (NEW +41)
apps/api/src/modules/expense-documents/dto/create-payroll.dto.ts +14
apps/api/src/modules/expense-documents/services/doc-number.service.ts +23
apps/api/src/modules/expense-documents/services/je-preview.service.ts +92
apps/api/src/modules/journal/cpa-templates/expense-accrual.template.ts +8
apps/api/src/modules/journal/cpa-templates/expense-same-day.template.ts +31
apps/api/src/modules/journal/cpa-templates/credit-note.template.ts +14
apps/web/src/pages/PaymentVoucherPage.tsx              +88 (now ~238 lines)
apps/web/src/pages/PaymentVoucherPage.test.tsx         (NEW +110)
apps/web/src/components/expense-form-v4/ExpenseFormV4.tsx +26
(+ 10 spec/test files)
```

### Issues Found

#### Critical
_None._

#### Warning

| # | File | Issue |
|---|------|-------|
| W1 | `apps/api/src/modules/expense-documents/expense-documents.service.ts` | File is now **1517 lines** — significantly over the 500-line info threshold. This is a pure bug-fix branch, so splitting is deferred, but a dedicated refactor ticket should be created. |

#### Info

| # | File | Issue |
|---|------|-------|
| I1 | `expense-documents.service.spec.ts` | `let prisma: any` — acceptable in test setup/mock context. |
| I2 | `apps/api/src/modules/journal/utils/wht-form-type.ts` | New utility file (41 lines) — correctly placed in `utils/`, has its own spec file (`wht-form-type.spec.ts`). |

### Positive Highlights
- **C8 idempotency fix** in `vendor-settlement.template.ts`: cheap `select journalEntryId` short-circuit BEFORE heavy join — prevents double-credit on concurrent webhook retries. Pattern mirrors `expense-accrual`/`expense-same-day` templates — consistent.
- **C11 SSO cap** enforced with `@Max(750, { message: 'SSO ต่อคนไม่เกิน 750 บาท/เดือน...' })` — Thai validation message present, correct per Thai SSO law.
- **C12 WHT routing symmetry**: `assertWhtFormType()` guard extended to SE / CN / PAYROLL — closes the routing hole that existed in non-EX document types.
- **V15 enforcement**: `ExpenseDocumentsService.post()` rejects `DRAFT → ACCRUAL` when `withholdingTax > 0` — prevents misfiling ภงด. period per ป.รัษฎากร ม.50.
- All new queries include `deletedAt: null` filters (`where: { companyCode: 'SHOP', deletedAt: null }` etc.).
- No new controllers → guard/role check N/A.
- `PaymentVoucherPage.test.tsx` is a new test file covering the frontend page — good coverage addition.

### Recommendation: ✅ APPROVE

> Track **W1** file size as a follow-up refactor ticket.

---

## Summary Table

| Branch | Files | +Lines | Critical | Warning | Info | Recommendation |
|--------|-------|--------|----------|---------|------|----------------|
| `feat/asset-ui-polish-pr2b` | 12 | +1019 | 0 | 0 | 2 | ✅ APPROVE |
| `fix/payments-6-critical-gl-bugs` | 24 | +1020 | 0 | 2 | 2 | 🔍 REVIEW |
| `fix/expenses-6-critical-gl-bugs` | 23 | +1822 | 0 | 1 | 2 | ✅ APPROVE |

### Action Items (shared)
- [ ] Open a refactor ticket to split `payments.service.ts` (2026 lines) into sub-services
- [ ] Open a refactor ticket to split `expense-documents.service.ts` (1517 lines) into sub-services
- [ ] For `fix/payments-6-critical-gl-bugs`: reviewer explicitly signs off on W1 (`toNumber()` display-only usage in `receipts.service.ts`)
