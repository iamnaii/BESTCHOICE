# Pre-Merge Guard Report — 2026-05-01 (v6)

Generated by Pre-Merge Guard agent. Reviewed 3 unmerged branches not yet covered by today's earlier guard runs.

---

## Branch 1: `feat/accounting-phase-a1b-intercompany-je`

**Author**: Akenarin Kongdach  
**Last commit**: 2026-04-29 18:10 +0700  
**Commits ahead of main**: 13  
**Commits behind main**: 0 ✓

### File Changes Summary (23 files, +3835/-181)

| File | Change |
|------|--------|
| `apps/api/src/modules/journal/journal-auto.service.ts` | Phase A.1b: payment JE now posts split FINANCE+SHOP entries; adds `createCustomerCreditOverpaymentJournal` + `createCreditAllocationJournal` |
| `apps/api/src/modules/journal/inter-company-link.util.ts` | NEW — `[IC-<uuid>]` description prefix to correlate paired JEs |
| `apps/api/src/modules/repossessions/repossessions.service.ts` | Repo resale JE post-commit (non-blocking); adds LOSS_ON_REPO_RESALE line |
| `apps/api/src/modules/repossessions/repossessions.controller.ts` | Adds `@CurrentUser()` on PATCH method for audit attribution |
| `apps/api/src/modules/accounting/bad-debt.service.ts` | Captures pre-reversal provision amounts for delta JE |
| `apps/api/src/modules/contracts/contract-payment.service.ts` | Adds `resolveShopCompanyId()` helper |
| `apps/api/src/modules/contracts/contract-workflow.service.ts` | Resolves SHOP company for A.1b activation JE |
| `apps/api/src/modules/payments/payments.service.ts` | Passes `shopCompanyId` + `financeCompanyId` to payment JE |
| Docs/plans | Two planning docs (+1959 lines) |

### Security Checklist

- No new controllers added → no new guard exposure
- Existing `@UseGuards(JwtAuthGuard, RolesGuard)` on both `RepossessionsController` and `AccountingController` — unchanged ✓
- All `@Roles()` decorators present on modified methods ✓
- No `$queryRaw` or `$queryRawUnsafe` introduced ✓
- No hardcoded secrets or API keys ✓
- All `companyInfo.findFirst` queries include `deletedAt: null` ✓
- No soft-delete queries missing `deletedAt: null` ✓

### Issues Found

#### Warning

**W1 — `Number()` on Decimal before re-wrapping (minor precision risk)**  
File: `apps/api/src/modules/repossessions/repossessions.service.ts`  
```typescript
const resellPrice = new Prisma.Decimal(
  dto.resellPrice ?? Number(repo.resellPrice ?? 0),
);
```
`repo.resellPrice` is a `Prisma.Decimal` from the DB. Calling `Number()` on it converts via string representation — safe for typical phone prices (≤10M THB, well within JS safe integer range), but the idiomatic pattern is `new Prisma.Decimal(dto.resellPrice ?? repo.resellPrice ?? 0)`, skipping the intermediate `Number()`. Low risk but inconsistent with the v4 hardening that eliminated 53 such patterns.

**Fix**: Replace `Number(repo.resellPrice ?? 0)` with `repo.resellPrice ?? 0` — Prisma.Decimal constructor accepts Decimal directly.

#### Info

**I1 — `.toNumber()` on journal line amounts is intentional**  
`createAndPost()` interface explicitly takes `{ debit: number; credit: number }` and re-wraps internally as `new Decimal(l.debit)` for the balance assertion. Pattern is correct and established in the codebase.

**I2 — Backward-compat `companyId` alias is safe**  
The `@deprecated` param on `createPaymentJournal` resolves via fallback chain and is clearly documented. Existing callers continue to work; new callers should use `financeCompanyId`.

**I3 — `inter-company-link.util.ts` approach is pragmatic**  
Embedding IC link ID in the JE description avoids a new FK while staying queryable. The regex is correct and handles case-insensitivity. Note: this means IC linking is broken if the description field is later truncated — the `JournalEntry.description` column should be verified to be `TEXT` (not `VARCHAR(255)`).

### Recommendation: **REVIEW**

One warning (W1, low risk) that should be cleaned up before merge. No blockers.

---

## Branch 2: `feat/collections-partial-payment-escalate`

**Author**: Akenarin Kongdach  
**Last commit**: 2026-04-28 12:02 +0700  
**Commits ahead of main**: 3 (bulk of diff is `package-lock.json` + weekly deps update)  
**Commits behind main**: 0 ✓

### File Changes Summary (30 files, +3817/-1936)

*Note: ~3760 lines of diff are `package-lock.json` churn from the weekly deps merge. Actual feature code is ~57 lines.*

| File | Change |
|------|--------|
| `apps/api/src/modules/overdue/dto/partial-payment-reschedule.dto.ts` | NEW — `PartialPaymentRescheduleDto` (full + partial pay) |
| `apps/api/src/modules/overdue/dto/escalate.dto.ts` | NEW — `EscalateDto` (LETTER/MDM/LEGAL actions) |
| `apps/api/src/modules/overdue/overdue.controller.ts` | Adds POST `/:contractId/partial-payment-reschedule` + POST `/:contractId/escalate` |
| `apps/api/src/modules/overdue/overdue.service.ts` | Implements `partialPaymentReschedule()` + `escalate()` |
| `apps/api/src/modules/overdue/queue.service.ts` | Minor update |
| `apps/web/src/pages/CollectionsPage/hooks/usePartialPaymentReschedule.ts` | NEW — mutation hook |
| `apps/web/src/pages/CollectionsPage/hooks/useEscalate.ts` | NEW — mutation hook |
| `apps/web/src/pages/CollectionsPage/components/PartialPaymentRescheduleDialog.tsx` | NEW — dialog UI |

### Security Checklist

- Class-level `@UseGuards(JwtAuthGuard, RolesGuard, BranchGuard)` on `OverdueController` — preserved ✓
- Both new endpoints have `@Roles('OWNER', 'BRANCH_MANAGER', 'FINANCE_MANAGER', 'SALES')` ✓
- LEGAL escalation SoD gated inside service via role check ✓
- `EscalateDto.reason` has `@MinLength(5)` + `@MaxLength(500)` — prevents empty/oversized inputs ✓
- `PartialPaymentRescheduleDto.evidenceUrl` has `@Matches(/^https:\/\/.+/)` — prevents non-HTTPS URLs ✓
- All DTOs use Thai validation messages ✓
- No raw `fetch()` in frontend hooks — uses `api.post()` ✓
- Both hooks call `queryClient.invalidateQueries()` after mutation ✓

### Issues Found

#### Warning

**W1 — `outstandingAfter` stored as `number` for `settlementAmount`**  
File: `apps/api/src/modules/overdue/overdue.service.ts`  
```typescript
const outstandingAfter = outstandingBefore.sub(paid).toNumber();
// ...
callLog = await this.logContact(contractId, callerId, {
  settlementAmount: outstandingAfter,  // number, not Decimal
  ...
});
```
`settlementAmount` on `CallLog` is `Decimal @db.Decimal(12,2)` in the schema. Prisma accepts `number` at runtime for Decimal fields, so this works, but it's inconsistent with the explicit `Prisma.Decimal` pattern enforced in v4 hardening. The value is bounded by the contract's outstanding amount (≤ original price), so precision loss is negligible in practice.

**Fix (optional)**: `settlementAmount: outstandingBefore.sub(paid)` — pass the Decimal directly to match LogContactDto type annotation.

#### Info

**I1 — `overpayment` guard path**  
The service rejects `amountPaid > outstanding` at runtime, but `PartialPaymentRescheduleDto` only validates `@Min(0.01)`. A client could still attempt an over-payment; the service guard handles it, but an explicit `@Max()` with a note would make the DTO self-documenting.

### Recommendation: **APPROVE**

W1 is very low risk (bounded amount, Prisma runtime accepts it). Code is clean, well-structured, all security patterns correct. Can merge without W1 fix; fix is recommended but not blocking.

---

## Branch 3: `feat/collections-promise-to-pay-lifecycle`

**Author**: Akenarin Kongdach  
**Last commit**: 2026-04-28 10:59 +0700  
**Commits ahead of main**: 33  
**Commits behind main**: 0 ✓

### File Changes Summary (44 files, +7373/-525)

| File | Change |
|------|--------|
| `apps/api/prisma/migrations/20260612000000_add_promise_lifecycle/migration.sql` | NEW — adds `PromiseSlot` table + lifecycle fields on `CallLog` + `keptPromiseCount` on `Contract` |
| `apps/api/prisma/schema.prisma` | NEW `PromiseSlot` model, lifecycle fields on `CallLog` |
| `apps/api/src/modules/overdue/promise.service.ts` | NEW — `PromiseService` with `createPromise()` (Serializable tx) + `findActivePromise()` + `calcCycleDeadline()` |
| `apps/api/src/modules/overdue/crons/promise-resolution.cron.ts` | NEW — replaces `broken-promise.cron`; per-slot kept/broken resolution |
| `apps/api/src/modules/overdue/crons/no-promise-lock.cron.ts` | NEW — 2-consecutive-no-contact → MDM auto-lock |
| `apps/api/src/modules/overdue/mdm-lock.service.ts` | `autoLock()` + `autoUnlock()` idempotent paths |
| `apps/api/src/modules/overdue/overdue.service.ts` | New P2P endpoints; N-slot `createPromise()` wiring; `(active as any)` casts |
| `apps/api/src/modules/overdue/overdue.controller.ts` | Adds GET `contracts/:id/cycle-deadline` + GET `contracts/:id/overdue-installments` |
| `apps/api/src/modules/overdue/installment-allocator.util.ts` | NEW — FIFO `allocateFifo()` using `Prisma.Decimal` ✓ |
| `apps/api/src/modules/payments/payments.service.ts` | `checkPromiseAfterPayment()` hook (non-blocking post-commit) |
| `apps/web/src/pages/CollectionsPage/components/ContactLogDialog.tsx` | N-slot manager UI |
| `apps/web/src/pages/CollectionsPage/components/SupersedePromiseConfirmDialog.tsx` | NEW — supersede confirmation |
| `apps/web/src/pages/CollectionsPage/components/InstallmentPickerPopover.tsx` | NEW — FIFO installment selector |
| `apps/web/src/pages/CollectionsPage/tabs/PromiseTab.tsx` | Slot status chips + cycle countdown |
| Backfill script | `apps/api/scripts/backfill-promise-slots.ts` |

### Security Checklist

- `OverdueController` class-level `@UseGuards(JwtAuthGuard, RolesGuard, BranchGuard)` — unchanged ✓
- Both new GET endpoints have `@Roles('OWNER', 'BRANCH_MANAGER', 'FINANCE_MANAGER', 'SALES', 'ACCOUNTANT')` ✓
- No new public endpoints ✓
- No hardcoded secrets ✓
- `no-promise-lock.cron.ts` and `promise-resolution.cron.ts`: both `deletedAt: null` in all queries ✓
- `createPromise()` uses `Serializable` isolation to prevent race conditions ✓
- `checkPromiseAfterPayment()` uses guarded `updateMany` (only first caller promotes) ✓
- `allocateFifo()` uses `Prisma.Decimal` throughout ✓
- No `fetch()` in frontend components ✓

### Issues Found

#### Warning

**W1 — `settlementAmount: Number(...) as never` bypasses type safety**  
File: `apps/api/src/modules/overdue/overdue.service.ts`  
```typescript
{
  settlementDate: new Date(dto.settlementDate),
  settlementAmount: Number(dto.settlementAmount ?? 0),  // number, not Decimal
}
// ...passed to PromiseService → stored via:
settlementAmount: s.settlementAmount as never,  // cast bypasses Prisma Decimal type
```
The `as never` cast on `createMany` masks a type mismatch (JS number stored in `Decimal(12,2)` column). Prisma accepts numbers at runtime for Decimal fields, so it works, but the cast is a red flag that this was a type-check workaround rather than a proper fix. Consistent pattern from `installment-allocator.util.ts` uses `Prisma.Decimal` directly.

**Fix**: Change to `settlementAmount: new Prisma.Decimal(dto.settlementAmount ?? 0)` in the slot array, and remove `as never` from `createMany`.

**W2 — Multiple `(active as any)` casts**  
File: `apps/api/src/modules/overdue/overdue.service.ts`  
Several response-shaping blocks cast the Prisma result to `any` to access fields from the `slots` relation include:
```typescript
settlementAmount: Number((active as any).settlementAmount ?? 0),
const deadline = (active as any)?.cycleDeadline ? ...
const activeSlots: Array<...> = (active as any)?.slots ?? [];
```
These exist because `findActivePromise()` in `PromiseService` returns the inferred Prisma type, but `overdue.service.ts` calls `findFirst` separately without the same `include`. The proper fix is to either share the typed return from `PromiseService.findActivePromise()` or use Prisma's `Prisma.CallLogGetPayload<{include: {slots: true}}>` type.

**Fix**: Import and use `Awaited<ReturnType<PromiseService['findActivePromise']>>` for the active promise variable type.

#### Info

**I1 — Migration timestamp `20260612` is future-dated**  
File: `apps/api/prisma/migrations/20260612000000_add_promise_lifecycle/migration.sql`  
Today is 2026-05-01; the migration is dated 2026-06-12 (six weeks in the future). This is intentional for ordering purposes (as established in the v5 spec rename commit `58488259`), but `prisma migrate deploy` uses the directory timestamp to determine order — if another migration is created between now and June 12, it will be ordered BEFORE this one. Coordinate with team to ensure no conflicts.

**I2 — `getSystemUserId()` in `payments.service.ts` lacks `deletedAt: null`**  
File: `apps/api/src/modules/payments/payments.service.ts`  
```typescript
const user = await this.prisma.user.findFirst({
  where: { isSystemUser: true },
  select: { id: true },
});
```
Consistent with `overdue.service.ts` `getSystemUserIdOrThrow()` which follows the same pattern. System users are not expected to be soft-deleted. Low risk but technically misses the convention.

**I3 — `paidAmount: slotAmount as unknown as Prisma.Decimal`**  
File: `apps/api/src/modules/payments/payments.service.ts`  
```typescript
data: {
  keptAt: now,
  paidAmount: slotAmount as unknown as Prisma.Decimal,  // number cast to Decimal
},
```
Same root issue as W1 — `slotAmount` is `toNumber()` result cast back to Decimal. Works at runtime; should be `new Prisma.Decimal(slotAmount)`.

**I4 — Backfill script missing error exit code**  
`apps/api/scripts/backfill-promise-slots.ts` — verify that `process.exit(1)` is called on failure so CI/CD can detect backfill errors. (Could not confirm from diff alone.)

### Recommendation: **REVIEW**

W1 and W2 are medium warnings — no functional bugs (Prisma handles number→Decimal at runtime) but the `as never` and `as any` patterns hide type errors that could surface if the schema changes. These should be fixed before merge. I3 is a minor cleanup of the same root cause. No critical security or data integrity issues.

---

## Summary Table

| Branch | Critical | Warning | Info | Recommendation |
|--------|----------|---------|------|----------------|
| `feat/accounting-phase-a1b-intercompany-je` | 0 ✓ | 1 (Number() on Decimal) | 3 | **REVIEW** |
| `feat/collections-partial-payment-escalate` | 0 ✓ | 1 (toNumber for settlement) | 1 | **APPROVE** |
| `feat/collections-promise-to-pay-lifecycle` | 0 ✓ | 2 (as never / as any casts) | 4 | **REVIEW** |

**No branches are BLOCKed.** `feat/collections-partial-payment-escalate` is clean to merge immediately. The other two have type-safety warnings that should be addressed — none are runtime data-integrity bugs given Prisma's runtime coercion, but the `as never` pattern in the P2P branch indicates a suppressed type error that should be fixed properly.
